---
title: Intro to VOPS - Houdini Beginner Tutorial
source: YouTube
url: https://www.youtube.com/watch?v=Kx3CJJei_Vs
author: Voxyde VFX
ingested: 2026-06-23
houdini_version: "any modern (H18+)"
tags: [vops, attribute-vop, math, noise, ramp, fit-range, bind, beginner]
extraction_status: complete
frames_dir: tutorials/frames/intro-to-vops---houdini-beginner-tutorial/
frame_count: 6
---

# Intro to VOPS - Houdini Beginner Tutorial

**Source:** [YouTube](https://www.youtube.com/watch?v=Kx3CJJei_Vs)
**Author:** Voxyde VFX
**Duration:** 58m58s | 1 section(s)

---

## Raw Data (for Claude Code extraction)


### Full Content [0:00]
**Transcript:** Kind: captions Language: en welcome to intro to vops part 1. in this part we are going to cover the most common operations that you'll be using on a daily basis these are the foundational building blocks that will tie into any type of VFX shot that you're working on so let's get started with vops and this will sort of tie in with the intro to Houdini course so if you're brand new to Houdini I will recommend you watch that video first and just so you know how to navigate the scene and what different networks are and what they do so the very Basics anyway we will need a geometry to get started so I will drop down a geometry container here and I will step inside and I can drop down a Taurus here and I might increase the resolution so I'll increase the rows and columns and if I press Tab and search for vop we can see that we have a few different types here so we have the attribute 4.4 primitive op vertex vop volume vop so most of these are just the attribute vob if I drop an attribute warp and I set this to run over Primitives I am essentially recreating the Primitive valve so if I drop a primitive verb node we can see that it's exactly the same so this attribute valp it's exactly the same as this primitive up the only difference being the class that it runs over so by default an attribute vault is essentially just the point warp if I also drop down a point drop we can see that these are exactly the same the only different one is going to be the volume vop which can only operate on volumes so not on regular geometry but this will be a different topic that I will probably cover in a different chapter or maybe the volume score so we don't have to worry about this for now all I'm trying to say is that attribute vob is the one that we want to use and most of the time and this is personal preference but if I want to use a primitive verb I will still just do an attribute of up and I will set it myself to Primitives so if we understand how the attribute vob works at the point level everything will transfer over when we want to do operations Over The Primitives or the vertices it's a little different again for the volume up but it's still kind of the same everything will still transfer over but with a small tweak to get everything to work for the volume low but again don't worry about that let's go ahead and connect our attribute to our Taurus let's preview the result over here and let's dive inside so we have inputs on our left and outputs on our right and I can go ahead and plug our input P into our outp so the input position into our out position and in between I can do operations so while selecting this wire I can hit Tab and do an add node here and I can drop a constant and set this to a tree float and I can add this to our input too and now I can use the values here to move around our geometry and we can see that everything is working nicely so again we have different attribute types Vector float integer string basically float is just a simple number it can be any number so 5.64 integers can only be whole numbers so either 5 or 6 and vectors are made up of more than one component so it can either be a vector made out of two components three components or four components the most common one is the one made out of three components so the one that we send that here this tree float vector and this will be your position your normals your velocity so this really comes up a lot the three type float Vector so we can go ahead and we can use these values to move our Doris around but just like I said in the intro to Houdini series and I can't stress this enough this is a per Point operation we're not really moving this entire geometry around we are simply adding this constant value to each individual point and it looks like we are moving the entire geometry because we are adding the same value to all of the points but we're still operating on individual points so this is what we have to know in the back of our head and sometimes if you're doing things in here and you're not getting the expected result you can look at your geometry and you can only isolate one point and run the operations while only focusing on one individual point and it'll be much easier to troubleshoot your problems this way or I can even go up and I can select the Taurus and let's frame on this by pressing F and I can go ahead and press s press 2 to go in my point mode I can grab one of these points I can press delete and I can choose here delete non-selected on this blast notes now we are only left with one point and in fact I will press D go to the geometry and increase the point size and for example now I can only focus on this one single point so all of these operations here inside our attribute vob I can focus on what they do on one individual point and if something's not working it will be easier to just follow the chain of operations over here and focusing only on one point so this is something to keep in mind let's go up I'll go ahead and delete this blast node and disable the point mode Let's go back and let's keep going so we have a constant here which is going to be the three float Vector type and we have our values over here that we can modify but let's say I want to map one of these values to something else so I will get rid of this constant the node that I will do a float to Vector node let's drop this and let's plug this into our ad so I can kind of do the same thing with this node as we did with our constant we can use these values to modify so we still have access to all of the individual values but now I can map these inputs to something else so I can use the time value here and again these three time time Inc and frame are going to be Global variables that any geometry will have access to so I'll grab the time and I'll set this to be my second component here so the Y Dimension so if I if I plug this here and check my animation and let's enable the real-time toggle so we can preview this in real time if I play we can see now that our Taurus starts to fly upwards and using the time or the frame to drive animations inside the vob context is something that we do all the time and later I'll show you more advanced setups with this but we can get really complex with these values here so anyway we can plug this into any of our components and we will get the expected results so I can also plug this on the z-axis and now it moves on the Z so we can create a vector from Individual float values but we can also split a vector uh two individual float values so I can drop a vector to float which is going to be the flow to vectors broader and for example I can grab our position and I can split the position to individual components so if I plug this here a common thing that we usually do is we split the position into individual components and then we put it back so I can do a float to vector and I will hook all of these as they are and if I plug this directly into our P nothing really changes it's still just the Taurus at the origin where it was placed but now and I'll get rid of this now I have access to the individual components and for example I can add to just the Y component here so I will drag down this wire drop an add node and I will add the time to this so now we have the same thing that we had earlier which is just this translation on the Y value and for example I can do the same thing on the x axis here so the first component let's do another anode and add our time and we can see now that we also introduced some translation on the X Dimension but let's say that I want to move faster on the Y component so our second component I can grab this time and I can do a multiply here and I can drop a constant like so and use it as my second input or I can middle Mouse click on where it says input 2 and I can choose constant so this will automatically create a custom value and hook it to our multiply and if I want this to go twice as fast on the Y value I can set this constant here to a value of 2. so now it's going to to be twice as fast on the y or if I want it to be slower I can reduce this to maybe 0.2 so now it's uh five times slower on the y or instead of multiply I could do a divide and if I promote this to a constant I can set this to a value of 5 here so now this will move five times slower essentially what we just did with the multiply so we have access to all of the basic math operations add subtract multiply and divide and if I know ahead of time that I only want to let's say for example I only want to multiply with the constant I can I'll get rid of this and I can drop down a multiply constant node and I can use this multiplier value here to set the constant by which we want to multiply so we can see now this is five times faster on the Y so it works so like I said this is when we know we only want to do a simple constant add or subtract or multiply and when we want to do more more than one operation so if I want to add more values here we can see that this can hold as many inputs as necessary so I could add a lot of values to this so if I want to do more operations I would use the regular multiply node but if I just want to do a simple constant add I will do an ADD const and use this instead so this is also something that we use a lot of the time also we can promote any parameter to the top level so for example if I drop a multiply over here I can middle Mouse click or where it says input 2 and I can choose promote parameter so now if I go up I will have this extra control here at the top level so at the geometry context and I can increase this I can set the multiply value here so if I set this to a value of 2 again it will move twice as fast on the Y and I can increase this and I can make it move faster and it says input number two but really I can go inside here I can click on this handle here and I can give this really any other name so where it says label I can rename this to speed for example or speed Y and for example I can also go to I can also go to where I'm adding the time to our X component so I can also do a multiply here I can promote the second input and I can rename this to speed X and if I go up now I have individual controls over the speed on both directions so I can set the speed X to negative value and I can start to combine these values and this is very useful for us because we can end up with a quite a heavy Network or for example when I want to use a noise and let's say that I want to use a unified noise we can see that there are really a lot of parameters here and we usually only need one or two of these parameters so I would be able to only bring the f scale so I can promote this parameter and I can go up and over here I can only play around with this value here at the top level so when I'm doing a simulation and I want to check values and try different values I don't have to each time go inside and look for the thing that I want to adjust I can just promote it to the top level and grab it from here and this also helps us stay a little bit more organized so I'll go inside and I'll get rid of this unified noise and I want to introduce a couple of new nodes which are going to be the Buy end so I'll drop a bind and the bind export so I'll drop this as well and this is how we can work with attributes so the bind will bring in an attribute that we already have and the bind export can update an attribute that we already have or can export a new attribute so it can create a new attribute these two nodes are essentially the same but we can see that the bind export has some different values set here so for example if I go to my bind so the regular buy node and I turn on use this node to set parameter attributes and I set the export here to always we essentially recreated the bind export node so the bind export node is just a handy preset that Houdini provides so let me get rid of this and drop a new bind import node so I can use these to work with other attributes with any kind of attribute so if we create outside a custom attribute we would be able to bring the same with our buy nodes but we can also affect the attributes that we already have so all of these over here PV Force age all of these and all of the outputs here can be replaced by these bind node so for example I can go to this bind so the regular buy another binder export and if I set this to P I would be able to replace actually this P here from our vob Global input so if I place this here we can see that this still works and I would be able to also export this to the same P value so this bind export okay this bind export which has these options set here can I can type the name to P and now we can see that something changed but when I plug the result of this Vector into our P we get the same result and in fact I could now just get rid of this entirely so I can just delete this and we will still have the same results so it works on the built-in attributes as well and even time as well so if I do another bind I can replace the time with this bind here so I can set this to time and this is with a capital T so we have to make sure that we write it like this so this is a case sensitive I can grab the wires that we had and I can just connect this to our time so now essentially I would be able to get rid of this as well and we can see that we still have the exact same result but of course we can use these bind nodes now so for example we can use this to update our P so an attribute that already exists but I can also use this bind export to write this to a new attribute so for example let's first set this type to A3 flow so we have to make sure that we are consistent between the types of attributes and let's just name this we can use whatever name we want here but uh let's just say my Vector so now if I go to the geometry spreadsheet we have this my Vector attribute which is the result of all of our operations here and we can see that our p is the same attribute but now for example if I want to do further modifications so this I can drop down a multiply constant and if I increase this value now we see that we update the my Vector attribute we are actually creating this and exporting this over here we are not really updating it but hopefully you get the point and I'll get rid of this and go to back to my viewport so again this is a per Point operation meaning that if I want to bind an attribute so I want to bring in an attribute that attribute has to exist on the point and this bind will only work on our geometry so for example if I were to go up and I grab this Taurus and I do some modifications to this so let's just say that we do a transform here and I just move this around in space and I plug this in the attribute vob when I go back inside this bind attribute is going to look for the p in our first input so it doesn't know how to grab the position information from the second input so that's what I mean that by default this will bring in the attribute on our own geometry and to read in any attribute from another geometry we have to use an import Point attribute and let's go ahead and recreate what we had originally with our input and output parameters so I will just drop another geometry Global parameters so this is also good to know that whatever you do you can always bring these two blocks back and I will connect the P here get rid of the bind and let's reconnect this to our time just so uh just so we are working with the defaults and over here let's do a geometry Vault output right and we can hook this here and get rid of this bind so let's take a look at this import Point attribute so we have to first point this to the geometry that we want to look at which will be our second input so this up input 2 and again all of these orange values simply represent the inputs that we have over here so one two three and four will be open input One open input 2 and so on so we have to point this to our op input 2 which is our moved Taurus and in our import Point attribute window we are going to look let's say that we want to use the same position so we are going to grab the p and this input mode is set to file so it's going to use this input 2 connection that we made here but for example I could also set this to the second input and now I wouldn't need this connection it would work like this but usually I think people prefer to have the connection so I will just uh hook this back and set this mode to file so that was just to let you know and let me actually get rid of all of these for now and just work on the Taurus without doing any modifications to it so the p is just plugged in the P here without any modifications all right so back to our import Point attribute we are also going to need a speedy num value and this will be a unique value that each point has and this unique value is created automatically for us when we create any type of geometry and we can also preview the Speedy num if I turn on the point number display here so we can see that each point will have a unique value that's assigned to it and this PD num has to exist no matter what you are doing with the geometry in fact if we go to the geometry spreadsheet we can see that we do have the position but we don't have a Pity num attribute showing over here and this is because it's going to be this value over here on the very left so all of these values that go from 0 to whatever over here these are going to be our PD noun values so this is something that must exist on any geometry and and this is an attribute you can delete either so let's go back to the scene view now if I go up because this is essentially so our original Taurus without being moved and our new Taurus that we moved uh let's take a look and disable the point number these are essentially still the same geometry so each point will have the same pity num value and now I can go back inside our attribute vob and we can grab our own Point number so the pity num value that we have on our current geometry as it is and I can use this as the same point number on our second geometry so the transform Taurus and I can point it over here so this import point is going to look in our second input and it's going to go over each individual point and if I turn back on my point display for the point uh let's say 5414 it's going to look in the other geometry for the same point number so 5414 and it's going to grab that position value and it's going to do this for all the points so now as a result if I plug the result of this import Point attribute in our P we can see that we grab the position from our second input and let's maybe turn off the point display so we grab the position from the second input and we set our own position to that value and again this only works because we are basically running over the same geometry and the point numbers match so this is the only reason we can do this and for example if I were to go up and let's say that something happened to our second geometry and I will go ahead and I'll get rid of some point so I'll add a blessed node here let's set this to point and let's grab a few points press enter so now we so now we got rid of some of these points when I did this operation Houdini gave us a new completely different set of Point numbers for all of our existing points so now when I go back to my attribute vob we can see that this no longer works because this import Point attribute can no longer find a point that has the same point number as our own now there are certain workarounds to solve this issue but we will get into that a bit later because I don't want to introduce too many things right as we're starting so I'll go up and get rid of this blast node and I want to introduce a new node here which is going to be the mix node so if I drop down the mixed node I can plug the original position in the first input and this new position that we grab from the second input I will place this as input 2 and if I plug the blend result in our P now we are blending between these two values so I can use this bias value here to control how much influence we want one input to have over the other now it's also useful to know that this bias values can go over values of one so we are not limited to 0 and 1 and sometimes this is useful but most of the time we are going to be operating with the zero to one range so this node is super powerful because we can map different values to this bias so just for example if I were to plug the time inside this bias we can see that over a duration of 24 frames which is essentially the value going from 0 to 1 because the time is in seconds we are blending these two geometries from one to another and for example if I want to make this slower I can do a multiply constant so all of the operations that we are now familiar with and I can slow this down so now I can make this transition happens lower so this isn't really super exciting but we can really get Advanced with this and later I will show you some cool setups that what we can do with this mix node so now we can introduce a few more of the basic type notes so a few more basic math operations and again if you have a background in programming all of these new operations will be very familiar to you let's maybe just go up and to demonstrate these other notes I will create a grid over here let's go to this and I'll press shift W to view the wireframe and I'll just increase the rows let's maybe use 200 by 200 so we have plenty of geometry to work with and I will do another attribute of up and let's step inside so first to show these other math operations I want to introduce you to a new node which is going to be displaced along normal so this is going to Simply look at our geometry and it's going to push the points along their normal so for example if I just plug the displays P inside our p and I increase this displacement amount we can see that the geometry is going up and by default any geometry in Houdini will have this normal vector and this normal Vector is probably something you have already encountered if you have experience with other 3D applications and I can go on and turn this point normal display and we can see that this is the vector basically maybe I will go up and let's reduce the rows and columns for now let's do 50 by 50 so we can see this a little bit better now this is actually stored on the vertices but it doesn't matter for now the normal is simply just a vector that is tangent to the surface of the geometry so really it's just a vector that's perpendicular to the surface so if I go back inside we can see that since all of these polygons are facing upwards in this direction when I do this this place along normal is going to bring the points along their normals so we are moving the whole geometry up so I'll go ahead and turn off this point normal display and again this isn't very exciting but what we can do is we can generate a noise value value for all of these points so now we will have an uneven displacement for all of the points so let's go ahead and drop a turbulent noise and we will cover noise separately in another lesson where we go a little bit more in depth but for now let's just drop this turbulent noise and we want to generate this from our position and this is what's actually going to generate the random values for each point is going to be based on the position again we will talk about this more later and I can use the noise values here and plug this into the either the amount or scale is pretty much going to be the same thing if I plug this into the scale I can go to the displays along normal and now I can control how much I want this noise to affect our points so this displacement amount here is sort of like a multiplier for this noise value so I can set this maybe to a value of one and now I can actually go back and let's increase our subdivisions back so let's do 200 by 200 and I will press shift W to hide my wireframe game and this will be the result so now we can see that we have this nice even displacement and this is basically a really cool way to how you can create the reins and this is uh pretty much how the height Field Works only the height field operates with volumes but it's essentially the same thing as we did over here so let's go back inside and now we can play around with the noise settings so I can reduce the frequency and I can increase the amount over here so this will be kind of like the same thing we are doing here with the displacement with the displacement amount let's go back to an amplitude of one we can increase the roughness of course or decrease it and the default noise type here is set to Alligator so the alligator noise will give us values that range from 0 to 1 and in order to show the other operations that we can do we want to work with ranges that also have negative values so for this I will set the noise type here to be a Simplex noise and if I go to my displacement amount and I increase this displacement we can see that some points are pushing downwards and some are pushing up or upwards and again if I go in my viewport and I hold spacebar and press 4 we can go to one of the side views and if I press W to view the wireframe we can see that our values that we are getting from the noise are centered around this zero value so some are going in the positive direction and some are going in the negative Direction so this is what we want let's go back to our perspective with spacebar 1. so again this noise will simply output a float value and in fact I can even preview this value if I place this in the CD and I disable lighting by clicking this icon here we can kind of see the values as they range from positive values so values that are closer to White and negative values that are closer to Black all right so let's disconnect this so for these noise values I can do the standard operation so I can do an add constant and we can see that this adds a value of 1 over the entire values or I can do a multiply constant if I want to increase the displacement that we are getting so I can increase this here which is exactly the same if I were to increase the displacement amount here so we know all that let's go ahead and introduce a new node which is going to be the negate so this negate is basically going to be a multiply operation with a constant of negative one so if I do a negate here this is sort of a quick way to get the reverse effect so if I disable this with q and I enable this now all of the values that are positives will be negative and the other way around so this is exactly like if we would do a multiply constant and we would multiply with a negative one and if I plug this here inside the scale we can see that we get the exact same thing okay so this is the negate so sometimes this is useful if you don't want to do this multiply constant by negative one but honestly sometimes I just do this negative one multiply constant operation instead of adding the negate but I just wanted to show you what the negate does and the next one that I want to bring in is going to be absolute so let's drop this in and what this will do for us let's plug this in and then plug this into our scale this will take all of the negative values and it will remove the sign so it will turn them into positive values this means that a value of negative 1 will become a value of one or a value of negative 0.5 will became will become a value of positive 0.5 and all of the positive values will still stay positive so if I go in a side view here we can see that all of the values that were negative turn positive so I can showcase this better if I turn this on and off we can see that whatever was above zero stayed the same and whatever was below zero became a positive value so now all of these that are below zero are jumping up so it's sort of like taking a negative value and just removing the minus that's in front of it so this is useful a lot of times as we can see here maybe we do not want any of these points to go below zero so this absolute value will push all of my negative values in the positive direction so it's sometimes useful all right so the next one is going to be clamp and let's just stay in this view for now so if I drop down a clamp this is pretty straightforward it will clamp the values to whatever we set over here so now we can see that no values go lower than zero which means essentially that now we have flattened all of the values that go below zero and we get this pretty cool result which kind of starts to look like a mountain range so I think you can already see that this clamp already comes in handy when we are dealing with height fields and I can increase this minimum amount so I can push everything even further up if I increase the minimum amount and also I can flatten everything down if I reduce the maximum amount so if I reduce this this will be the result so hopefully we can see that this is also extremely useful and the next I want to also show you the our node so if I drop down a power this will take our value and it will raise it to this exponent value and this is sometimes useful when you want to introduce more contrast to your values so if I decrease this exponent here we can see that the points that were high up get an even higher value and the points that are closer to zero kind of stay the same so for example in this case if I wanted to affect mostly the Peaks I could be using this power value and I can reduce this here and this would uh affect our Peaks a little bit more than our values so this power is also sometimes useful but but I don't find myself using this really on a daily basis so now I will bring in one of my all-time favorite notes which is the fit range so let's go ahead and drop this fit range into our Network so here it is and let's say that I want to scale this displacement on the z-axis so I want this to have no effect over here and I want it to have a full a full effect at the end over here so I can grab the Z component of our position and I will go ahead and from the P I will do a vector to float and the Z direction will be this third value over here and I will plug this value into our fit and if I look at this we can see that our Z position goes from a value of around negative 5 to a value of five so this will be our source Max and our source mean so inside this fit range our source mean this will be negative five and our source Max will be 5 and this will get mapped to these destination mean and destination Max values and as a result If I multiply the noise values that we get over here so if I multiply here we can also do this multiply after our P but let's just do it here so if I multiply these values and I plug this here now we can see that our noise amplitude gets this move blend from 0 to the full value which we set over here so I can still go in this displacement amount here and I can control this and it will get mapped to this new zero to one values and I can press spacebar 4 to see this better so this is what the feed range will do for us and I usually always promote one at least one of these values here so I might promote the destination Max I can middle Mouse click and promote parameter so I can go up and I can play around further with the fit range values but let's go inside and I'll get rid of this so this fit range is very useful and now to get even more control over how the values are mapped we will introduce my next favorite node which is going to to be the ramp parameter so if I type here ramp I will choose ramp parameter and let's drop this so the default type that Houdini gives us is going to be this Ram type color ramp which is this one over here and we will get to this in a second but the most used one is going to be the spline ramp float and now we will have this visual representation over how the values are mapped so I can expand this window here and now I can add new handles here and really dictate how I want the values to be mapped so let's go ahead and first I will plug the result of this fit range into the ramp and now I will plug this result of the ramp inside our multiply so replacing the fit range and we can see that nothing happens and this is because for this ramp we can't actually modify the values inside the vob network we have to step outside so I'll press U and we can see that Houdini automatically exposed this parameter for us at the top level and this is where we actually have to do the modifications to our ramp and we can see that for some reason Houdini doesn't give us by default that heel upwards graph that we had earlier and we can reset the ramp and we can resend reset any parameters if I hold down control and then middle Mouse click over the title over here where it says ramp so control emitter Mouse click this will now reset our ramp and it will give us the default that we have over here so I can also reset this and if I go up when I reset it's going to give us this upwards linear thing so by default now we have exactly what we had previous with only the feed range but now I can expand this window and I can start playing playing around with the settings so all you have to do is left click to add a new handle and we can now see how this affects our noise and I can really control how the values are mapped all the way from zero to one so I can do all kinds of stuff here usually what I like to do is do an exponential ramp so I will reset this I will add a DOT somewhere around the middle and I will drag this over like so and I can hold down shift click this one on the left and then while holding shift I will click on the right one so we select all of the handles in between and we can change their interpolation here and I usually like to use this B spline interpolation so now we can see that this gives us a nice curve in our Ram so this will be the smoothest interpolation basically that you can get and I can add another handle here and we can really create something that flows really nicely along our geometry so all of this that I'm doing here with the fit range and the ram this comes up all the time in visual effects so we should really get used to these two nodes now one thing to note about this ramp this kind of works like a fit range as in it takes the values that you output here so in this case we are outputting the value 0 to 1 it takes these values and it remaps it to New values based on what we are drawing over here but the difference is is that in the feed range we can give this any source mean and Source Max that we want but the ramp will actually clamp the source mean and Source Max values to 0 and 1. so for example if the values that I'm inputting over here from this fit range let's say that I want to input a value that can go over one if I increase this over here we should start to see the entire displacement over here getting bigger but instead if I increase this we can see that this stays the same so it's clamped at a value of 1 and it's just simply pushing the other values that are below one is going to raise them over to one and it's going to keep them there so also if I go to the destination mean and let's say that I want to go to values below zero if I were to decrease this below zero we should start to see these values on the other sides start to get their displacement affected but if I go below zero we can see that we are actually just pushing the other values that don't reach yet a value below zero we are pushing them towards the zero value so it gets clamped from zero to one so let's reset these values here to zero and one so this is a little bit annoying I'm not gonna lie but what we can do usually we can do other operations after our ramp so for example I can do a multiply constant over here if I want to bring the entirety of the values up so I can just increase this over here and this would work so there are certain workarounds you can do I'll go ahead and remove this so that's something to keep in mind this will clamp your input values to 0 and 1 and it actually took me a while until I realized this so usually after the feed range node if you want to use a ramp after it's beneficial to you to actually keep the destination Min and Max here for the fit to zero and one and then do the ramp and then if you want to do for further modifications you can do it after the ramp so this was the spline ramp version let's check out the other type so let's drop another parameter and let's check out the color version and when I drop this we can actually see that some of these options are grayed out so if I look at this we can go ahead and change the type and change the label but when I go to this one these are grayed out so why is this happening and this is another little gotcha over here if we have multiple ramps or multiple parameters with the same name over here so we can see that this ramp the name is set to ramp and this second ramp we also have the same name here which is ramp so because they have the same names Houdini won't be able to recognize our second ramp but if I grab the second ramp and I change the name here to ramp tool now we can see that the options are no longer grayed out and we can use this just fine so remember that we can have two parameters with the same name and the name is different than the label the label is just the text that shows outside so if I give this a label ramp spline and go up when I go up we can see that this ramp spline label shows up over here but when we hover over this text we can also see the parameter value which is ramp so if I go back inside this ramp is going to be the name and this is what Houdini is actually looking for so this is how it will identify the parameter and this is how it will know how to set certain values so Houdini cares about this name over here and doesn't care about this label name in fact I can have the same label name so if I grab this and set the label for this to be the exact same it everything will still work and if I go up we will have the same labels here but the name has to be different on the second one so this is going to be ramp two so let's go ahead and see how this works again we will be only able to edit this ramp at the top level and let's map this ramp to our noise value so I'll plug this over here and I will plug this result in our CD so we can visualize this so I'll plug this here and if I go up I can go ahead now and I can change these values so let's maybe use a red and for our second color we can use a blue now the reason this isn't giving us the exact result is because again this will clamp our values from 0 to 1. so since we have a lot of values that go below zero every one of those values will get a color of red so we would have to go ahead and also do a fit range here and fit our source mean and Source Max to this zero and one so I can go for our source mean in the negative values and I will use my scroll wheel to just scroll down and I think the values by default that are being output here by the noise is somewhere around the ranges of negative 0.3 and a source Max of 0.3 so let's set the source Max here to 0.3 and now we are kind of getting the expected result for our noise so this is an example of the color version of our ramp but uh probably ninety percent of the time we are working with a spline ramp so let's do round two of math operations and the next one we will talk about is going to be the complement so in between our ramp here and let's remember that this ramp will output for us values that ranges from zero to one if I do a complement after after this we are going to pretty much reverse this ramp so this is a little bit different than negate so if I were to use a negate here and plug this in my input we can see that this negate will just simply multiply our values with a value of negative one and as a result it will sort of invert the amplitude whereas this complement kind of reverses the entire Ram so when I plug this in it's going to do a 1 minus our values and we can actually see this here it says 1 minus X so this is exactly if I were to let's do a constant of one and let's do a subtract and if I plug my Ram values in inside here and I replace our complement this will give us the exact same result so this is essentially what's happening with the complement I don't usually find myself using this that much because my preferred way would be if I remove this and I do a fit range I can do another fit range here that goes from zero to one and I will just remap this to one and zero so we can see that this does exactly the same thing as our complement but now we also have additional control by playing around with our destination Min and Max and so on so that's the compliment let's move forward and next we can talk about maximum and minimum so if I do a maximum node here this would be sort of like a clamp so let's do a constant and let's plug this in our input and let's actually better showcase this let's get rid of all of this and just go back to our simple noise and just clean this up a little bit I'll remove some connections so now we only we have the turbulent noise which gives us these noise values that also have negative and positive values plugged in our scale here for the displacement and I can set the displacement amount so let's do our maximum again so if I do a maximum here and I promote the second input to a constant we can see that this kind of does the same thing that a clamp will do for us so if I do a clamp here and if I plug the same constant in our clamp for the max let's preview this result we can see that we can increase this to around a value of roughly 0.25 and then if I increase it further it doesn't do any changes for us but when I plug the result of the max and I plug it into our scale we can actually see that we start from the same place and I can keep pushing this value up and if I go over the maximum values that are being output by the noise we can still push this even further so it will take whatever value we have here and it will push it to that value so this is sometimes useful when you want to accumulate an attribute from one frame to another and you want to make sure that each frame is at least as much as it was on the previous frame but you want to add to that so probably this will make more sense with an example and we will look into this later and the minimum will do the same if I drop down a minimum and let's add this here we can adjust the constant now so we can also push this further down below so below the values being output by this noise so this is maximum and minimum and we can get rid of these next let's look at the ceiling and to show what ceiling does we will need a float value so we will demonstrate this by using the time so to visualize this as an attribute I will go ahead and I will do a bind export on our time and I will just rename this value to test so now we can go in the geometry spreadsheet and if I scrub through my timeline we can see how this is is now mapped to the time so at frame 25 will have a value of 1 and then at frame 49 will have a value of 2 so this is in seconds and we have this increasing float value that we can play around with now so if I do a ceiling in between our bind export and the time and I go back to the first frame now as soon as I go to the next frame so on frame 2 this will now jump to a value of one so ceiling will look at our decimal values for our float and if it's over a whole number it will jump to the next whole numbers so if we have a value of 2.3 it will jump to a value of three and the same if it has a value of 2.9 it will still jump to three so this is different than around two integers so if I do a round to integer this will now give us the whole number that it's closest to so a value of 2.3 will return to us a value of 2. so if I go over the frames now we can see now that we are still on frame 7 and 8 and we are still getting a value of zero and this is because our time currently is below a value of 0.5 so if I go if I disable the round to integer over here by pressing q and if I go further when we reach this 0.5 value at frame 13 and I turn this on now it will jump to one so we can see previously that it was at 12 we had a value of 0 and a 13 it jumped to 1 because we because we moved over that 0.5 threshold and then floor will do the opposite of what ceiling does and it will always give us the lowest whole number so a value of 2.9 will return a value of 2. so we can see now that with the floor we are going to have a value of 0 up until frame 25 where we do the switch so these are pretty useful when we do animations and simulations because for example with this floor we can see that we can hold the same value for one entire second so we can have have something happening for the first second and then when we switch to the next second we can have something else happen entirely and again these operations will make more sense when we see some examples now it's important to note that around to integer doesn't actually return an integer value we can see here that this color is still teal and we are exporting a float value so if we actually want to export an integer we would have to do a float to integer so now we can see that we no longer have any decimal points so previously if we do a round to integer and I exported this even though we have a whole number it's still a float value as evident by having this decimal point here so it's going to be 1.0 not actually one so when I switch to this flow to integer operation we can see now that we don't have any decimal values to our test attribute and on this note even though we go to our bind export port and the typeset the type here is set to float we are actually exporting an integer value so it will automatically set the attribute to the correct type depending on what you're feeding into this buy and Export but I always just like to set this type here as well in the buy and Export just so we don't risk running into any trouble so the opposite of flow to integer is going to be integer to float so if I do an integer to float let's plug this here and this doesn't actually make sense because we already have a float value here so instead of working with the time let's work with the PT num and let's export this so this will return our current Point number and we can see that we are using an integer here so we can do an integer to float and this will now turn all of these into float values now this is important because if we for whatever reason have to do a division with an integer it will only work if we first turn it into a float so if I drop down a divide here and let's promote this to a constant and let's do some operations and if I increase this we can see that we can do the division just fine but if I were to disable this conversion from integer to float if I disable this now we can see that no matter what we are dividing with this will give us a value of zero so this is why usually this integer to float conversion is pretty important when it comes to divisions all right so let's get rid of these another operation that we can do on float values so let's plug our time back into this we can drop a fraction function over here and this will now get rid of the whole number that's in front of our value and only return the decimal values so we can see that we go from zero and we are increasing over to one and when on frame 25 we should reach a value of one it's actually going back to zero and on the next frame 26 we are starting to do the whole thing again so we are going to go from 0 to 1 over the next 24 frames so it's simply taking out the whole number that's in front of our value and this is useful for us because as we can see here we get a value from 0 to 1 that keeps repeating so if I do a multiply constant I can speed this process up so if I do a value of 3 here now I can make this cycle go even faster so this is how we can do repeating type pattern animations and a similar note to frac is going to be the modulo operation so if I drop down a modular node and this will work better with frame so I will plug the first input in the frame because you will usually use this in combination with whole numbers so frames are whole numbers this will do a division with the second input that we can plug over here or that we can type here and it's going to Output the remainder of that division so if I set this divisor value to a value of 5 and I go to the first first frame we are going to do 1 divided by 5 which is 0 with a remainder of one so this one is what's going to be output here so if I go forward another frame we are going to do now 2 divided by 5 it will give us a 0 and with a remainder of two and uh so on on to the next frame which is going to give us a remainder of three four and then on five when we do five divided by five it's going to be one with a remainder of zero so when we go to Five it's going to give us a value of zero and then on the next frame 6 divided by 5 is going to be one with a remainder of one so again we are sort of doing the same thing when we are where we are creating a cycle so we can see that every five frames we are repeating the cycles that goes from zero to four and then back to zero and so on so this is super useful for us again when we are trying to repeat different types of animations and we can also use this to have certain things happen only on certain frames so let's say that I only want something to happen every fifth frame we can do the result of this module I can drop down a compare and I can compare this result and we can compare the result with any value here so when the modulo will give me a remainder of 0 and this compare will check if that value is 0 I can output the result of the bull in this test and we can see now that if I go to the beginning we have a test value of 0 and then when we go over to frame 5 we will have a value of one and on the next frame we are again going to have a value of zero so this value now will only be one every five frames so we can go forward and see that at frame 10 we are going to have one and then 0 up until frame 15 where we have one again so we are turning this on and off every five frames so this is again something very useful and we will see examples of this and finally we also have this compare node so this is pretty self-explanatory it's going to to compare a value with something that we can type over here or we can plug any other value inside this input tool and the operations can be equal less than greater than less than or equal greater than or equal and not equal so these are all operations that you will get used to in no time and the result as we can see in our test can only be zero if it's false and one if it's true so again we can use this result to map different behaviors and attributes and we will see again more examples of this later other than the regular math operations we also have some trigonometric functions that we can look into now this is mostly going to be sine and cosine so you might be somewhat familiar with this depending on your background but essentially the sine function will give us cycling patterns so if we have an increasing value that expands over time or over a dimension we can use that to generate a sine wave and in this example we have a simple line that starts at C in origin and it points in the X Direction so it's going to point in this one zero zero Direction a length of 10 over here and we also have 100 points on this so we can displace these points and I can demonstrate the sine function so let's go ahead and do an attribute of op and we will step inside so it's easier to show this on a simple line because we are going to deal with only one dimension so if we split this position Vector so if we do a vector to float we only really have information on the X component in here so the second and third components are basically zero so I can take this x component so this is sort of our growing value because it goes from 0 to 10 and let's plug this into a sine wave and this will output for us a float value and if I place this in the CD we will have a value that starts at 0 at origin and then it's going to increase over to a value of 1 over here and then it will slowly decrease as we move further on our X direction to a value of negative one then it goes back to a value of 1 and then negative one as well so it's going to oscillate between 1 and negative one and we can also check our geometry spreadsheet so we can see that these go from roughly a value of one to roughly a value of negative one so let's go back so we would be able to better see this if we use this sign result as our y Vector so let's do a float to vector and we want to use the same amount for our X component and then then we can plug the result of the sine wave as our second component and we don't need any information on the Z so I will leave this component 3 to 0. and now let's maybe get rid of the CD and if I plug this result in our P we have a nice representation of our sine wave so probably this makes a little bit more sense on what it does so again if I move in a front view we are starting at zero then we are going to one and then we are going to negative one back to one and so on so for example if I were to increase the length of our curve this would keep repeating indefinitely so as long as this is a value that sort of grows consistently we can spread out the sine wave as much as we want let's go back inside and now in order to increase the frequency of the sine wave we can multiply the value that we fit into this sign so let's do a multiply constant here before the sign and if I increase the values this controls the frequency of the sign and if I want to offset this sine wave we can do an add constant and if I increase this we see that we shift the science position now we can do all of these three things here if we use a trigonometry node so let's go ahead and use the trigonometric functions node and I'll get rid of all of these and let's plug our X component inside the radius here and the result of this node in our second value so now we have the same sine wave and we can control the frequency here just like we did with the multiply and also the offset as well we can shift this around so this basically picks up a few other a few functions together now we can also combine the result of this sign with some other math operations so for example if after our sine we do a negate we are going to have the reverse effect on the Y so not particularly useful but let's go ahead and use a clamp so by default this will be the minimum set to zero so now all of the values that go below into the negative ranges will be flattened to zero so this is sometimes very useful and we can also do an absolute so if I drop an absolute over here now all of the values that go from 0 to negative 1 will turn into values that go from 0 to positive one so we end up with this nice bouncing pattern so the sine wave is used in procedural modeling to get nice objects that are based on curves so this is how you would model a spiral for example now this isn't particularly useful for us as VFX artists for procedural modeling but we can use this sine wave to create some animation so let's go up and I have a sphere on the right here so it's just a simple sphere and I have disabled the lighting over here let's do an attribute warp let's go inside and let's map the sine wave to the time so we will grab the time from here and we will do a trigonometric functions and if I place this inside the CD to preview the result when I play the animation now we can see that we have this oscillating value again that goes from 1 to negative 1 and back to one so we have this repeating animation and if I increase the frequency maybe we can see this better so if I play now we can see that this goes from black to white now we can see that this stays more on black and this is because we are going actually to negative one and again if we use an absolute to turn all of the negative values into positive ones we are going to have a much faster flashing so this is very cool for us because now we can map certain attributes to the result of what we are doing here with our sine wave and I will show you some examples later but for particle simulations this is how we would be able to create a pulsating effect on our particles so I will show you how you can map this value to drive the formations and stuff like that so the sine wave is very useful in procedural modeling but also very useful to us as VFX artists

**Frame:** tutorials\frames\intro-to-vops---houdini-beginner-tutorial\frame_000.jpg


---

## Structured Notes

### Core Technique
Comprehensive intro to Houdini VOPs (Attribute VOP context) covering all daily-use math nodes, attribute binding, noise displacement, fit range + ramp parameter, and trigonometric animation. Every operation runs per-point; understanding this mental model is central to the tutorial.

### Summary
Part 1 of a VOP series. Covers the full spectrum of foundational VOP nodes used in production VFX: vector/float types, all basic math ops (add/subtract/multiply/divide with constant shortcuts), parameter promotion, bind/bind export for attribute I/O, import point attribute for cross-geometry lookups, mix/blend, turbulent noise + displace-along-normal, fit range, ramp parameter (spline and color), negate/absolute/clamp/power/complement, min/max, ceiling/floor/round/frac/modulo, compare, and sine/cosine for animation.

### Key Steps

**1. Attribute VOP Fundamentals**
- Attribute VOP = generic container; set class (points/prims/verts) manually
- Primitive VOP / Vertex VOP are just Attribute VOP with class pre-set; Volume VOP is different
- Default inputs: P (position), N, v; default output: P
- Per-point operation: every node runs independently on each point — visualize by blasting all but one point when troubleshooting

**2. Data Types**
- float: any number (5.64)
- integer: whole number only (5, 6)
- vector2/3/4: multiple components; vector3 is the workhorse (position, normals, velocity)
- Float↔vector conversion: `vector to float` splits; `float to vector` assembles

**3. Math Operations**
- add, subtract, multiply, divide — each takes multiple inputs
- `add constant` / `multiply constant`: shortcut when second operand is a literal
- Middle-mouse on any input → "constant" to auto-create a wired constant
- Promote parameter: middle-mouse on input → "promote parameter" to expose on parent node

**4. Global Variables**
- `time`, `timeInc`, `frame` — accessible anywhere in the VOP network
- Animate with `time` (0→1 per second at 24fps); use multiply constant to scale speed

**5. Bind / Bind Export**
- `bind`: reads any existing attribute on the current geometry
- `bind export`: writes/creates an attribute; presets "export = always" + type
- Bind export sets attribute type from what you plug in — still good practice to set type field explicitly
- `import point attribute`: read from a different geometry (second input); requires matching ptnum

**6. Mix Node**
- Blends between two values by bias (0–1+, not clamped)
- Plug `time` into bias for temporal blends; `multiply constant` to slow the transition

**7. Noise Displacement Workflow**
- `turbulent noise` → plug position → output float noise value
- `displace along normal` → plug noise into scale, set displacement amount as multiplier
- Noise type Alligator: 0–1 range; Simplex: -1 to +1 (needed for bidirectional displacement)
- Preview noise as CD by plugging into color and disabling lighting

**8. Fit Range**
- Maps source min/max → destination min/max
- Use Z component of position (e.g., -5 to +5) as source to create spatial gradient
- Multiply noise × fit result = spatially-varying amplitude
- After fit keep dest min=0 / max=1 if feeding into a ramp (ramp clamps 0–1 input)

**9. Ramp Parameter**
- Drop `ramp parameter`; edit ONLY at the top level (Houdini auto-exposes it)
- Two types: `spline ramp float` (most used, remaps 0–1 input to 0–1 output) and `color ramp`
- Input is clamped to 0–1; do any scaling before the ramp, then scale again after if needed
- Multiple ramps must have unique `name` field (not just label)
- Ctrl+MMB on label to reset ramp to default

**10. More Math Nodes**
- `negate`: ×-1 (same as multiply constant -1)
- `absolute`: removes sign from negative values (useful to fold noise below zero upward)
- `clamp`: min/max hard limits; flatten values below 0 for mountain/heightfield look
- `power`: raises value to exponent; low exponent → amplify peaks, values near zero unaffected
- `complement`: computes 1–x (reverse a ramp); equivalent to subtract from constant 1
- `maximum(a,b)` / `minimum(a,b)`: per-element max/min; useful for accumulation patterns
- `ceiling`: jumps to next whole number above (2.3 → 3)
- `floor`: rounds down to whole number (2.9 → 2)
- `round to integer`: rounds to nearest — NOTE: still outputs float (1.0 not 1)
- `float to integer`: actual int conversion — required before integer divisions to avoid zero result
- `fraction`: removes integer part, returns decimal (1.7 → 0.7); creates 0–1 repeating loop from time
- `modulo`: remainder of division; e.g., frame % 5 cycles 0–4; combine with compare to trigger every N frames

**11. Compare Node**
- Operations: equal, less-than, greater-than, ≤, ≥, not-equal
- Output: 0 (false) or 1 (true) — use as activation flag

**12. Trigonometric (Sine/Cosine)**
- `sine`: takes growing value, outputs -1 to +1 oscillation
- Use `trigonometric functions` node: controls frequency (multiply input) and offset (add)
- Add `absolute` after sine → 0–1 bounce pattern
- Add `clamp (min=0)` → only positive half of wave
- Map sine result to Y position on a line → visual sine wave shape
- Use `time` as input → looping animation driver (great for pulsating particle effects)

### Houdini Nodes / VEX / Settings
- **Attribute VOP** — main context; set "run over" class
- **vector to float / float to vector** — split/assemble vector components
- **add, subtract, multiply, divide** — standard math; multi-input
- **add constant, multiply constant** — literal-value shortcuts
- **bind / bind export** — read/write any point attribute
- **import point attribute** — cross-geometry attribute lookup (needs ptnum match)
- **mix** — bias blend between two values
- **turbulent noise** — noise from position; Alligator (0–1), Simplex (−1 to +1)
- **displace along normal** — pushes points along N by scale
- **fit range** — remaps value range
- **ramp parameter** — visual 0–1 mapping curve; edit at top level only
- **negate, absolute, clamp, power, complement** — value shaping
- **maximum, minimum** — per-element max/min
- **ceiling, floor, round to integer, float to integer** — numeric rounding
- **fraction** — decimal part only
- **modulo** — division remainder; cycles 0 to N-1
- **compare** — boolean output (0 or 1)
- **trigonometric functions** — sine/cosine with frequency + offset controls

### Difficulty
Beginner

### Houdini Version
any modern (H18+)

### Tags
[vops, attribute-vop, math, noise, ramp, fit-range, bind, beginner]

---

## Related Tutorials
- intro-to-houdini-volumes---beginner-course.md (Volume VOP context — extends these concepts)
- intro-to-houdini-particles---full-beginner-course.md (applies VOP math in particle sims)
- vops-02, vops-03, vops-04 series (continuation of this series)
