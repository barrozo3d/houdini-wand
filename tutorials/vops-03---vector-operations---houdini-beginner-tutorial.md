---
title: VOPS 03 - Vector Operations - Houdini Beginner Tutorial
source: YouTube
url: https://www.youtube.com/watch?v=oT6qzs-Vffk
author: Voxyde VFX
ingested: 2026-06-23
houdini_version: "H19+"
tags: [vops, vectors, cross-product, dot-product, normalize, particles, simulation, beginner-intermediate]
extraction_status: complete
frames_dir: tutorials/frames/vops-03---vector-operations---houdini-beginner-tutorial/
frame_count: 4
---

# VOPS 03 - Vector Operations - Houdini Beginner Tutorial

**Source:** [YouTube](https://www.youtube.com/watch?v=oT6qzs-Vffk)
**Author:** Voxyde VFX
**Duration:** 31m12s | 4 section(s)

---

## Raw Data (for Claude Code extraction)


### Subtract, Normalize, Length [0:00]
**Transcript:** Welcome to Intro to Vops Part 3, Vector Operations.  Knowing how to manipulate vectors is going to be your ticket to mastering any kind of simulation  in Houdini.  Now we can talk about Vector Operations, so as visual effect artists we have to know how  to manipulate vectors in order to control the movement of any type of VFX, so whether  we are working with particles or with smoke simulations or we are doing rigids, it's  going to be extremely important to know how we can tell the geometry to move in the way  we want.  So let's go ahead and get started and I will just drop down a Geocontainer and let's  do our test on this Robert toy test geometry, so I will drop this here and I'll just make  this bigger, maybe I'll set the uniform scale to a value of 3 and let's go ahead and do  an attribute VOP and we will step inside and in order to create just a simple vector  we can just drop down a constant and I will set this to a tri-float which is going  to give us a vector and I will just plug the result in our normal, so now I can turn  on the normal display here so we can visualize the vector in our viewport, so I can give  this any direction I want, I can set this to 0, 1 and 0 and now we see we have this vector  that points up and I'll actually press D and go to the background here and I'll set  this to dark so we can visualize this better and also maybe it's better to work with points  instead so I will go up and I will drop a scatter after the Robert test toy and let's maybe  just increase the points, so now we can focus only on the points, so maybe this will help  us visualize better also when it comes to the normal display if I press D and go to the  guides, we can see that the normals are scaled down so this is only in the viewport, I will  set this to a value of 1, so we have the actual scale of the vectors that we are creating,  so let's go back inside and we can give this any direction, we can do 1 on the x and  we will have this vector, so this is already pretty useful if I want this geometry to emit  particles that are going in this direction, I could do it like this, now one of the most  basic ways to create a vector is also by doing a subtraction, so let's go ahead and remove  this and we can see that these are the regular normals of our geometry, so for example if I want  a vector that points in a certain position we can do a subtraction, so if I want all of our  points to have a vector that points to the center of the scene, I can grab the positions  and I will do a subtract and I will promote a second input to a constant and I will set  this to be a tri-float, so when we are doing this subtract operation with vectors we have  to make sure that we are subtracting a vector from another vector and I will set this constant  to a tri-float and I will plug this result in our normal and now we can see that we have  a vector that points away from the center and if I want the reverse of this, so if I want  the vector to point inside I can just reverse the order of this subtraction, so I will  grab this and press shift R to reverse the order and now we have this vector that points  right in the center of our scene, so it points right to this position, so from the position  of each point we are drawing a vector that points to this position so at 0, 0, 0, 0 and  now I can grab the value of this constant and if I were to move this up, so I will increase  the second component here, we can see that the vectors get updated, so now it points  to this new position that I'm setting over here and I can really have full control over  where I want this vector to point to.  Now in order to make this a little bit more interactive I will go up and I will create  a point so I will drop down and add, let's go to this and let's click on this plus to  add a point right in the center of our scene, maybe I will press D and I just want to make  the point size a little bit bigger and now I can press enter and I can control the position  of this point through this transform handle, I will place this as my second input in the  attribute of up and now if I go inside in order to grab the position of our point from  outside we are going to use an import point attribute, so the file will point to open  put to which is our point geometry and I will promote this pt num value to a constant  and I will leave this at 0, so if I go up this geometry only has 1 point, so this will  be point number 0 because all of the point numbers increment from 0, so if I go back inside  this import point attribute will by default look for attribute p which is the position  which is what we want, so now I will replace our constant with our point position so I  will plug the result over here instead of the constant and now I can remove this and  if I go up I can grab my add, press enter to grab my handle and we can see we have this  nice interactive vector, so we will talk more about this import point attribute in a different  chapter but for now I just wanted to use this so we have this nice interaction in our  viewport, so let's go back inside, so again we can reverse the order of the subtraction,  so if I press shift, we now have a vector that points away from our point position, so  this is already super useful for us, if for example I wanted to have an explosion, I can  direct the movement of the geometry through this vector and we also have this nice display  in the viewport, so if I go up and I play around with this add, I can control this position  and I can get a very accurate idea of how the points will move when we are doing a simulation,  so if I also go back inside, I would be able to also do a noise, so let's grab our position,  do a turbulent noise, signature 3D noise, let's set this to a simplex and I can add this  on top of our vector to introduce some randomness, so let's add this here and if I increase the  amplitude and I increase the frequency, we can randomize this vector a little bit, so if I turn this  on and off, we can see the result and I can also increase the roughness and maybe I want to add  even more randomness, so I will increase the amplitude, so already just by having this vector,  if for example I would plug this into the V, let's just quickly see how this would work in a  pop simulation, so I would drop down a pop network and because we are working with points,  I have to go inside and set the emission type here to all points, I will disable the guide,  and we updated the velocity, so this normal display isn't really useful right now,  but if I play the simulation, we have this result and really quick, I will just set this to a pixel  display, so we have this direction and of course I would still be able to animate this turbulent noise,  so if I grab the time here and do a float to vector, let's go ahead and I'll leave this as the  first component and I just want to update the offset, so if I go back here and let's also preview  our normals, again, if I play the animation now we can see that our vector is changing a bit,  so we are changing the offset of our noise, so let's turn off the normal display and go back to  our pop net and this will be the result, so already this is pretty handy and of course I can still  grab this add node and maybe this even updates in real time, I can control this point and we can  direct where we want the particles to move, so this is pretty cool, now also you can see one  thing here as I move this point further away, our particles are actually moving faster and if I  move this closer to our geometry and let's reset the simulation, they are moving a little bit slower,  so let's go ahead and delete this, let's go back in our pop net and turn on the point display,  a point a normal display, I will remove the turbulent noise just to make things a little bit clearer  and this add and let's get rid of this velocity connection, so again if I move up and grab this  point and move it further away from the geometry, we are actually scaling this vector as well and  this is why the particles are moving faster, so let's say that I like this direction I'm getting  from this position but I want the particles to move slower, we could scale the vectors if we do  a simple multiply, so after the subtract I can drop a multiply, I will promote a second input to  a constant and by default this will be zero, so if I increase the value we can see that we are  increasing the length of our vectors, so this would essentially control the speed in a simulation,  in a particle simulation, now when we want to do multiplications with vectors it's very important  that we keep track of the order this multiplication is done in, so for example if I were to grab this  multiply and press shift R to reverse the order we see that our vector changed entirely and this  is because when we want to scale up or down a vector the vector has to be the first input in this  multiplication operation, so I will press shift R and reverse the order, so when we see that our  first input is a green color, so a vector we also output a vector value over here with this product  but if I were to have this float value be the first input, so if I press shift R, now the product is  also a float value, so this is no longer a vector, it's essentially grabbing the float result and  it's using the same value on all of the components of the normal and this is also why this wire  here turn into a dotted wire, because Houdini will make this work like I said by using the same float  value in all of the three components of the vector, so we can plug a float result in a vector input  but it's not really ideal, so this is why we have this dotted wire and when I reverse the order,  so we output a vector value, now it turns into the regular wire, so this is the proper connection  so to say, so we can scale the vectors and I will go ahead and remove our multiply and let's go  back up and if I grab this point and move it closer to our geometry this one really be noticeable  but when I move this closer to our geometry, we can see that all of our vectors actually have  different lengths, so the points that have a position that's closer to the geometry will have a  shorter vector than some of these other points which are further away from this position, we can  see that these vectors on the side here are actually a lot bigger than some of these other vectors,  so again in a particle simulation this will mean that all of the points that are further outside  will have a higher speed than these other points here that are closer to our point position,  so sometimes this is what we want but most of the time we will want a consistent speed, so we want  to maintain this direction that we create here but we want all of the points to move at the same speed  and if I go back inside we can now normalize our vector so after the subtract I will drop down a  normalized node and when I drop this we can see that all of the vectors now have the same length,  so what this normalized will do is it will set the length for all of the vectors to a unit length  of 1, so if I check any vectors if I were to measure the length of any vectors it would be a value of 1,  so now in a pop simulations all of our particles will have the same speed, so this is extremely handy  and it's something that we do all of the time and we can also compute the length of any vector by  using a length node, so for example after this normalized if I were to do a length, so I will drop  down this length node, this will return to us a float value, so it's going to give us the scale  of our vectors and for example if I were to do a bind export and I will name this attribute to  length, if I go in the geometry spreadsheet because we did this normalized thing here all of the  vectors will have a length of 1 as we can see here and for example now if I were to disable this  normalized node so I can grab this and press Q, now we have a different length for all of our  vectors as we can see here, so let's go back to the scene view and we can do a lot of things with  this length node, this isn't really here just to troubleshoot, so for example let's maybe drag  this up and I'll get rid of this normalized and the bind so we only have the length, we could for  example use the length and I will turn off the normal display and let's set these the display  back to points, I could plug this length into our CD and let's do a feet range in between and if  I increase the source max and maybe I reverse the order here for destination min and max and I play  around with some of these values, we can now create a attribute that has a value based on how  further away it is from the point that we created outside, so I'm using this length to determine  basically which points are closer to our other point and a lot of times in a particle simulation  we are using this length on the velocity to compute the speed of the particles, for example use  that speed information to color the particles in certain ways, so for example by using the length  I would be able to make particles that are going fast red and the particles that are slower blue,  so we can use this length to control all types of effects and then if I turn back on this normal  display we are using the normalized node to get our initial direction but to have consistent length  on our vectors and especially when we are doing other vector operations like a dot product or  a cross product we will need to work with normalized vectors so this is also why this is very

**Frame:** tutorials\frames\vops-03---vector-operations---houdini-beginner-tutorial\frame_000.jpg

### Cross Product [13:18]
**Transcript:** important, now we can talk about the cross product and to illustrate what the cross product is,  I created a simple representation in subs, so ignore all of this over here, this is basically  just creating this thing we have here, so let's say that we have a point over here from where these  two vectors are originating from and if we do a cross product between these two vectors  we are going to get a vector that's perpendicular to these vectors, so if I turn on the normal display  we have this point here with this blue normal vector and if I were to adjust our other vectors,  so if I were to modify this, this vector will always be perpendicular to this sort of imaginary plane  that's created between the other two vectors, so I can make these vectors closer to each other  and it will still create this imaginary plane and we will have this vector that's perpendicular  to the plane, so this is the cross product only of course this vector will also originate from  the point that actually contains these two vectors, so a more accurate representation would be if I  move this point to the same origins, so over here where we have the actual origin of the point,  I just placed this point in the centers so we can better see how this works, so this is the  cross product whenever you have two vectors you can imagine an invisible plane that the vectors  create and the cross product will be perpendicular to that plane with the origin to our point,  so we use this all the time to control our vectors and for example if I go to the geometry test toy,  so we already have normals here, so when we do our scatter, we are going to transfer down the  normals and I'll get rid of these and let's do another attribute VOP and we will step inside,  so I will drop a cross product and this will take two vectors, so the two vectors that we talked  about and a lot of times when we are working with geometry we are going to use the normal of the  geometry, so if I plug the normal in the first input we can either use another vector or we can  set that vector here as our second input, so if I leave this vector as it is, so 0, 1 and 0,  which is a vector that essentially just points straight up and I plug the cross product result into  our normals, so we will replace the normals just so we can see this in our viewport when I plug this,  we have a vector that spins around our geometry, so maybe I can let's go ahead and make more points,  so I will scatter more points and I'll go back inside and I will press D and also scale down our  normals, hopefully we can see this, maybe I will switch the point display to pixels, so we can see  this really cool vector that spins around our geometry, so when we compare the regular vector,  so if I disable this for a second, when we compare this vector that points outwards from the surface  and we compare it with a straight vector that points up, we get this vector that spins around  the geometry and we can see that this is spinning on the y axis and if I were to change this vector  to here, so the second input, if I set this to 0 and I set this to be 1, 0 and 0, so this will give me  the x direction, now our vectors will spin around this x direction and the same will go for our z  direction, so we can really do a lot of cool stuff with this, so when we do this in a pop simulation,  I'll disable the display normal for now and I'll go up and I'll place the scatter inside a pop  network and I go inside, let's set the emission type for our source here, emission type will be  all points, let's go ahead and reset, so we are importing the points, we will disable the guides  and I'll go ahead and do a geometry vop here, so essentially just like a netribute vop,  let's go ahead and we will grab the normal and we will do the cross product with the up vector  and if I plug this just simply as it is in the velocity, let's see what we get,  so we have this very cool spinning animation and let's maybe do a few adjustments here,  I will make the particles die off quicker, so maybe I'll set the life expectancy to 0.6  and also introduce a bit of variance, so life variance 0.3, let's go up and let's actually just  use the regular geometry and go inside our pop net and change this back to what it was originally,  scatter on two surfaces and if I reset now we get this really cool animation and I can maybe  increase the particles, so let's maybe add another 0 here to our constant birth rate and if I play,  this is the result, so this is pretty cool and if for example let's maybe just make this last  a little bit longer, so 1.5, I could have this vector as it is and I can just add a curl noise  on top of this, so when I do a vector to vector 4 because we also want to evolve our noise,  let's plug this inside a curl noise, set this to a simplex and I just want to add this on the result  of our dot product, probably the amplitude will be too high, so I will set this to 0.1 and let's just  add this as it is with the other default settings and we will press play, so now we also have a  little bit of randomness, maybe I will increase the amplitude and decrease the frequency,  let's increase this even more and let's reset, so we also have a little bit of fluid motion to  our particles and all of these are spinning around our geometry surface, so this is just  scratching the surface of what the cross product can do for us and here we are using it with  in combination with our normal which is very common, so you will find this use case a lot,  so in combination with the normal, so we can use a cross product and then we can also do just a  simple mix, so maybe if I want to skew that movement of our particles in a certain direction,  I could use this mix node, so for example if I want them to do this spinning movement but also  go a little bit up, I can use this mix node and I can combine this with a vector that points up,  so now if I play the simulation, they are spinning and also moving up at the same time,  the curl noise is added on top to create more fluid motion, so we can hopefully see how we can  combine some of these vector operations to really get the movement that we want, and just one more  example with the cross product, I'll go up and I want to create a new test geometry, so I will  just drop down a torus and maybe I will increase this radius a little bit and again we will use this  inside a pop network and let's step inside and disable the guide for our pop source, let's do  another geometry VOP and let's say that I want to create something like a tornado effect, what I can  do is I can reverse engineer what a cross product result will be, so I know that if I want a vector  that spins around, so let's turn on the point display back and we will grab a screen of this and  go in Photoshop, so for example if I know I need a vector that points around the center of our scene  like so, I can reverse engineer this vector, so if I were to imagine that perpendicular plane  that I was talking about, so something like this, so our vector is perpendicular to this plane,  I know that this will be the result of a vector that points in this direction, and the second  vector will point in this direction like so, so we need a vector that points to the center of  our scene and a vector that points completely straight up, so knowing this we can go back inside  and let's go inside this geometry VOP and in order to create the first vector that points to the  center of our scene, we will just subtract the position of the points where they currently are  with a vector that 0, 0, 0, so just like we did earlier when we moved around the point and all  of the vectors got updated to that position, so let's go ahead and we will do a subtract and we  want to promote this to a constant and we need to change the type here to 3F, so 0, 0, 0, which is  the center of our scene and I can even check what this will do for us if I plug this in the  velocity and I press play, this is currently pointing outward, so already this can be the base of  some kind of explosion effect, so we can reverse this subtract and now our points will move to the  center of the scene and then we will need let's drop a constant and we will need just a vector that  points straight up, so I will just set this to 0, 1 and 0 and I can also plug this in the velocity  and preview the result, so now all the particles are going up and we can do a cross product between  this, so we will plug our first vector, let's do a cross product, this will be our first input  and this will be our second input and usually when we are dealing with cross products we want the  vectors that we plug in to be normalized, so currently this second vector that we creating 0,  1 and 0 will have a length of 1, so we don't need to normalize this but the result of this subtract,  we have to normalize, so we are working with that 1 unit length scale vector, so let's do a  normalized here and if I plug the result in the V, we can see now that we got that vector that we  set out to create so this red vector over here that goes around our scene, so now to add some  upwards direction to this, I can simply do an add after our cross product, let's drop another  constant here, let's do a tri-float and just create another vector that points up or we could  have used the same one here, but let's keep this separate and let's add this to our cross product  and let's check what we have and this could be the basis for a tornado effect and in fact I did  use something like this to create a tornado, so we can imagine that if we add some noises on top of  this it can actually look like a tornado, so if I go up there is a node that already does this for  for us, so it's going to be this pop axis force and if I plug this here in our chain we have a lot  of options here for the radius, the height, the direction and stuff like that, but to be honest I  never use this not even once because I prefer to just recreate all of the controls I need inside  this geometry vop, so for example if I wanted to scale the speed of our particles I could use a  relative to bounding box node to scale our vector so they would be faster at the bottom and slower  at the top or we can compute a distance from the center so particles in the center would be faster  and we would have a lot more control like that, so if I press tab and go to the pop step we can see  that we have a lot of nodes that we can use here and probably all of these can be recreated in a  geometry vop, so this is why I keep insisting that we use geometry vops or rangles if you like vex  because if we are inside this geometry vop there really is no limit to the control that we have  because we are telling all of our particles precisely the movement that we want and we can change  stuff at any step, so hopefully this gave you an idea of the cross product out of all the vector  operations this is probably the most important one and the other one will be the dot product which  we will talk about next. To understand the dot product I created another simple illustration in

**Frame:** tutorials\frames\vops-03---vector-operations---houdini-beginner-tutorial\frame_001.jpg

### Dot Product [24:00]
**Transcript:** the viewport here, so again we are going to have two vectors that originate from the same point  and when we do a dot product operation between these two vectors this is going to be the result  that the dot product will give us, so it will look at the angle between the two vectors and it  will generate a value that goes from one to negative one, so if I decrease the angle between these  two vectors like so we can see that when we start to line up both of these vectors we get a value  closer to one, so when they are completely pointing in the same direction we are going to have a  value of one and as I start to increase this angle difference between the vectors we are going to  go over to a value of negative one, so negative one will mean that the vectors are essentially opposite  to each other and when they are perpendicular we are going to get a value of zero, so this is the  dot product let's move over to our rubber toy and again we will have normals on all of the points  and let's just work with points so we will work from this scatter node, so I will drop an attribute  of here and let's go inside and we will compare the normal with a vector that points directly up  and let's see what happens, so from the normal let's do a dot product and for the second vector  we can see that this is already zero one and zero so the vector that points up and if I plug this  into the CD we can visualize our values so we can see now that all of the points that have a normal  value or rather a normal vector that has the same alignment as our up vector so our second value  in this dot product all of those points will be closer to white and we also get this nice gradient so  if I do a feet range between the dot product and the CD I can further control this so because the  dot product outputs values that go from negative one to one we would have to set the source mean here  to negative one as well to get the proper values so now we can see that we sort of spread that  gradient and let's also go up and I want to un-template the text for the dot product from that  other example and let's go back or if I just want a sharper contrast I can squeeze these values  together so I can decrease the source mean like so and I can even decrease the source max so now we  get a sharper contrast and we can already see how useful this can be when we are creating effects  such as no falling on geometry or I sometimes use this to create impact hits for rain but this  dot product can have a ton of applications so we are comparing here the normal vector but we  could example use this in a particle simulation and generate a dot product based on the velocity  so we could say something like when the particles are moving in a certain direction we can compare it  with another direction and we can map some different behavior by using the dot product this way  so instead of setting the values for the dot product here in in this node so we can see that we can  modify the values here to get different results let's go ahead and create that same setup we did  earlier by using the position of a single point outside of this verb network so let's go up let's do  an add and create a point and I will plug this in the second input here and now to import this  point position I will drop an import point attribute file will be open put 2 let's promote this  to a constant which will be 0 and the attribute is going to be p and if I plug this in the second  value for our dot product if I go up and grab this point press enter in my viewport I can now go  and now we can use this point position to get an interactive representation and now I can go to  this fit and I can play around with the settings so I can spread these values apart and I can also add  a ramp after the fit and let's set this to a spline ramp and go up reset the ramp and now I can  squeeze the values even further now maybe I can increase the total count for the scatter so we can  see this better and we can still do the same trick with the turbulent noise so if I add a noise  to the position here where we do the subtraction let's go ahead and drop a turbulent noise  let's set this to a 3d noise and simplex generated from our position and let's add this to the values  that we subtract so when I add this and I increase the amplitude we can also introduce a little bit  of noise and this will give the border a nice textured and broken up pattern so I maybe want to  increase this even more and maybe we can see how this works as I play around with the frequency  all right probably the amplitude here is too high but maybe this will give you some ideas so this  is the dot product it's a little bit simpler than the cross product in my opinion and later I will  show you more examples of how we can apply the dot product in projects one final note that I want to

**Frame:** tutorials\frames\vops-03---vector-operations---houdini-beginner-tutorial\frame_002.jpg

### Reflect [29:00]
**Transcript:** cover is going to be the reflect and this is a vector operation that I rarely use but sometimes  it can be useful so if we focus on our green vector here this will be the first input in our  reflect node and this gray arrow here this is the second input and the result will be our red  vector over here so as I change the rotation of the green vector we can see that this red arrow  gets reflected and this gray arrow you can think of this as sort of like the plane that creates  this reflection so this green vector will sort of bounce off of this gray vector and it will create  this bouncing red vector so over here I have a grid as we can see that's also mapped in the same  direction as our gray vector so we can visualize this a little bit better so if I go back to my green  arrow and change the rotation we can see how this creates sort of like this vector that bounces off  of the ground so we can imagine that if we have a particle simulation and something comes from  over to this side if we want a separate effect to happen we can match the angle sort of on the  other side with this reflect node so now also if we change the rotation of our gray vector we can  see how this affects our red vectors so this vector is just essentially bouncing off of this gray  vector like so so it's nothing really that complicated there are a few cases where you would want this  so this is why I also wanted to talk about this and include this operation but usually even if I  have a particle simulation and I want something to happen in like a bouncing manner I would still  create this red vector here by doing a combination with a cross product maybe and combining it with our  normal so probably in the case of this red vector here we could grab this gray arrow and then we  could grab a vector this perpendicular to this grid and if we do a mix and we would set the value  to 0.5 so it's half in between we would be able to get something like this red vector but it's  worth knowing that we have this option

**Frame:** tutorials\frames\vops-03---vector-operations---houdini-beginner-tutorial\frame_003.jpg


---

## Structured Notes

### Core Technique
VOPs tutorial on the four core vector operations: Subtract (direction vector), Normalize (unit length for consistent speed), Length (scalar from vector), Cross Product (perpendicular vector for spinning/tornado effects), Dot Product (alignment angle −1 to 1 for snow/gradients), Reflect (bounce). Practical focus on combining these in particle simulations and geometry operations.

### Summary
31m12s VOPs Part 3 by Voxyde VFX. Four sections:
1. **Subtract, Normalize, Length** — Create direction vectors with subtract; normalize for uniform speed; length for scalar from vector; interactive point via Add node + Import Point Attribute; turbulent noise for randomization; scale with multiply (vector must be first input).
2. **Cross Product** — Perpendicular vector to plane formed by two inputs; Normal × Up vector = spinning-around-surface motion; tornado: Subtract(P, center) + Up → cross product + add upward offset; normalize subtract input; combine with curl noise + Mix.
3. **Dot Product** — Compares alignment of two vectors; result −1 (opposite) to 0 (perpendicular) to 1 (same direction); Normal dot Up → snow/rain falloff gradient; Fit Range −1→1 → 0→1; ramp for stylized control; turbulent noise on position for organic edge; import point attribute for interactive target.
4. **Reflect** — Bounces a vector off a plane normal; rarely used; usually cross product achieves same result more flexibly.

### Key Steps

**1. Subtract, Normalize, Length**
- Create direction vector: Subtract → plug P into first input, promote second to tri-float constant (target position)
- Interactive target: Add node outside VOP (single point) → plug as second input → Import Point Attribute inside VOP (file=second input, ptnum=0, attribute=P) → replace constant with result
- Reverse subtract order with Shift+R to flip direction
- **Normalize**: unit length 1 for all vectors → consistent particle speed regardless of distance
- **Length**: float scalar from any vector; use for color (Fit Range), particle speed coloring
- **Multiply**: scale vector magnitude (speed control); **vector MUST be first input** — if float is first, output becomes float (dotted wire warning)
- Add turbulent noise (3D simplex) to subtract result before normalizing for randomized direction

**2. Cross Product**
- `cross(A, B)` → vector perpendicular to plane formed by A and B
- Pattern: `Normal × {0,1,0}` (up vector) → vector spins around geometry surface
- Swap second input to `{1,0,0}` or `{0,0,1}` → spins around different axis
- In Pop Network: Cross Product inside Geometry VOP → plug into V → spinning particle effect
- **Tornado setup**: Subtract(P, {0,0,0}) → Normalize → Cross Product with {0,1,0} → plug into V; add constant up-vector after cross product (Add) → upward spiral; scale with Relative Bounding Box for speed variation
- Combine cross product with Curl Noise (4D) + Mix for organic fluid spiral
- `Pop Axis Force` SOP exists but all its controls can be recreated in Geometry VOP for full flexibility

**3. Dot Product**
- `dot(A, B)` → scalar from −1 to 1; measures angle alignment
- **Snow/rain effect**: dot(Normal, {0,1,0}) → faces pointing up = 1 (white), sides = 0, down = −1 (black)
- Fit Range: source min=−1, max=1 → dest 0 to 1 for usable gradient
- Import Point Attribute for interactive target point
- Turbulent noise (3D simplex) added to P before dot comparison → organic/textured border
- Squeeze fit range min/max for sharper contrast
- Use in particle sims: dot(V, target direction) → how fast are particles going in a particular direction

**4. Reflect**
- `reflect(incident, normal)` — bounces incident vector off a normal plane
- First input: direction vector (incident); second input: plane normal
- Result: reflected direction (like light off a mirror)
- Rarely needed in practice; cross product + mix usually gives more control

### Houdini Nodes / VEX / Settings
- **Subtract** (VOP) — direction vector from B to A; Shift+R to reverse order
- **Normalize** (VOP) — unit length 1; required before cross/dot product inputs
- **Length** (VOP) — float scalar from vector; use for speed coloring or falloff
- **Multiply** (VOP) — scale vector; **vector MUST be first input** (green input = vector output)
- **Cross Product** (VOP) — perpendicular vector; normalize A first; B can be {0,1,0} for up
- **Dot Product** (VOP) — alignment scalar −1→1; Fit Range source min=−1 after
- **Reflect** (VOP) — bounce vector off plane; rarely used
- **Import Point Attribute** — grab position/attribute from other geometry; ptnum=0 for single point
- **Add SOP** (interactive point) + Import Point Attribute → interactive viewport handle
- Pop Axis Force SOP — exists but Geometry VOP gives more control; prefer VOP approach

### Difficulty
Beginner–Intermediate

### Houdini Version
H19+

### Tags
[vops, vectors, cross-product, dot-product, normalize, particles, simulation, beginner-intermediate]

---

## Related Tutorials
- vops-02---random-noise---houdini-beginner-tutorial.md (Part 2: random and noise)
- vops-04---geometry-interactions---houdini-beginner-tutorial.md (Part 4: geometry interactions)
- mops-motion-operators-for-houdini-part-3.md (advanced vector math: dot/cross/matrices/quaternions in VEX)
