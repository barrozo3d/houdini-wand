---
title: MOPs: Motion Operators for Houdini Part 3
source: YouTube
url: https://www.youtube.com/watch?v=q_aD6sza6gA
author: Houdini.School
ingested: 2026-06-23
houdini_version: "H18.5+"
tags: [mops, math, vectors, matrices, quaternions, vex, linear-algebra, trigonometry, aim-constraint, forward-kinematics, transform, dot-product, cross-product, slurp, quaternion, intermediate-advanced]
extraction_status: complete
frames_dir: tutorials/frames/mops-motion-operators-for-houdini-part-3/
frame_count: 56
---

# MOPs: Motion Operators for Houdini Part 3

**Source:** [YouTube](https://www.youtube.com/watch?v=q_aD6sza6gA)
**Author:** Houdini.School
**Duration:** 163m38s | 56 section(s)

---

## Raw Data (for Claude Code extraction)


### Math Fundamentals of CGI [0:00]
**Transcript:** All right, let's go do some nerd math.  This course is going to be sort of a practical exploration of the math concepts that go into what underlies most of mobs.  And the vast majority of it is linear algebra, which is things like vectors, matrices.  It's essentially how computers handle all of the transformations that you apply to points.  I mean, you know, translate, rotate scale.  All the things that mobs is is doing quite a lot of.  And all the things that computer graphics do as a whole.  So a lot of this stuff that I'm doing with mobs, you know, it's, it's nothing new.  This is stuff that computers have been doing for 3D graphics for a very, very, very long time.  But a lot of artists coming into 3D aren't taught these things really.  They're just sort of taught like, you know, parent these things together, but they don't tell you like,  what is parenting? What does it actually mean?  And a lot of this math also applies to things like shaders.  And especially when we start talking about vectors and some of the things that you can do with them, like it all ties together really nicely.  So having a, you know, a practical understanding conceptually of what's going on under the hood is going to help you in a lot of different ways during your time as a 3D artist.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_000.jpg

### What [1:20]
**Transcript:** I've opened up in the example files, which I hope you all downloaded that I posted yesterday.  I'm going to open up a one vectors.  And this course is going to be a little bit more paint by numbers in the sense that I'm not going to drop down every single node.  Because none of the networks are really that complicated.  It's going to be more about sort of visualizing things.  And then there's going to be a few instances where we're going to step in and.  And do a little bit of Vex just to like solidify some of these math concepts.  So let's just jump right in.  So the this all of these concepts are linked.  But they're kind of they build on top of each other is layers.  And there's with math unfortunately, there's no way around it like you have to take.  Concepts and layer them and layer them and layer them and layer them before we can get to the good stuff.  So I'm going to start with something that you guys probably already know and is a little bit boring, but we're going to talk about vectors first.  And so if you open up vector vector basics here and go to visualizer one.  A vector is a direction and a magnitude.  So all it's doing is saying.  You know, it's an arrow of a certain scale that's pointing in some direction.  And the way that you typically think about factors is as a direction pointing out of the origin.  Although you'll find out very soon that it doesn't necessarily mean that conceptually in the same way.  When you're talking about things like position vectors.  If you grab this simple vector, that's noted and just drag the scale around or drag the vector direction around.  You'll see that all it's doing is just it's an arrow and it points to a certain point in space.  So I could say, you know, negative 1.2 here and you can see that it's pointing to negative 1.2 and it's one and why it's a point in space.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_001.jpg

### Why [3:08]
**Transcript:** There are different reasons why you might want to look at this as being a point in space rather than a direction.  It kind of depends on.  You know, again, there's multiple ways of looking at each of these things and sometimes it's easier to look at it in one way versus another.  But.  The important basic part here is that we're looking at a direction and a magnitude.  That's a vector.  We're going to use these everywhere.  So let's go to the next visualizer here.  Normalizing a vector is something that you hear a lot when you talk about normalized normals or a normalized velocity vector or whatever.  And all normalizing means is that you're dividing something by its own length.  And the reason why you do this and the reason why normalized vectors are so important is when you're dealing with things that involve direction.  So you're going to have a lot of different ways of looking at this.  Rather than position in space, a lot of the computations that you do on factors when you're trying to figure out like what way is something facing or how do I rotate this angle to meet this angle.  A lot of those calculations are way easier to do.  When your vectors are all unit length, meaning that they're exactly one unit.  And see that when I normalize this vector, regardless of the scale of it, it is pointing in exactly the same direction.  Any time you're dealing with orientations, you need normalized factors.  And when you're doing any kind of math operation on these vectors later on, when you're trying to figure out anything that involves orientation rather than speed or position.  You're going to want to normalize the inputs and possibly the outputs of those operations.  If you don't, you can get some pretty weird results. So just keep that in mind when you're doing any kind of vector operations that involve direction.  Let's go to visualizer three here.  Adding vectors is something you'll do pretty frequently and literally all you're doing is just adding up all of the individual components.  So in this visualizer, the red vector is a and the blue one is is B.  And if you go to the ad vectors here, most of this is just stuff for visualizing like everything under here.  But you have vector a vector B and then vector C, the white vector is a plus B.  So you can move these around if you want like a quick visualizer or if what's happening.  But you're literally just adding the various components together.  In practice, when you're working in Houdini, you'll add vectors when you're doing things like accumulating velocity or there's a few other situations where you'll do it.  But you'll find subtraction happens a lot more often. And the rules, you know, addition to subtraction are very closely related.  The rules are largely the same. If I go to visualizer four here and then just grab this.  You can see that again, I'm subtracting a minus B here and the result here is C subtraction happens a lot more often because let's say you want to aim something at something else or you want to, you know, sort of guide a vector towards another point.  The way that you find the direction from a to b is generally through subtraction. And let me just show you what I mean here. I'm going to do some, let's hope this annotation thing here works.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_002.jpg

### Subtraction [6:19]
**Transcript:** If you look at this and you say, OK, here's here's vector a this red one and here's B this blue one and the white one coming out from the origin looks like it, you know, it doesn't look like it's quite necessarily related.  But if you, if you sort of move this third vector up here and look at it this way, I hope that makes more sense where if you're trying to basically say what is the difference between these two vectors, you're literally subtracting, how do I get from a to B, this vector is telling you that.  You'll be doing this a lot more often when you're trying to like figure out a goal, for example, for a particle or if you, if yeah, again, if you need to aim something at something else, looking at subtraction this way can sometimes help sort of drive home the point of like, this is how you're going to relate vectors a little bit more closely as opposed to addition.  I have to turn off this draw thing, OK, so that's the very basics of dealing with vectors.  A lot of the more interesting stuff comes when you start doing vector products, which is what we're going to go into next.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_003.jpg

### Products [7:30]
**Transcript:** There's two products between vectors that you're going to hear talked about all the time, the simpler one is the dot product and what the dot product is is a, I've got to clear that error that arrow off, then I.  The dot product is a ratio between vectors that's easiest way to look at it and what it's doing is just it's returning when you're dealing with normalized vectors anyways it's giving you a value between one and negative one depending on how similar the two vectors are.  So if I right now I have just a vector a here, which is pointing towards positive Z and vector B pointing towards positive Y, and if you grab this visualizer and just rotate around X is probably the easiest one for this example, if you rotate these vectors around X, you can see that you're getting this number that's roughly between negative one and.  Positive one based on how similar they are so if they're parallel you're going to get a value very close to or equal to one if they're perpendicular you're going to get a value around zero and if they're exactly opposite each other you're going to get a value negative one.  There's a lot yeah the dot product shows up everywhere in computer graphics and it's really important because the way that you compute the dot product is.  Is deeply involved in a lot of other parts of vectors like how you compute size of triangles and things like that and we're going to build on this shortly.  But it shows up everywhere and it's really important to know sort of basically what it's doing is you're you're really just figuring out how far apart in angular terms are these two vectors and again the dot products can be and is often used on vectors that are not normalized.  But generally speaking you want your vectors to be normalized when you're doing this calculation or you're not going to get a negative one to one range you're going to get something else and there are reasons why you might do that when you're doing things like projections.  Which will get into in a second but again keep in mind when you're comparing the angles between vectors when you're trying to compare them like this when you're talking about vectors as directions and positions keep them normalized.  Let's go to the next visualizer here.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_004.jpg

### Projection [9:47]
**Transcript:** So another thing that you can do with a dot product is you can you can literally project vector B onto a.  This is useful for when you're doing things like projecting something onto a plane.  Or if you there are other reasons why you might need to project these that are a little bit more complicated that will sort of touch on later on.  But if you look at the projection effects code here what I'm doing is I have a vector a and vector B.  And for C what I'm doing is I'm computing the dot product again.  But I'm I'm computing the dot product between a and B.  So what I'm doing with vector C here which is the white one is I'm computing the dot product between the two.  Which sort of figures out that ratio and then I'm multiplying vector B the blue one by that ratio.  And you can see what that what I end up getting is this projection of this red vector vector a onto B.  And you can see this a little bit more clearly if you sort of try adjusting these values a little bit.  Let's say I move this up you can see that it's it's literally just projecting onto sort of an infinite version of that blue vector.  And if I move B around like example if I move it on the X you can see that it leans to stay parallel with it.  So this is something that's going to come up a lot to when you're trying to you know there's situations where you might want to project a point onto an imaginary plane a certain distance away.  Dot product also comes in handy there so it.  This might seem a little bit abstract but we're going to sort of build on this concept of of projection and comparing vectors later.  Let's get into a slightly more complicated example here.  The dot product is also used a lot in shading calculations because when you're comparing angles of things.  The you know that comes into play when you're for example illuminating something's a lot of the simplest illumination models and computer graphics are based on the dot product because.  So I'm going to be the ratio between the direction of surfaces facing which is the surface normal.  And where a light is sort of implies how bright an area is going to be shaded at that point.  So I'm not going to go over this entire network but basically what I'm doing is I'm I'm bringing in like the point light icon and then transforming it and moving it.  So I have a control here that's using the point light dot BGO so it's just for this is to build an icon that's all it is and I'm transforming it.  So this is so that I can rotate it basically that's all this is it's a really quick visualizer.  So the first thing that I need to do here.  To do the calculation and just to give you an idea of what's going to happen at the end here.  I have this sphere and I'm going to illuminate it and I'm going to do it using the dot product.  So what I want to do is first get the light angle as a vector right so where is this in relation to the origin because the direction of the light is what matters when doing this shading calculation more so that the position would be for like fall off you know for trying to do light decay we're not worrying about that for this example.  So what I'm going to do is get the position of of this light and so I merged in this point here up top because I have just a single point right in the center and that's point zero so that's what I'm going to grab is the position of point zero.  So I'm going to say vector.  vector n is equal to point zero.  I'm getting the attribute P and I'm getting that attribute from point zero and then I'm just going to save this I'm going to bind this to the normal attribute for this object so I'm going to say.  At n equals normalize and that local variable that I just made.  So this is running in a detail wrangle so what I've done is just created because I only need to do this once it's going to be the same I don't need to run this over every point and so I have is right now is a vector where it's equal to one because this light the normalize vector here is on positive Z.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_005.jpg

### Vex and Projection [13:55]
**Transcript:** Now to actually apply this to the points in the sphere.  What I want to do is take that vector and compute the dot product between that vector and the surface normal of the sphere.  So I'm going to run this on points for the sphere because it needs to apply to every point on this.  And the first thing I want to do is grab that camp that normal that I just computed so I'm going to say vector n or let's call this camn to differentiate it from local.  And then I'm grabbing this from the second input here like where I just computed that other normal so.  Detail input one and then grabbing the n attribute I just created.  And I'm going to say float dot because the dot product is a scalar you'll sometimes hear the dot product actually called the scalar product instead of the as opposed to the vector product or the cross product.  And that's because the dot product between two vectors is a ratio it's a single number a scalar.  Anyways, I'm going to that's this is equal to the dot product in vex between the normal of this sphere for each point and that camera normal.  Now I have a ratio between the direction of the light coming in and the surface normal for each point.  What I'm going to do now is with typical shading calculations, you don't usually call back faces, you don't see them at all.  And if you remember from that first example, if the dot product is less than zero, the two vectors are basically pointing in opposite directions meaning it's going completely away from the light.  We're not going to see anything there that's that's in shadow.  So what I'm going to do is I'm going to clamp this value.  Between zero and one because it's if it's you know if the value is negative one, I don't care it's just going to be zero it's not shaded.  So I'm going to clamp dot between zero and one.  And now what I want to do is just say the color value of each of these points in the sphere is equal to I'm just going to ramp this because I like ramps gives us a lot of control.  I'm going to look up a ramp by this is here to one value and one little trick when you're doing.  C H ramp if you've ever done this on a node and then you click this little button here to auto generate parameters.  If you do this normally tramp by default is going to return a float which is fine for this because it would just give you black and white values anyways, but if you really want to make a color ramp if you.  So I'm going to force this to be cast as a vector when you when you write this operation.  It the this auto on this automatic parameter generation button will actually give you a color ramp so just real quick trick otherwise you would have to actually go into the parameter interface for this click on the ramp and then change it from float to color.  So this is just a shortcut for that if you're just trying to bang out vex really quick.  Now that we're here let's go back to this highlighted transform node and then just try rotating this around and you'll see that what you're getting is this sort of very, very basic illumination of this fear from this this fake light that we built.  And as it turns out this this is Lambert shading effectively there's like a couple other nuances you might have to worry about but when you're talking about very simplest core idea of Lambert shading it's the dot product between the light angle and the surface normal.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_006.jpg

### Light Example [17:08]
**Transcript:** Next example real quick this is another similar thing that you'll often see done with the dot product and shading is the idea of like a for now or a.  A expracheter so if I go here again I left this code out but we're going to do effectively do the same thing but just sort of change the terms a little bit.  Let's say that instead of this being a light this is a camera and i'm looking through this and i'm going to just going to reset the transforms we're looking straight down to make this easier.  So again the code is almost exactly the same i'm going to grab instead of a light normal we're going to call it a camera so.  Grabbing the detail attribute of input one here i'm grabbing that and attributes of the position of that same thing there i'm going to compute the dot product between.  So dot between each point on this pickhead and the camera normal i'm going to clamp it.  This is really code same code is before but I just want to type it again to drill it home.  And i'll wrap this up as a vector the same way.  Make sure that the parameter interface auto casts it as this and what you can do here is if you reverse these.  And then look at it through the camera with this is looking straight down these so this is what your your imaginary camera would be seeing and then kind of bend this a bit we can see is that the.  The areas that are perpendicular to the camera so where the normal is facing.  Or thogunally to the camera you get these highlights so it's it's highlighting edges relative to the direction that you're looking and this is something that you'll see a lot on the so called for an L shaders or shader like sheen velvet shaders things like that it's a really common fake that people add.  To real time shaders and in the old days we used to do this a lot to shaders that didn't have fancy bsdf's like we have now.  Where you can just get this kind of extra sheen you know if you're trying to describe a material that's very velvety it'll often have because of the tiny little hairs on it it'll have this kind of extra lighting on the on the edges.  And all it is is adopt product calculation in my this is like what the facing ratio if you do the sampler info facing ratio node that's all this is doing so.  All of these things are very common throughout computer graphics we just call them different things but under the hood it's the dot product between the camera and the surface normal and then just sort of taking that resulting value and bending it somehow to make what you want.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_007.jpg

### Cross Product [19:57]
**Transcript:** So the next thing to talk about before we get start getting a little bit weirder is the cross product and if you go to the first visualizer here.  And the last product is the is an operation that essentially gets you an orthogonal vector to the two vectors that you put in and by orthogonal it just means perpendicular it's.  It's not opposite in that you're not just flipping it because for that you just multiply a vector by negative one it's finding something that points in a direction that neither of the other two can ever point in that's sort of what orthogonal means.  So you go up here to cross vectors and you start playing with these values underneath you can see that I don't actually want to scale this you can see that what it's going to try to do is just find something that points in exactly the direction of the other two doesn't so if I manipulate these a little bit you can see that it's adjusting that third white vector based on what's going in.  And the next thing else worth noting here is that you see that when I these vectors start getting skewed that the cross product is getting longer.  This is also another operation that's very vulnerable to vectors that are not normalized if you click this little check box here.  This normalizes the input vectors and you can see now that you're getting unit length vectors for this whole thing so again you're dealing with vectors and you care about orientation make sure that your inputs are normalized.  And up here all it's doing is it's taking in your input vectors a and b and saying if that box is checked normalize both these vectors and then cross them.  That's all this is doing so it's you know if you're doing cross products you know on paper it's a little bit more effort to get this done but conceptually it's very simple given to input vectors find a third that doesn't point the same way.  And this is something that we're going to do a lot when we start dealing with matrices and rotating things because something you might note here is that if we wanted to for example rotate this red vector on to be this third vector here would be a very convenient access of rotation between the two right so like you can imagine that i'm taking this red vector and spinning it to point to vector B and it would be spinning on that white axis.  So you're going to start seeing this a lot when you want to find out how to rotate one thing onto another let's go to the next visualizer here.  So what this is doing is and you might notice that this is pointing in a different direction from before.  Try rotating vector a around why so right now it's a negative 90 degrees but just try grabbing this and rotating it you know maybe i'll do a little bit slower and see that.  You can see that as it crosses this line vector c starts getting unstable it's not really sure what to think and it flips at some point this is because the dot the cross product is not commutative meaning that the terms that you put in vector a vector B if you switch them you'll get a different result.  The exact order of the result that you get is dependent on something called the right hand rule which is.  You know worth googling could but it's a simple concept it's basically saying that.  For a coordinate system in computer graphics there's sort of some coordinate systems are left handed and some are right handed and what that means is basically just if you have vectors a and B on your and you imagine that your hand your thumb in your first two fingers are these vectors.  If you cross them what direction is that resulting vector going to be so if you're crossing your index finger and your thumb in a right handed coordinate system that third vector is going to be coming out towards your middle finger and left handed coordinate system it's going to do the opposite.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_008.jpg

### Cross and Aim [23:42]
**Transcript:** There are computer graphics programs out there that are left handed I want to say that unity might be a left handed system.  I could be wrong there and there are others i'm sure.  Who Dini is right handed and a lot of other coordinate system computer graphics uses right handed coordinate systems.  You might have run into this before if you're trying to directly translate polygons from one DCC to another and you notice that like normal get flipped for some weird reason or like your objects get scaled in weird ways that's why.  The other thing to note about this is that again if I if I start rotating these things so that.  These factors are almost facing each other just on do this rotation that I just did.  There's you're going to hit a point when they're too close together that they become unstable and freak out and if I go even slower you can see that there's going to be this.  This time in the middle where that.  It's not working as well that wanted it to.  But basically the reason that you.  The reason I bring this up is because if you have two vectors that are almost or are actually parallel to each other and you're trying to find an access to rotation cross product between the two.  There eventually comes a point where it's indeterminate you're not going to find anything useful.  Where you might have seen this in practice is when you're dealing with something like an aim constraint.  If you've ever made an aim constraint before you'll notice that you need a forward vector like your aim factor and an up vector in order to.  Solve an aim and you'll notice that if your objects are passing right over each other you'll get some possibly popping or flipping behavior as that as that thing crosses the reason for that is because of this property of the cross products like it becomes increasingly difficult to get you a stable state.  So you can see that the solution as those two vectors converge and then once they go in the opposite direction it flips.  So keep that in mind.  This is why that behavior happens and knowing about it mean and carefully determining where your up vector is and making sure that it's as orthogonal as different from the normal vector or your aim vector as it can be means that you're going to get more stable solutions to these problems.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_009.jpg

### Part 1 - Geo Normals [25:58]
**Transcript:** And the visualizer here is sort of another practical application of what you can do with the cross product.  There's something that you might have heard effects artists talk about whether call it like the double cross or sometimes the double cross product.  And what it is is a way that you can kind of manipulate normals on a surface in order to get interesting behavior for particles or for sometimes other things.  So to really quickly go through this example. I have a line here and I'm resampling it and then using guide process to bend it along one access and then copying it so that I get this sort of funnel shape.  I'm going to skin it into a surface and then reverse it.  So now I have this funnel where the normals if I turn them on are pointing out this way.  I'm going to compute normals on points so that I have one normal vector per point instead of the default vertex normals.  So you can see how they're all facing here.  So let's go down to this point wrangle here.  And what I want to do is kind of show you what the cross product does on polygons.  So let's say that I want instead of I want a force that's going to kind of swirl around this funnel.  And I've got these normal vectors pointing this way, but like how would I rotate them so that they are swirling around something.  And the cross product is going to come is going to come in handy here. What I can do is I need to define sort of what the access of rotation is going to be to rotate these normals into the position that I want.  And a lot of the time an easy way to do that is to just figure out what my if you imagine this being a cylinder for us or a vortex force, what is that access going to be.  I'm just going to use world up for this example. So I'm going to say up equals.  I can just say zero one zero for this or if I want to parameterize this just for an experiment, I can say normalize and then c hv, which means a vector parameter for this wrangle up.  So I'm going to take whatever I put in here as up and normalize it and that's going to be my up factor.  So if you hit this button, you're going to get this parameter added here. I'm going to set that to one. And so now my my up factor is world up real simple.  And I'm going to say n equals normalize again, we want to make sure that our normal our vectors for this are normalized after the fact.  And I'm going to cross the normal factor and the up factor.  And if I'm just going to go down to this ad notes that I'm not seeing the polygons necessarily, what you can see is now I get this swirl force where these things are sort of following the direction of the surface.  Sort of rotating around what I've defined as that up factor and you can you can see these things kind of leaning if I adjust this up factor so that it's pointing in different directions.  It's going to try and lean around a different axis, but this this tube is facing or this funnel is facing an obvious world up axis. So it looks best here.  So  the double cross, which is something that I brought up earlier is sort of doing this twice. So I'm going to go down to this point wrangle here. And what I'm going to do is sort of cross that vector that I just generated again against up.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_010.jpg

### Part 2 - Geo Normals [29:35]
**Transcript:** Or against n to get you an even different flow.  And I'm going to analyze this ad note here right now. This is going back to our original normals.  So again, I'm going to define an arbitrary up vector. So this is going to be just a parameter that I define is up that I'm going to normalize. So we're going to set this to zero one zero again.  And now I'm going to set the new end to be normalized. And then I'm going to again cross n against up.  Instead of crossing and just against the up vector, I'm going to cross it against another cross product. So forgive me for this.  So I'm crossing the normal and the up vector and then crossing that again against the normal.  And the result is going to be that it's going to sort of follow the contours of this along that up vector that I defined.  And then I'm going to take like a funnel force where you wanted particles to be sort of following the surface but flowing downwards.  The double cross product is a great way of getting that. So basically I'm finding that that swirling force first. And then by crossing that against the surface normal again.  It finds the orthogonal vector, which happens to be pointing.  So it's basically taking the up vector because I'm starting from up and end and finding the orthogonal vector. So like the third one.  And then crossing that again against that up vector is is finding the orthogonal vector, which means it basically wants to point down again.  But projected onto the surface. So like following the contours of it.  And then I'm going to come and trick that you'll see a lot that effects are to see is even if they don't really understand what's going on under the hood.  Because if you want particle flows to follow things and you don't necessarily want to do that using collisions or whatever.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_011.jpg

### Q&A: Cross Product [31:26]
**Transcript:** This is a very cheap and effective way to get that effect.  So the cross product is again getting you this angle. So if I cross these original normals against and I'll just put add on this as well. So that's a little easier to see.  So here's our original normals and I'm crossing this against the up vector. So world up pointing straight up and the orthogonal vector that you get from that is this one.  So it's following the contours of the surface, but it's pointing sideways. All of these vectors are more or less pointing along the XZ plane because we're crossed against the Y axis.  So everything's been projected onto XZ, but following the direction of the surface.  When I take that and cross it against the up vector again. So crossing it against I'm sorry crossing against the normal again.  Which is this go down here.  If you remember the way the normals are originally facing and then you take these projected on the XZ plane. What I'm going to get are things that are going to be trying to point down the Y plane.  Just do this again. But they're effectively relative to their point position.  They're how do I explain this? This does get hard to sort of describe in words.  But they're going to be trying to point down why but because of the where the original vertex surface normals were on this count.  They're going to be facing along the surface because they're influenced by that normal direction.  But it's just a sort of an orthogonal 90 degree rotation from these vectors, right?  So these are this is the first cross product and then by crossing against the original normals again.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_012.jpg

### Refresher [33:17]
**Transcript:** We get this third we get this orthogonal vector.  All right. Well, the trig example here is the second file and this is mostly just a quick demo.  It's it's it's not something we're going to do a whole lot of Vex to fill in. I've just built a few visualizers here to help you.  So we're going to do some of the trig concepts that we're going to be using.  So if you open up O2 trig and then open up trig basics here go all the way to the left.  So when we talk about trigonometry.  If you're not thinking about triangles the first thing that most people are going to think of is sign cosine maybe tangent if you remember anything from that time and of your life.  We think of sign ways all the time it's like yeah line goes up line goes down it's real simple.  All I'm doing here is computing a side wave and then I have some things in here to determine you know my amplitude the frequency all that stuff like.  And that's commonly a way that people will think about sign waves right you think about it almost like audio.  You're probably also familiar with sign and cosine referring to the ratios of the sides of a triangle.  Because these things this is sort of why sign and cosine exists is to describe the relationships between these angles on a triangle.  So I built this little little visualizer here.  Where you can kind of see if you scrub the timeline that the sides of the triangles change we have this guy on the bottom here this is our angle you know like when you talk about.  Solving triangles you usually are picking an angle you're picking a corner and saying like this is theta right this is my angle this is what I care about.  And then you name the other sides.  Based on that reference angle that you've chosen so this guy on the bottom here is my adjacent side this guy here is my opposite side and this guy that connects the two is the hypotenuse.  So so Katoa all that stuff when you're talking about the sign you know sign is the opposite over the hypotenuse all these things are ratios of triangles and the angles between them.  So again you can kind of move this around you can see that this angle theta here is a consistent ratio that's solvable based on the lengths of the sides of this triangle.  We go to the next example here and keep in mind take a look at these numbers and just sort of keep them in the back of your head as you go to this next example.  So rather than just look at this as the lengths of the triangle you can also look at this as coordinates so this top point here that's made where 3.4 is the length of one of these sides and 3.66 is the length of the other when we're talking about these blue sides.  You can see that these are basically coordinates in a coordinate system if we're looking into dimensions.  And something that you'll also notice is that these this point follows a circular arc.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_013.jpg

### Unit Circle [36:14]
**Transcript:** But thankfully in Houdini a lot of this stuff will be handled for you so you won't have to memorize quite as much.  I'm losing my train I thought there.  So again this is this is important to think about this as it's not just the lengths of sides of a triangle you can also think of this is almost a this is a coordinate system right like I am this point in space is directly related to these links of the triangle sides.  Let's go to this next example.  So now we're doing a 90 degree rotation.  So at the very bottom here you can see that theta is just about zero and we're at you know this this we're at five in coordinate space five comma zero.  We go all the way up here and now we're at 0 comma five approximately.  And you can see that at the at the very top here at the top of this arc the value of theta down here is roughly 1.7.  Well, one point five six seven.  And if you were to multiply this by two let's just grab a calculator and do it.  Let's say 1.567 times two and this is rough because this is not mathematically perfect.  This number may look familiar to you this is pie.  So what we're dealing with here is a 90 degree rotation and if you remember from basic geometry that the circumference is a circle is two times pi and then times the radius but we're dealing with unit circles here so we can forget about the radius.  We are dealing with two times pi is forming a complete circle. So this 90 degree rotation here in radians because we're dealing with angles most of the time we're dealing with radians because it's much simpler for the math and more precise.  We're dealing with a pi over two rotation and that's where this number comes from so keep that in mind.  All of this comes back to the unit circle which is going to come back to these normalized vectors and everything else is all very important when when you're thinking about rotations.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_014.jpg

### Cosine [38:28]
**Transcript:** So one more example here.  What I'm going to do is let's just go back here and I've run this through a time shift so that we're focused on a very specific point.  If you take a look at this vex here.  What I'm doing is just defining vector one vector two here and which is derived directly from these points in this visualizer and this expression here is basically using the inverse of the cosine function.  So the cosine is of an angle describes the ratio between the two sides of this triangle.  But if you have the two sides already let's say I already know that my vectors here are 0.53.3.53 whatever I know what these vectors are.  But I want to find this angle theta. I can turn the cosine on its head and that's called the arc cosine or the shorthand and vex is going to be a cost.  And then get the dot product between them which is the ratio between those two angles. Remember we're talking about ratios with the dot products are the difference between how these things face.  And finding the art cosine of that gets me that angle theta.  This is also going to come up quite a lot. This is basically telling me what the angle between these two vectors are.  And if I open up the spreadsheet and go to detail view because this is a detail wrangle you can see that theta here is 0.785398 which matches this exactly.  So the art cosine is something that's also going to come up very soon in this lesson because it is a bog standard way and CG of telling you how what is the exact angle in radians for me to rotate one vector onto another.  And if you remember if you want to find the axis of rotation between those two vectors, you have the cross product to solve that. So now we've just found a way to relate the two sides of this triangle or these two vectors in space in a very precise way.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_015.jpg

### Introduction [40:41]
**Transcript:** This is just a really brief overview. Obviously, trig goes way deeper than this, but these are concepts that we need to kind of know right now.  Okay, great. Yeah, real fast trig crash course.  So let's go on to the third section here, which is O3 transforms.  And this is where stuff is going to get much more closely related to mobs and also a lot more difficult.  So there's a lot of examples in here. We're going to start the very top in matrix intro and then just start here on all the way on the left.  So if you're looking at this visualizer here, it's just home selected.  And then click on this so that you can see this sort of this matrix toy node.  A matrix in the simplest terms when you're dealing with CG is a stack of vectors.  So when you're looking at a transform matrix like this, these are the input values that I'm that I'm putting in. And then I have some controls here to be able to rotate it to manipulate it.  And then these are the output vectors that you would get from that.  You can see here that X is equal to 1 0 0. Why is equal to 0 1 0 and Z is equal to 0 0 1.  This is called the identity matrix or the identity transform. And it's called that because this is basically saying, if you look at these as X Y Z coordinates, it's saying that X is facing X.  And you can see that with this arrow if you compare it to this little nomen at the bottom. Why is facing why it's facing up and Z is facing Z.  So it's a transform that's just saying everything's normal.  You know, I am not my coordinate space is no different from the world space that we've all agreed on.  And you can see that if I drag this theta slider here, it's going to rotate around whatever axis we've picked.  And the output vectors are going to change accordingly.  So we're going to use this toy a few times to illustrate a couple of different concepts about what matrices are, but it's important to be able to look at them and say this matrix is essentially a stack of vectors telling me which way this shape is or which way this my local coordinate space is facing relative to the world.  So let's go on to subnet two here.  This matrix is representing a 90 degree rotation around X. So my access is 100 and theta is pi over two, which is a 90 degree rotation. Remember two pi is a full circle. So pi over two is 90 degrees, a quarter turn.  So my input values are the same. This is an identity matrix going in, but my output values, ignore that these are coming in and scientific notation and just assume that this is basically a zero and so is this.  This is just because floating point precision always means that your numbers get a little bit weird when you're talking about very small things.  But notice that when we do this rotation X is still 100 because X is our access of rotation. So that vector isn't changing at all.  But why is now facing positive Z and you can see that here when you're comparing it to this nomen down in the corner.  And Z is facing negative Y.  So this is, this is important. I mean, when you look at these vectors and you see these values.  It's describing sort of at once an orientation. So like this is how this thing is facing. And it also is potentially describing a rotation from some reference point in this case, our reference point is world space.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_016.jpg

### Direction [44:06]
**Transcript:** It can mean either thing. I hope that's not too confusing, but you need to be able to look at this in both ways.  Like either this is a matrix and like this is just its point and space. This is the direction that's thing is facing or this is a rotation that we could accumulate onto something else and we'll go into that deeper later.  Now let's go over to subnet three here. And this is just again the same visualizer, but I've tied theta to time.  And so if you rotate this slowly and just take a look at the numbers that you're getting in the output.  So I'm going to go roughly to a 90 degree rotation here.  And look at the numbers that we're getting. So why is roughly facing Z and Z is roughly facing negative Y.  And then go back again to about here. And now we've got Y facing negative Y and we've got Z facing negative Z.  And then back again and notice that these numbers as we rotate are always oscillating between negative one and one.  You're never going to get past that when you're dealing with normalized factors when you're rotating these things.  Those numbers should or those ranges anyway should seem pretty familiar to you because they are the ranges of sign and cosine functions.  When you take the sign of a number, you're going to get a value between negative one and one.  You're taking a cosine. It's the same deal.  This whole sign cosine nonsense from trig is popping up again here when we're talking about rotation matrices because again we can describe rotation through sign and cosine.  If you remember that visualizer on the trig end of things where we're taking those two vectors and turn and thinking about it as a coordinate system instead.  And then it all goes back to sign and cosine that rotation that transformation that we're applying to that point can be looked at as a rotation from the origin.  And that's described by theta that angle right.  So all of these things are pretty closely linked together.  So let's do this with geometry now instead of doing this with this little this little visualizer here.  What I'm going to do is go over to this pick out here and go down to this this point wrangle.  And it is totally okay if you don't understand what's going on here, but I just want to fill this out with you so that you can kind of see what's going on here in terms of trig.  So I'm going to define theta first.  So theta equals CH theta just like we had before.  And now my access of rotation is X.  And I'm just going to say X is one zero zero.  Just world X that's what I'm going to rotate around.  Now what I'm going to do is describe Y and Z in terms of trigonometric functions.  And again, you don't this is not something that you would normally directly do in Vex.  This is all I want to do is describe the math under the hood.  What a matrix is doing when you tell it to rotate around X because it's very particular.  So let's define vector Y.  And again, we're going to use set because this is how you build vectors in Vex when you're not building them as constants.  You use the set function instead of using curly braces.  So vector Y is going to be again zero.  But why is going to sort of oscillate in Y between negative one and one based on a particular trig function.  And in this case, it's going to be the cosine of theta, which I'm going to in the channel that I'm building.  I'm going to build it as degrees to make it easier to think about.  But we need to convert degrees to radians. So that's why the radians function is here.  And it's the it's the cosine of theta essentially.  And then why is Z component.  Because again, the X component isn't going to change because we're rotating around X is going to be the sign of theta.  And then Z is going to be very similar, but just slightly different.  Again, we're not going to change the X component because that's our axis of rotation.  So in this case, the Y component is going to be the negative sign of theta.  And then the Z component is going to be the cosine.  So it's a lot of sign and cosine functions with very specific orders sort of wrapped up into these three vectors.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_017.jpg

### Rotate [48:49]
**Transcript:** What we need to do at this point now is make a matrix out of them.  And in Vex, if you want to make a matrix out of a stack of vectors, you can again use the set function.  And we're going to say matrix three M.  And keep in mind, a matrix three is what we're dealing with right now because we're dealing with a set of three vector stacked on top of each other to describe.  In this case orientation.  It's not the same as a matrix, which is a four by four matrix. We'll get to that later on.  Just make sure you're calling this matrix three because the distinction is important.  And I'm just going to say set XYZ.  So I'm literally just stacking these three vectors on top of each other and making a matrix out of them.  Just like we had in the matrix toy when I was showing you the visualizer there, same deal.  And now let's just as a test, I'm going to bind that matrix to a variable so that I can see it in the spreadsheet.  Just for curiosity sake, like so we can see the numbers.  I'm going to say three at test equals M in Vex, the prefix for a matrix of three by three matrix type variable is three.  And the last thing that I'm going to do is when you want to transform something by a matrix, whether it's a vector or another matrix, you multiply them.  Part of the magic of matrices is that they are straightforward for computers to multiply, especially when they're square.  And we're always going to be dealing with square matrices and CG.  But if I want to take this matrix that is a rotation in space and apply it to these points, the point attribute P is a vector.  So all I have to do is just multiply it by this matrix.  So I'm going to say P times equals M.  And now I'm going to click this so that I can make this theta parameter.  I'm just going to set theta to frame so that it rotates.  You know, according to degrees equals frames.  So you can see that what this thing is going to do is rotate pivot it around the origin.  Around the world X axis.  So again, I don't, I don't have this memorized.  I wouldn't expect anybody to memorize it because this is literally just like, just if you want to do a rotation strictly around X.  And I'm talking about rotating around multiple axes like you have to start solving slight variations of this matrix for each component.  It gets really cumbersome. And this is why you have computers to do this for you.  The point here is to try to relate the fact that all these trigonometric functions talking about that, you know, those sine waves going up and down negative one to one is talking really about circles.  And when you're talking about circles and rotating from one point to the other, now you're talking about vectors.  That's why again, everything is talking about a unit circle.  So we have this very simplified system that we can use to an extrapolate that to finding relationships between vectors and so much of CG.  As we see it involves vectors, your, your, your normals, your point positions, all of that stuff.  And that's why this stuff keeps coming back over and over again is because it's all really tightly interwoven.  So anyways, I've got this really great spinning pig head.  I'm going to just split my viewport really quick here so that I can see the, I have a built in.  So I can see the spreadsheet.  And if you remember, I bound that test attribute.  So let's just stretch it out here.  So you can see that I've got these numbers. This is my transform matrix. Essentially, this is a matrix that I just built. So again, X doesn't change because that's my access to rotation. So that's a 1 0 0.  And as I slide, you can see these things, these values against oscillating between negative 1 and 1 as they go through this rotation process.  And what I want to do really quick here at the bottom here is, let's just, I'm going to, I'm running this through a mobs convert some turning this into a packed primitive now.  And if I go to the transform modifier here, I'm just doing exactly the same rotation, right? I'm rotating around the X axis.  And I don't have a transform attribute here now that means anything because I've converted this. But if I go to primitive view, then activate intrinsic and go to transform.  And you can see that again, I've got these this three by three transforms and nine values. And I'm started, you know, the X axis is 1 0 0.  And it's exactly the same values going across what I'm doing this rotation. So I'm oscillating between negative 1 and 1 across these, these various axes. And you can see that the X component of both the Y and the Z vectors that make up this matrix are not changing because we're rotating around X.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_018.jpg

### Scale [53:42]
**Transcript:** And this is the math that mobs is doing under the hood, abstracted somewhat by Vex to rotate something. It's all it's all trig under the hood.  Okay, let's do one more here. I want to talk really briefly about scale as well. So we've got another peghead here. And then another wrangle and what we're going to do is build up a matrix and scale it instead of just rotating.  So let's just build out a quick little interface so that we can just type in vectors and get a matrix out of it and see what happens. So I'm going to say vector X is equal to vector channel X and just do the same for Y. Right. So Y equals CHV Y and Z equals CHV.  So I'm just going to do this faster ways to do this, but whatever.  Then I'm going to make a matrix three, call it M and set it to that stack of vectors XYZ. And at the end, I'm just going to say multiply P by that matrix.  And then when I hit this button, now I've got the stack and you can see that everything is zeroed out.  I'm going to make an identity matrix to start. So X is 1 0 0 Y is 0 1 0. And now you can see I've gotten this this pancake pig completely flat because Z is zero. And now 1 0 0.  And now I've got the complete pig again. And you can see that if I scale any of these vectors up. So there no longer unit vectors. I'm stretching the pig out along that axis. And if I set them all the same.  This to to to down that diagonal. Now I've got a pig head scaled up by double.  Another thing to notice here is that if you start bending these axes so that they're no longer.  They're no longer normalized vectors and are thogonal to each other. You can see that now I'm going to start getting a skew.  So this thing is sort of stretched out in weird directions.  Scaling matrices is literally just scaling the vectors that go into the matrix. So a three by three matrix like this.  And this is important. A three by three matrix defines both the orientation of something in space like we were describing earlier and the scale.  So all the sine cosine stuff is mostly handling like rotations like figuring out which way all of these things are pointing.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_019.jpg

### Aim [56:07]
**Transcript:** And then the length or the magnitude of those vectors is determining the scale of the object and all of those things combined together makes your three by three matrix that determines what this thing is doing in space.  Okay, so let's go to the next section and this is aim matrix. So all the way on the left here.  What we're going to do is build a very simple aim constraint, right.  Essentially what mobs aim is doing under the hood, but also anything else that does constraints and CG.  This is how roughly how it works. So I've built out this quick little setup here where I've got a control object.  So just this little plus here. I'm packing it and the only reason I'm doing that is so that I have a single point at the centroid of this so I can just use this as a control essentially.  And so it's easier reference in Vax and now I have a point in space that I'm going to copy something to.  And that thing is going to be the pick head.  So what I want to do is make a matrix that describes an orientation in space.  So I still like again, I need those those three vectors.  But what I want to do is make it so that the vectors are pointing in a specific direction that I care about.  This will make more second sense in a second, but what I want to do is and let's template this target.  I want the vector that let's do this annotation thing again, I want the vector that points from this pig heads position to this.  And if you remember from a few minutes ago when we were talking about vectors, the way that you can sort of figure out where's the direction of something like how to like point something as something else is through subtraction.  Get that target position as a vector.  So let's say vector target is equal to point.  And then one because I've got this packed point going into the second input here so input one P and then it's just one point so that is the position of this little cross in space.  What I want to do now is subtract my current position, the position of the pig head or the point that this pig head is going to be copied to the single point from that from that targets position.  Because again, vector subtraction and I'll scroll over this in a second vector a equals normalize because again for orientations everything's got to be normalized.  Make sure you're doing this target minus the point position.  And so if you remember vector subtraction, I've got this vector here, which is essentially an error with no length and I've gotten and then I've got my can't I draw.  And then I've got no you don't like that.  It does not want me to switch off of mouse mode so unfortunately, let's try this again. There we go.  So I've got this vector and what I'm doing is I'm subtracting this minus this and the order matters what that's going to do is say that like I can describe it.  And then I'm subtracting this target minus this makes sure that rather than the opposite make sure that the arrow is pointing this way because this is my aim direction and then normalizing it makes it a unit vector.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_020.jpg

### Aim with Orthogonals [59:30]
**Transcript:** So now it's about this long and it's going to describe the orientation rather than this full stretched out vector describing the exact difference between them.  Okay.  So the next part.  Remember that in order to define a matrix and a matrix is an orientation, I need three vectors right I can't just say forward because then that's only one component of that matrix stack right we need three.  So if you're used to dealing with constraints and 3D the other vector that is takes part of this is the up vector right like that's the other part that you usually have to define in order to define to describe an orientation.  And this can be really whatever we want in this case you know we're not doing anything too complex.  So let's say up equals world up zero one zero.  So with these two vectors were almost there.  We don't quite have a full matrix stack because we need three vectors right and when you're describing an orientation for something.  You generally speaking you want that matrix to be orthogonal that means that you're going to get a stable orientation something that's not going to jitter and flip a bunch.  So the way that you do that is finding the orthogonal vector to end and up and if you remember from from before if you want an orthogonal vector to two others you use the cross product.  So I'm going to say vector side is equal to and again we're going to normalize we're going to cross that up vector against our aim vector.  Now we have a stack of three vectors right.  The problem with this approach and it's you know it is a problem but it's it's solvable is that again I'm going to stack to be draw here remember that we've got this aim vector here and we've got this up vector here.  These are the two vectors that we've described and our cross product from these two is going to roughly be directly along the x axis so it's going to kind of be pointing out here.  But the cross product of these two vectors and if you if you look at like the.  If you look at the difference between the aim vector here and the up vector this is not an orthogonal matrix right like this vector here the side vector is orthogonal to these two.  But up and an aim are not orthogonal to each other so we're going to get weird results out of this if we try to use this to describe an orientation.  So what we need to do is take another cross product to basically say we're prioritizing the aim vector in this case because we know that we need the aim to be perfect.  What we're going to do is say okay this aim and this new side vector we know that those are orthogonal to each other because we just crossed them.  So what we need to do now is cross the up vector again against this aim vector in order to get a third basically to adjust the up vector so that it's pointing in a way that makes sure that these three vectors that make up this matrix or all orthogonal to each other.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_021.jpg

### Aim Constraint [62:42]
**Transcript:** And that means that we're going to get a stable orientation that's not going to not going to look weird.  Again, the way that we do this is through more cross products so really quick i'm going to say up is equal to the cross product of the aim vector which we're prioritizing and this new side vector that we created.  So up is not going to be perfectly world up it's going to be something else that we made up now i'm going to make this matrix so matrix three m is again we're going to set and we're just going to say side up aim.  The reason for the order is because when you're talking about orientations in new dean the normal vector which you'll often in world space you'll think of as as positive z is the third vector in the series like when you when you think about aiming anything at anything else the default aim orientation in new dean in a lot of other 3d software is z camera's traditionally and cg point along the negative z.  But it's it's always z at least almost always.  So that's why the order here we're starting with side because that's our x axis which is sort of conventionally this is side the up axis is relative to why or world up so it's our second vector and then aim is the third.  So now we've got that stack of vectors and we've turned them into a matrix and so now the only thing that we have to do is turn this into an attribute that copy to points understands we could if we wanted to just define VN and up as two vector attributes and feed that to copy to points and copy to points will do this for us but what I want to do for now is just feed it this raw matrix data and see if our idea here worked.  Copy to points recognizes a lot of attributes there's a whole stack of them that you can view in the in the help for template point attributes in who do you need.  But one of them that are recognizes is a 3 by 3 matrix called transform so i'm going to say 3 at transform is equal to this matrix M that we just generated.  And now if I go down to copy to points and I commit that Vex you can see that this pig is looking directly at that point and if I just move the sky around.  You should do the same you can see that he is doing his best to stay glued to that thing.  So this is essentially how an in constraint works and if we don't provide an orthogonal matrix if I don't do the second cross product you're going to see that he he starts getting real weird because we're no longer providing an orthogonal matrix.  And so he's actually getting scaled and if we move this control around he's going to get scaled in increasingly strange ways the closer that the end and up vectors are to each other when we build this.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_022.jpg

### Vex Shortcut [65:40]
**Transcript:** So that's why we do that that extra cross product to make sure that all of the vectors that we're describing are orthogonal to each other that's very important if you don't want to skew something.  Okay, with that let's go to the next example.  So this is a pretty similar setup we're just going to do the copy to points and we're going to aim based on that control but rather than having to make this matrix from scratch every single time.  It's much simpler to just use something like there's Vex functions that are built to do this for you so again let's get the target position so vector target.  And so vector target is point one P zero because this is the same setup we're just grabbing the position of this little cross here.  I'm going to say the aim vector is equal to again normalize it I'm going to subtract P from the target position so we're getting the factor that aims the pig head at the cross.  And then we're going to get an up factor again and for this just be arbitrary to find zero one zero is our up factor and then rather than having to do all these cross products manually who Dini has this really great function called make transform that's going to do all this for you.  So matrix three m is equal to make transform what it wants there's a lot of different overlords to this function you can see them in the help.  So I'm going to do a simplest what it wants is an aim vector and an up factor so aim up.  And then again I'm just going to buy into this resulting matrix to three at transform.  And now copy to points reads that transform attribute and I can again move this controller around.  And it's doing I've got this wrong one template here should turn that off it's going to get confusing.  Now I can move this around and again who Dini is basically doing that all of those cross products to get you an orthogonal matrix under the hood for you so you don't have to spell it out completely.  But make transform is essentially doing exactly what we just did.  The other thing that we can do and there's like there's a lot of different ways to solve these problems like they're always are in who Dini rather than go through all of the effort of even figuring out an up factor and all this other stuff.  We can just say we want this target vector and then just say matrix three m equals look at and we just give it the target point and the starting position like what vector we starting from to look at bind that to a matrix and it's going to do exactly the same thing.  So can grab this control wiggle it around and it's doing exactly the same thing so vex gives you a ton of tools to do this so that you don't have to think quite so hard about the math.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_023.jpg

### Blending [68:24]
**Transcript:** But it is important to understand conceptually what you're actually building when you build a transform matrix you want an orthogonal stack of three vectors that's the basic three by three transform.  Okay, let's keep going. So another thing that you can do in and this is something specific to who Dini and I'll explain in a little bit more detail why this is the case later and this is something they actually added pretty recently.  Who Dini allows you to blend between two matrices by using the slurp function.  Normally slurping is something that you can only do between quaternions which we're going to go over at the end of this whole section.  But who do you need kind of gives you this little shortcuts so that you can quickly blend between matrices and not have to worry so much about going into quaternion land because it can just it takes up extra lines of code and quaternions are really confusing.  So what I'm going to do is this is again the same setup that we're just sort of layering on over and over again I'm going to grab the target.  Find it to a vector. I'm going to make that aim matrix that we just made using the look at function.  So look at target from position P.  And then I'm going to create a third matrix that's just going to be let's say I want just a blend to decide how much do I want to have this pay head look at this object.  So I'm going to say matrix three we'll call this ID or the identity matrix and then set it equal to identity.  Ident is a function that generates the identity matrix and if you remember from early on when we were talking about that when we built looked at that matrix toy.  The identity matrix is just ones diagonally across the board and it just means that X is X Y is Y ZZ.  So we're essentially saying no operation.  Now what I'm going to do is determine a bias value.  So this is going to be basically the blend factor in mobs terms. This would be the fall off.  So here's my bias factor and then what I'm going to do is say M this matrix that I want to output is going to be.  Flurp which is basically blending orientations.  I'm going to say blend between the identity matrix and no operation.  And our aimed version of this and I'm going to blend it using that bias value, which is sort of a zero one value where zero is going to be the identity matrix and one is going to be our aim matrix.  And now when I do this and I go here, if I grab this control.  Sorry, I just need to.  Oh, I forgot to bind it to transform. So three at transform equals M and now copy the points is reading it.  Or should be.  Just make sure I'm looking at the right.  There we go. So now when I drag the slider, let me template this.  I can move this thing around and it's going to be looking at it and aiming the same way it was before.  Sort of crank him up a little bit.  But now when I go to this wrangle and I adjust this bias, I'm interpolating between those two matrices.  So I can blend between them.  This is effectively what fall off is doing for a lot of operations.  It's just taking these matrices or quaternions or whatever they are.  And interpolating between them based on a bias.  This is applying the same bias to every point.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_024.jpg

### Wrong Orientation [71:45]
**Transcript:** So now let's go to this next section aim without up.  And what I want to talk about here is sort of related to a very common problem that happens in Houdini when people are trying to orient orient things.  And I don't understand these fundamental terms that we've been going over here.  So in this first copy to points here all the way on the left.  I have this pig head here.  And the pig head is facing positive Z by convention up is up Z is forward.  And now I have a grid and I've computed normals on it.  And you can see that the normals are all facing straight up.  When I copy this pig head to these points, why are they facing this way?  Like you would expect that if I'm, you know, everything is basically, you know, air quotes normal.  I've got this grid. It's facing straight up.  Why are these pigs laying on their backs?  Like, why is that happening?  What's happening is that Houdini is trying to guess internally because you're only giving it an n attribute.  It has no up factor.  And so what it's doing is trying to guess at what that means.  Like what what up what's up?  It has no idea.  And so what's doing is trying to take an educated guess.  So again, if I go down to this visual factor visualizer here, turn off my actual vector thing.  The up vector here is positive Y.  This is this is sorry, my normal vector here is positive Y, which is also conventionally what you would think of as the up vector.  They're completely perpendicular.  I'm sorry, completely parallel.  So Houdini is trying to guess which way up should be by creating an identity matrix and then rotating that matrix so that it's n lines up with yours.  Let me just turn on another visualizer here.  So you can see that like if Z instead of facing Z this way is facing up, then what it's doing is saying well up must be facing back here towards negative Z.  It's rotating the whole thing because it needs an orthogonal matrix in order to work.  And you're giving it a normal vector that by definition requires it to be facing this other direction that doesn't know what to do.  Let's go over here to visualize your three.  So the guess that it's computing is based on the same vector math and trig that we were doing before.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_025.jpg

### Understanding Orientation [74:21]
**Transcript:** So it's looking at your old normal like sort of the default world normal, which is zero zero one.  It's then saying, OK, your new normal is zero one zero right like that's the normals of the grid.  It's trying to figure out the axis of rotation between them. So this is step one. How do I get this this the world forward vector positive Z to be facing positive why, which is what we've told Houdini to do.  So it's crossing those two vectors together and then getting you this red axis here, which is the X axis. So that's step one.  The next thing that it's doing is, and I'm just going to turn on this visualizer here so that we can see what's going on is, and you can see that this is sort of returning the identity matrix right now X is X, Y is Y is Z.  So what we're going to do is do a little bit of trig to get the angle and the axis to rotate positive Z on to positive Y.  So bear with me here. There's a little bit of code involved in this.  I'm going to describe my old normal factor. So this is let's say world, and this is the world normal essentially this is our original case is zero zero one.  So we're already are new normal where we want to get to is zero one zero because if you remember that what we were looking at earlier are our grid normals are pays are facing positive Y.  So as we know, if we want to find the axis of rotation between these two vectors, we can use the cross product to do it. So I'm going to say our axis is equal to the cross product between old and and new end.  So the axis is the fact that we're going to be X or negative X, depending on the order in which we define the cross product.  So the next thing that I need to do is find the angle between the two vectors. And before if you remember, we can invert the cosine function and use that on the dot products, the ratio between the two vectors to find the angle between them.  So the float angle equal to the art cosine a cost of the dot product of old and a new ends this is going to give us the angle between these two vectors in radians, which is going to be pi over four essentially we already know this.  Now that I have the axis in the angle, what I'm going to do is create an identity matrix. So I'm just going to create a generic matrix here.  And then rotate this matrix and rotate is a special vex function that if you didn't already know this when you look at vex functions. There's a handful of them, including rotate scale XYZ is also a really common one that if you look at the documentation and you see an ampersand here in your parameters.  What this means is it's an out type argument of a function. And what that means is that rather than just return.  You know, whatever is on the left here like normal, you'd say like float or matrix three or vector where like the function returns something so you could say this equals this.  And out type argument with this ampersand means that it's going to write directly to those values in the function. So I'm not actually returning a value here. I'm just overwriting the matrix definition by using the rotate on it.  So the first argument here M is the matrix.  And this function is going to write directly over that value that I put in this can get really confusing to people who aren't used to vex.  But if you look at XYZ disc, it's one of the more common functions that you use in vex to like figure out sort of the closest position on a surface.  And it will do the same thing because the closest position on a surface with XYZ disc is it's determined by two completely separate values, the primitive number that your closest to and the parametric UV coordinates on that privative.  And so a lot of functions that write the multiple things like that are going to have these output style parameters. So look for that ampersand when you're looking through the vex docs.  Anyways, I'm going to rotate this matrix M. I'm going to rotate it by the angle that we found earlier in radians and I'm going to rotate it around that axis. And I'm going to bind this resulting matrix to three at transform so that the point wrangle could see it.  And now you can see that immediately when I do that when I the visualizer that I have set here is showing now that Z is facing positive and Y is facing towards negative Z.  So and now if I go to the copy to points here, you can see that all the peg heads here are laying on their sides. And this is why it's again, who do you really needs an orthogonal matrix to make all of this work.  And if you only give it one vector if you're not providing an explicit up vector when you're trying to aim something, the is going to take its best guess. And this is how it does it in order to make sure that when you're copying things it's at least consistent from seeing to seeing even if it's very rarely what you actually want it to do.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_026.jpg

### Shortcut [79:20]
**Transcript:** So last example here, I'm going to go over the copy points here and I've got this whole group of pickheads and what I want to do is just really quickly sort of show that so it shows a shortcut to all of this like this whole axis angle or cosine thing that we just did.  There's a much simpler function that you can use to figure out how to rotate one vector onto another, which is what who do you and copy to points wants to do. So I'm going to grab old N, which is again, Z.  And then the new end, which I know is positive why. And then rather than do all this our co cosine wizardry, I'm going to say matrix three M is equal to dihedrial, which is a really magical little x function old N.  What dihedrial does is it computes the matrix that would rotate vector a on the vector b, which is exactly what we just did using a cross product and our cosine of the dot product earlier.  Is just wrapping all that up into one very convenient function for you so you don't have to do all this by yourself.  Now I'm going to say three at transform equals M.  And now you can see copy to points, leans all these guys on their sides exactly like we'd expect.  So this is something that I think every beginner to who do you need gets tripped up on if they don't already know how this stuff works under the hood.  Who do you need needs an orthogonal matrix and to describe any kind of orientation or transform in space.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_027.jpg

### Transform [81:09]
**Transcript:** And the way that it's going to compute that is by doing this math under the hood.  So it's going to you're if you're not explicitly telling it what up is it's going to take its best guess.  Let's keep going then now let's talk about accumulating matrices.  Just again, head all the way over to the left here and then I've got this quick little example written up here.  Let's zoom in here. So what I'm doing is I have an axis at an angle.  And I'm doing this twice. So I'm I'm defining a 90 degree rotation around the y axis.  And then I'm defining a 90 degree rotation around the z axis.  And I'm I'm applying both rotations to this matrix.  And then multiplying.  And then I'm in position against matrix a and then matrix B.  So you can see that this thing is rotating around why first here. I'll just disable this temporarily.  So it's if I don't do either of these rotations.  It's facing this way by default like we'd expect.  So here's that 90 degree rotation that we're applying.  And now here's the 90 rotation against around the z axis.  And then I'm changing the order of operations.  So the you'll you'll have to supply to oiler rotations as well.  If you like to define things there that this is why you have to say what rotate order you want.  If you're defining things in terms of like, you know, I want to rotate 30 degrees around x 60 degrees around y 90 degrees around z.  That's why rotate order matters.  So the order in which you multiply transforms together is going to determine the order of operations.  It's going to determine the it's going to affect the result of what you're kicking out.  As your your final transform.  So now, rather than just multiply P against matrices.  Let's start actually messing with packed transforms.  Because this is when you're doing this,  copy to points and things like that.  A lot of the time when you're defining these transforms,  your multiple your your sort of tweaking the intrinsic under the hood.  This is what mobs does as well.  So I'm going to start with this pig head and then just run mobs convert.  And this is just a fancy you can do this with a pack stop.  It doesn't matter.  Mobs convert is just convenient.  And then I'm going to use a transfer modifier to rotate this guy.  Four to five degrees around x.  And I still have my spreadsheet open showing.  The packed intrinsic, but you can see that.  It's all the vectors have been slightly adjusted.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_028.jpg

### Product [83:54]
**Transcript:** So this rotation has been applied.  What I want to do with this is sort of explain the difference between a local space rotation and a world space rotation.  Because it's it's simpler than it sounds.  But this is going to be a lot of X.  So apologies.  What I'm going to do first is I'm going to grab the intrinsic transform that we've just.  So the transform intrinsic, at least the one that we're going to be dealing with is a matrix three.  So it's describing orientation and scale.  The translate part of this is just defined by at P, right?  Because we with packed primitives, the position we still have accessible to us is a factor attribute.  You can get the full four by four matrix if you want to, but we'll worry about that later.  So matrix three a is equal to print intrinsic is the function of the matrix.  So matrix three a is equal to print intrinsic is the function that you use to fetch intrinsic attributes about something.  And I'm going to grab them from input zero because I'm just working on myself.  And then the name of the intrinsic is transform.  I'm going to fetch it from the current point number.  Primitive intrinsics are fetching attributes from primitives and not points.  But because the sort order here because my point sort order is the same as my primitive sort order.  I don't have to differentiate between the two, which is why I'm using PT now.  So now what I'm going to do is make a second matrix and this is going to describe the rotation that I want to apply to this pack primitive.  So we'll start from an identity matrix.  I'm going to call this B.  And it's just identity.  So currently it's applying no transformation at all.  I'm going to define an angle.  So we're going to say float angle equals radians.  And then let's make a parameter for this so we can kind of play around with it.  So I'm going to make a float channel called angle.  We're going to specify it in degrees.  And then we're converting it to radians for the purposes of rotation.  Most vex functions in in Houdini, unlike H script expressions, most vex functions want radians.  H script if you're using like sign functions or anything like that, they usually want you to talk about things in terms of degrees.  But vex wants you to approach this like a mathematician. They want you to use radians.  So keep that in mind.  You have degrees and radians functions to do those conversions for you.  Because most artists are going to want if you're if you were parameterizing something like this and presenting it to an artist, they're going to want to think in degrees.  They're not going to want to think in radians.  But just keep these conversion functions in mind.  They'll come in handy.  The other thing to describe a rotation that I need is also an axis.  So I'm going to say vector axis is equal to this is by this to we want to rotate around world up.  So we're going to rotate around the y axis.  And now I'm going to apply that rotation. So rotate B by that angle around that axis.  So now I have my original transform and I have my B transform.  To make this easier to look at, I'm going to create a third matrix.  And this is going to be our output.  So I'm going to say matrix three out and then just not define it as anything because we're just we're going to write to it in a second.  What I'm going to do is make a toggle on here that says whether I want to rotate in world space or not.  So yeah, again, part of all the Vex, but I'm going to say is if.  And I'm making an integer channel here called world space.  This is effectively a toggle. So if this toggle I'm about to make is one and not zero.  Then I'm going to rotate in world space.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_029.jpg

### Product Order [87:27]
**Transcript:** So out is going to be equal to a times B.  Otherwise out is going to be equal to V times a.  Okay. And now now that I have this output matrix, I need to write it back to the intrinsic.  So set for him and trincic is the function to use for this.  I'm going to say zero.  Transform again is the name of the intrinsic.  I want to write to the current transform intrinsic associated with this point number.  And then the value that I'm writing is out this matrix that I just made.  And then hit this button to define these terms.  So I've got this world space toggle here at the bottom.  And then I've got this angle.  And let's say I'm just going to type an expression in here and just say frame times five.  So every frame, it's going to rotate five degrees.  So with world space off.  I'm rotating when I'm when I'm building up this matrix, I'm starting with matrix a,  which is the current transform.  And then I'm multiplying it against the rotation around Y.  So this axis here, that rotation is happening after the fact.  So if I scrub the timeline, you can see that he's not rotating around Y.  He's rotating around what he thinks is why.  So if you look at the picket here in the right camera, he's rotating around his own local idea of what Y is.  But if I enable world space here, so if I just set this to some non zero value.  And now it's b times a, it's I'm applying that rotation around Y first.  And then I'm applying the local rotation or the local angle that he already had that sort of tilt in 45 degrees that we described earlier.  And now you can see that he's rotating around world Y.  So when you're thinking about something happening in local space versus world space,  it's really just about the order of operations.  It's about like are we considering his own local rotation first and then applying this new matrix because that, you know, when you,  when you multiply matrices together, you accumulate all of these different transforms.  All you got to do is it's decide, well, what I do, I want to do this rotation first or do I want to do it second.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_030.jpg

### Part 1 - Scaling [89:49]
**Transcript:** That's it.  So it's it.  It took me a while to wrap my head around it until I sort of had to break it all down and spell it out for myself while,  I'm doing these operations and mobs, but it's literally just, is it a times b or b times a what order of operations you apply in this rotation and.  And let's go to one of their example here. So here I've got basically the same setup.  And what I'm doing on this one instead is in the transform modifier I'm rotating around the X axis over time.  And what I want to do is just sort of show how scaling works with this as well because the rules are very similar.  So I'm actually just going to copy.  Most of this code because it's primarily the same.  But what I'm going to do for these matrices is going to be a little bit different. So instead of defining an angle and access.  I'm just going to say vector scale is equal to, and I'm just going to make an arbitrary scale vector.  And then I'm going to do this b matrix, which again is just the identity right now.  I'm going to scale be by this scale vector that just put out everything else is the same because with matrices the magic of them is that you just define what these numbers are and multiply them together and then you accumulate transforms.  So when I click this, I'm just going to set my scale to one to start.  And then view this. And so right now it's just doing what it was doing before.  Let's say I want to scale along why.  So on the first frame where I have no rotation applied, you can see that he's stretched out vertically.  That's probably easier to see here as well. And if I just spin the timeline, you can see that he's stretched out consistently along his own local conception of what why is.  And then we're reverse the terms of the matrix. Now he's going to start stretching.  It might be hard to see in the screen, but you can see he's no longer stretching along his own access. He's kind of morphing as he rotates because we again we've reversed the terms.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_031.jpg

### Part 2 - Scaling [91:46]
**Transcript:** We're applying the scale first and then we're rotating instead or we're applying the rotation first and then scaling.  And that affects the outcome pretty considerably.  And the next thing that I want to talk about, and this is where things are going to get a little hairy is we're going to start introducing four by four matrices here and start talking about translation.  So we've been dealing so far with very simple, comparatively simple three by three matrices, we just describe scale and rotation.  In computer graphics, regardless of the interface that who Dini gives us to describe these things in computer graphics, you're generally dealing with a four by four matrix, which describes translate rotate and scale. So we're adding position into the mix.  The reason why it's four by four is essentially just for a lazy math shortcut.  And if you remember, we had three vectors that were describing our position orientation just x equals x, y equals y is equals z.  Now we want to offset that. And so we need a fourth vector. But if we just layered that on, we would have a three by four matrix, and it's really annoying to multiply matrices together when they're lopsided.  And then we just have this extra three set of numbers at the bottom here. And then a bunch of zeros that are added on and these are for most purposes, you can just completely ignore them. They're just there to take up space, but they make the math a lot easier to handle under the hood.  And one other thing I should mention is that the order in which you're seeing these values here is what we call row centric. So row one here is our x vector wrote two is our y vector row three is our z vector and row four is translate.  A lot of the time if you're looking at math online or in math textbooks or in other software, you'll see wrote you'll see matrices determine described as column centric.  And it's basically taking all of this stuff and rotating at 90 degrees. So x would be and said this first column, why would be the second columns you would be the third column and translate would be the last column.  This can get confusing if you're trying to translate this to directly what you're seeing in new dini. It's really just a convention of like how you want to store the data and there's some like memory management angles to it, like when you're building up a raise of vectors, sometimes it's easier to read them one way, sometimes it's easier to read them another way.  Just keep in mind that who Dini is row centric and a lot of other matrix descriptions, like if you're looking at any of these operations in Wikipedia, the probably going to describe them as column centric.  It's a big point of confusion, just be aware of it because you'll have to sort of transcribe this in your head when you're when you're doing all this stuff.  Anyways, a four by four matrix largely works the same way if I stretch out one here, he's going to scale along Z, same with Y, same with Z. And if I scale along multiple vectors, he'll start skewing.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_032.jpg

### Multiplying Matrices [95:07]
**Transcript:** These numbers down here are the translate, so I can move him along x here, I can move him along Z here, and I have like this quick expression to just make him move up and down and why using a sign function.  So real simple here we're not doing anything magic yet.  Now let's start multiplying these matrices together.  So what I'm doing here is I'm again starting with this CH4 is sort of the shortcut for describing a matrix as a single parameter, so that's where this is coming from.  And then I'm making a B matrix again starting for my identity, and then I want to rotate it, so I have an axis at an angle here, I'm rotating 90 degrees around Z, and then I'm going to rotate this object around that axis.  So here's my start point.  Looking like this, Simon.  And here's my end point, if I switch these to, I'm sorry, before switching the terms, I want you to just look at this and scrub the timeline a bit.  And you can see that because I'm starting with a as my matrix order, when I move this guy back and forth, even though I'm translating along why the translation is applying to local why because I'm multiplying a first and then B, so you can see that really he's moving along the x axis, but it's his own local idea of what why is.  If I switch these terms, so I'm doing B times a now he's going to be facing sideways the same way it was before, but the translation is going to be applied in world space it's going to be applied in the terms of the second matrix or this first matrix here rather than the second.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_033.jpg

### Invert [96:37]
**Transcript:** So rules for four by four matrix matrices are largely the same we just adding some extra numbers to it.  Another thing that you can do with matrix is matrices that's really fun that you should know about is you can invert them and inverting a matrix is basically an undo.  So I've created I have this quick matrix parameter that's already sort of built for you.  And all I'm going to do is just say I accidentally deleted this parameter, but I'm going to say matrix a is equal to CH4 matrix.  That's going to bind directly to this parameter that we just defined.  And then I'm going to multiply P.  By this matrix a that we just defined.  And then the last thing that I want to do is I want to save this transform as it is to an attribute that we can read later.  And a four by four transform as opposed to a three by three is in Vex if you want to bind that to a parameter use the prefix for so four at X form is equal to a.  So if I look at my spreadsheet under points here, you can see I've got this very long X form attribute that's 16 digits four by four matrix.  So I've bound this to something and because I'm multiplying P by by a here is the same as any other matrix and I multiply these together, pick us up, pick us down.  Now let's undo this.  So what I can do this is the reason why I saved this attribute to or I saved this matrix to an attribute is in the second wrangle down here.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_034.jpg

### Q&A: Position/Direction [98:19]
**Transcript:** I can I can undo this operation. So I'm going to say multiply P again by invert.  And the name of that X form attributes that I just saved out.  And when I apply this transformation and scrub the timeline, nothing happens.  Sorry, Tom, does the ident for an M4 have a one and the lower right.  Yeah, the one and the lower right is.  Yeah, it's, I think it depends on exactly the coordinate system and feel free to correct me if I'm wrong here, but it largely doesn't matter a lot of the time so that you have like a, you know, a true orthogonal like determinant on a matrix or whatever, like you'll see a one down there.  It in most situations, it doesn't seem to matter that much.  But a lot of the time when you, when you see these set up like in other 3D software, there'll be a one down there.  But for what we're doing, it's not really going to affect anything. Does that help?  So it's possible you wouldn't normally describe direction using a 4x4 matrix like this, although it is embedded in there.  But yeah, I think in most CG software that I've seen you would typically end up having a one down there just to keep this orthogonal, but for what we're doing, you can see that if I said it to 0 or 1, it really doesn't.  It's not affecting the result considerably for what we're doing, you know, I can change it and it's not doing anything.  I think for safety reasons, it would usually be one, but I would defer to divvies class because he knows messages matrices like the underlying concepts of it from math terms, he probably doesn't better than me.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_035.jpg

### Invert (Continued) [100:02]
**Transcript:** Okay, yeah.  So going back to this, the nice thing about inverting matrices, not only is it an undue step, but what it allows us to do is we don't have to invert our own matrix.  We can invert someone else's matrix and apply it to ourselves. And what that allows us to do is if we multiply a matrix, our matrix by the inverse of someone else's, we can sort of move ourself into their space, which is really useful for things like parenting operations, where we want something to move as if it's tied to something else.  Having that inverse matrix there sort of gives us the power to space switch, it gives us the power to move into other more useful coordinate spaces, do an operation there and then potentially move back if we need to.  Before we go further into that, what I want to do is describe how pivots work.  There is a pivot intrinsic impact primitives and it largely means the same thing is what we're about to deal with here.  So what I've got is a pig head.  And I'm just moving it five units away from the origin.  Right.  And now what I want to do is, let's just do the same rotation that we've always been doing to these things. So I'm going to create a matrix.  Make it a full matrix instead of a matrix three.  Identity matrix.  I am going to describe an angle. So let's just say radians.  We're going to take the sign of time and rotate. So we're going to rotate back and forth by 45 degrees. That's all. So 45 negative 45 just bouncing back and forth.  And we're going to rotate around the x axis.  So again, to apply this rotation to the matrix that we've just built we use the rotate function. So I'm going to rotate m by angle around axis.  And then as last step, I'm going to multiply p by this new matrix that I built.  And when I run this rotation, you can see that he's not rotating locally. He's rotating still around the origin.  Because when we do these matrix operations, unless we say otherwise, everything is based around the origin, which is effectively our parent space.  So we're not just getting rotation now. We're getting, you know, these points are translating.  And then we're just rotating around the local origin.  It's a very different move than if we let's go up here to this peak and then move this transform back.  And you can see that he's translating back to the origin.  This is effectively how pivot work.  If, and if we want this pig head to rotate around his own local origin, even though he's off here out in space somewhere, we need to make that adjustment.  And then we're going to make that pivot adjust what we're going to do is we're going to make that adjustment before we do this matrix operation.  In order to make sure that he rotates around his own local space.  So I'm going to go back here to pivot intro here and just copy these first three lines. So we don't have to type that again.  So the rotation is going to be the same. We're going to bounce back and forth, but 45 degrees.  What I want to do next is I want to find the pivot point that I want to rotate around.  And in this case, I want this to basically be rotating around the center point of the pig.  I could just take the value of this translation and say like, oh, I want the pivot to be zero zero five.  But let's say that that's not an easy thing to fetch. And I don't want to bother pulling in that data.  I'm going to define the pivot as being the centroid of the bounding box at this pig.  And there's a very handy vex function for that called get the box center.  And I'm just getting the center of whatever stop geometry is in input zero.  So now I have a vector that's pointing to the exact centroid of this pig head.  And now again, I'm going to take this matrix that I built, say rotate M by angle around axis.  And now before I multiply P by this matrix that I've built, what I need to do is offset everything so that the pivot point is at the origin.  To do that, I'm going to subtract vectors. So I'm going to say P minus equals pivot.  So I'm subtracting five, which is that that pivot point from every point here, which is effectively going to move that pig head so that its centroid is at the origin.  Then I'm going to multiply all points by that matrix to apply that rotation.  And then after that, I'm going to add that pivot vector back in world space.  So that he goes back to his original position. And when I do that and then scrub, you can see that now he's rotating around his own local centroid.  And this is basically how pivots are working under the hood. It's like giving you an offset so that you you move that pivot point to the origin because matrix operations like this are always going to have relative to whatever your parent spaces, right.  And in soaps, you don't really have parent child relationships unless you're using kin effects like everything is just sort of happening in the space of the object container that you're in.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_036.jpg

### Q&A: Apply to RBDs [105:33]
**Transcript:** So if you want to be doing these kinds of pivoting operations on soaps where you don't explicitly have parent spaces to find.  You need to make these changes yourself before you're multiplying anything against a matrix.  Yes, but the the slippery part about RBDs is that a the pivot changes over time with every collision, it will update the pivot point.  And a lot of the time on the first frame of simulation, it's going to shift the pivots of every RBD chunk to match the center of mass, unless you disable that in the solver.  And so that can kind of complicate things. And it's also it's it's depending on how you're reading the data from the RBD sim.  So you could either be using the pivot intrinsic if you're like fetching packed packed primitive data like you just basically pass through packed primitive data it's going to write all of this to the pivot intrinsic.  And that pivot intrinsic the offset that it describes is in local space.  So the pivot that it's giving you and if you want code examples of what this looks like crack open mobs pivot because mobs pivot is doing all of the same math under the hood to figure out pivot orientations.  But if you describe a pivot point in as an intrinsic for Houdini of like 1 0 0 so you're like offset by 1 and X that offset by 1 and X is in the local space of the transform not in world space.  So you have to you know if you're trying to shove shuffle the pivot around in world space, you need to first take that pivot vector and multiply it by I think the inverse transform matrix of the of the packed primitive.  Then do your changes and then probably multiply back by the by the the original matrix again before you reassign that intrinsic.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_037.jpg

### Introduction [107:47]
**Transcript:** It's very confusing.  I couldn't even quote the math for you off the top of my head because it is really confusing to do I would mobs pivot has all of that stuff built in and you can kind of see how it differentiates all those operations based on whether it's solving it in local or world space.  Okay, so I'm going to do one more really hideous example here and I apply for apologize for all the facts, but I kind of want to describe how you would define a parenting like relationship just using sob geometry.  So this is something that can affect sort of does for you.  It's still can be confusing to work with because kin effects is still a little new.  But what I'm going to do is basically describe forward kinematics to you.  Forward kinematics is a series of transforms and this also applies to like a typical object hierarchy like if you're in OBG a context in Houdini or if you're looking at something like the outliner in Maya or whatever the equivalent is in C4D where you have like a tree view where objects have parents and children.  The concept behind it is essentially accumulating transformation matrices over time from parent to child.  So it's going to be a lot of X and I apologize for it.  But bear with me here and feel free to ask questions if you get stuck just just chime in.  So what I'm going to do is I'm first essentially I'm going to take all five of these pig heads in a row.  And I'm going to treat I'm going to rotate each one of them locally by about five degrees.  But I'm going to treat it as if this is a joint chain where this pig head all the way on the left is the root.  And then each one of those is parented to the one before it and they're all going to get rotated by about five degrees.  So what we should end up with is sort of like a bendy line like if you can imagine selecting all the joints and a chain and rotating them all at the same time.  We'll start curling up.  That's what we're going to do here.  So float angle is equal to again, I'm just going to build an angle access matrix.  But I'm going to do this based on a parameter. So I'm going to define an angle and degrees as an argument and then I'm going to define an access and I'll just.  I'll do both of these as parameters just to start. And if I click this button, I get them both at the bottom here.  And just to have this ready to go.  I'm going to have these guys rotating around X so that they're all bending perpendicular to the way that they're facing.  And let's just have this the.  Let's have the angle be the sign of the current frame.  And let's speed it up. So we're going to multiply frame by four so we get a faster movement.  And then I'm going to.  How about instead of five degrees? Let's rotate a little bit further. Let's set it to 20.  So the frame multiplied by four is going to increase the speed, which is sign wave of value it. So we'll get a faster bounce and then I'm multiplying by 20.  So we're going to rotate each joint by a minimum maximum of 20 degrees.  And everything's going to be rotated around around the X axis.  Okay, so let's give you one second here.  Let's start coding.  So what I'm going to what you need to do in order to get a parent child relationship to work is for each object in this chain and notice that this is a detailed wrangle.  So we're not iterating over points at a point wrangle for this.  And the reason we're doing that is because order matters.  When you're doing a parent child relationship, we want to solve the parents transform first and then carry that transformation matrix over and over and over again down the chain.  That is how forward kinematics works. So you can't solve it in parallel the way that you'd like to where you're like evaluating all points at once.  So you need to solve the root first and then carry that transform down the chain.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_038.jpg

### Matrices [111:31]
**Transcript:** And what that means is that we're going to solve this as a detail wrangle and we're going to use a for loop to iterate over every point in order.  Because if you if you use this as a point wrangle point wrangles run the threaded they run in parallel, you have no control over who goes first.  So just trying to explain why that's happening there.  So before we go into that loop, what we want to do is define sort of our starting transform like who's who's first.  And to get that transform.  I'm going to say matrix parent X form.  You can call this whatever you want. This is a local variable that I'm calling it this visibility.  And rather than grab the.  There is an intrinsic if you.  And the list here for packed primitives.  Sorry, I have to go back to copy the points here.  Aside from just the transform intrinsic, which is a three by three, there's another one called packed full transform.  And this is the full four by four transform matrix for each one of these things.  You can. Invex fetch that by using the same prevent intrinsic function we used before, but there's a shortcut that they added sometime around new 18 maybe called get packed transform.  And I'm getting it from input input zero.  So my the first input here and the number, the primitive number I want to get is the first pig. So point zero.  So now I have the transform matrix of this first pig stored as this value.  And the second thing that I want to do is get the pivot point right because there's a there's a parent child relationship here.  And I want each of these things to be rotating.  Relative to its parents.  It's going to be rotating locally, but I, but the transformation it needs to be happening from its parent position.  So I'm going to store the current pivot.  Or the initial pivot as the position.  Of that point, I could also if I wanted to I could pull P out of this transform matrix if I had to instead of fetching it as a separate attribute, but it's just way more convenient to get it as P.  And I'll explain more about how to sort of pull information out of matrices in a second.  So now what I need to do is I need to iterate over each point in order in order to get its transform matrix and apply this rotation.  So to do that, I'm going to use a for loop.  I'm going to start into I sort of are are if you're not used to for loops in vex.  I as our iterator what we're doing, we're saying let's start.  Let's start counting and we're just going to write to this variable I.  In our counter, we're going to start with it at zero.  And we're going to end when we reach, if we say I is less than the number of points, we're saying just like end when we hit the last point, like stop the loop.  And then this third term just means I goes up by one for each count. So all we're doing is just we're counting over and over again and in the value of I is increasing from zero to four, essentially, but which is the index of the last point.  So now we're going to do is build this transform.  This is the transform that we're going to be applying locally to each one of these pickheads. So matrix B is equal to the identity matrix.  And let's rotate it. So we're going to rotate B by angle around axis.  And again, that's going to be this sort of sine wave function that we've generated down here.  And now what I want to do is so again, I have the parent transform set up here, but I need to get my own transform and that we're going to call that matrix a right. So I'm going to say matrix a is equal to get packed transform.  Again, I'm pulling from index zero into the wrangle and the primitive number that I want to get this case, because I'm talking about like my own transform for each point is going to be number I.  And getting packed transform I in the loop. So the current matrix that's being affected.  Now, in order to get this transform to be, because I want each of these pickheads to be rotating around, like basically the transformation needs to be applied to itself relative to the parent transform.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_039.jpg

### Part 1 - Parenting [115:45]
**Transcript:** And if I rotate pig this first pig here, I want the rest of them all the children to also follow that rotation. And then I want the the the pig head itself to rotate by itself.  That additional 20 degrees and that's what's going to get us that curling action, like that's what's going to get us the FK part of this rather than a pig head just rotating individually.  So what I need to do is I need to take this pig head and I need to move it.  I need to say essentially it subtract the pivot position of whatever the current pivot is defined to be.  From my current matrices position.  Because we're dealing with matrices and not the pH are here.  This is a little bit more complicated, but Vex gives you functions to handle this. One function that you can use is translate.  And then you can basically just pass it a matrix. And then say, you know, I want to translate it by the opposite of the pivot, you know, because we were subtracting.  So I could just say translate a negative pivot. But translate is going to apply in the wrong space.  What it's going to do is it's going to consider all of the rotations that are applied to this matrix when we do the translation.  And we want to do that we, you know, we're fetching our pivots our point positions in world space.  So this is sort of a weird distinction. But in order to get this translation to happen without the rest of the matrix interfering in that direction.  What we can do is instead say pre translate, which is a convenience function that Vex gives you to basically say, we want to do this translation before we do any other wild stuff with matrix before we before we consider anything else.  There's also pre rotate and prescale functions. And if you're messing around a lot with kin effects, you'll start seeing those coming in handy a lot, especially pre rotate when you want to define rotations in local space and not end up getting like really weird results.  Anyway, so what this is doing now is it's shifting each pig head back onto the previous pig head if it exists.  Now what we're going to do is determine our output matrix. So matrix out.  And what we're going to do is we want the.  We want each pig head to be rotating basically following any transforms that happened to its parent in its parent space, which is why we did this pivot offset.  And then we want to apply this rotation to itself.  So what we're going to do is multiply by the parent transforms. We're going to apply our parents transformations.  And then we're going to apply our own transformation, so our own pack transform again.  Basically moving it out of its parent space after we've done the original transformation. And then we're going to apply this new rotation to itself.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_040.jpg

### Part 2 - Parenting [118:31]
**Transcript:** So for each pig head, we step back, we apply the parents transformation, we apply our own transformation again, and then we apply the rotation.  It's complicated, but I hope this makes sense. FK honestly is a complicated thing to kind of wrap your head around.  It gets away worse if you're trying to understand IK.  And now what we're going to do is update the pack transform now that we've computed this new one.  And there's another handy shortcut function for that. You don't have to use set premium transit. If you're dealing with four by four matrices, you can just use set packed transform.  And we're going to set zero input. We're going to set pet transform number I, because we're again, we're in this loop.  And we're going to set the value to be out.  And then before we close the loop, each parent transform, you know, we define parent transform at the beginning, and it's transform number zero.  But for each successive point, we want to get the new parent transform. So like we need to update this at the end of the loop so that the next primitive in order is reading the previous transform that we computed because this is accumulating throughout the loop over time.  So I'm going to say parent transform or sorry parent x form is equal to out this new matrix that we just computed.  And similarly with pivot, I'm going to say the new pivot point is equal to the new position that we've just computed of point I.  Now we close the loop.  And if we do this, what you're going to see is you wiggle the time around around is that each of these pig heads are translating in space as if they're being rotated by their parent and then doing their own rotation.  So you get this kind of wiggly snakey behavior as if you were rotating a joint chain.  So and if you were doing the same thing with transforms, it's not just joint joints, it's any kind of parent child relationship, you would get something like this. So again, it's just it's all it's matrices all the way down everything in CG that is in vectors is going to be matrices and it's just a lot of these fancy transform just depends on what order you're doing them in and parent child relationships becomes like another order rather than just world or local space that becomes okay, well, we need to take this this one starting transform from like the great grant.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_041.jpg

### Part 1 - Explanation [120:45]
**Transcript:** All the way at the top of our dependency graph and move all the way down to eat the end of each chain.  And when you do any kind of rotations or anything like that in any computer graphics program, this is what's happening under the hood you're taking all these transforms and just accumulating them top to bottom for every leaf in your tree.  Okay, so really quickly I want to go into quaternions.  I'm not going to go deep into this because quaternions are terrifying wizard math and I don't I don't understand them myself, I don't expect anyone else to.  But I understand enough about them to use them practically and to sort of loosely describe what they're meant to do so.  If you want to visually see what quaternions are, if you feel like you're ready to start looking at four dimensional spaces.  Quaternions are a math trick basically they're using imagine they're using complex numbers, which means that the imaginary number I, if you've heard of that before.  Basically, if if you were to break down like this into a 2D graph, let me just get my annotation tool back here.  And you could say that instead of a 2D graph being x and y, let's say that this axis is real numbers.  So this is one, two, three, with imaginary numbers, you've got a separate axis here, which is i.  And i is a number with extremely weird properties that mathematicians kind of just made up that is basically saying the square root of negative one is i.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_042.jpg

### Part 2 - Explanation [122:30]
**Transcript:** And so like, sorry, i squared is equal to negative one. I think that's how it works. It's very odd.  But what it means is that if you multiply i over and over again by itself, it oscillates between one and negative one.  So when you take like a, you know, and again, we're simplifying this into two dimensions, but when you take real components, so we have like a vector like real numbers out here.  And you multiply them over and over again by i.  And these are complex like polynomial equations. So there's more to it than that. But basically you get this oscillation going up and down in the imaginary number plane.  And if you have real coefficients to those numbers, you basically start getting circles.  So quaternions are doing a lot more than this, but essentially what you're getting is a really cheesy way of being able to take a three dimensional factor, right, that's describing like a rotation.  And break it down in a mathematically convenient way to be able to describe and also interpolate between rotations.  And then you're only using four terms because the quaternion is a four dimensional factor.  It's, it's, it's wizardry. I mean, quaternions are amazing, but I am not qualified at all to understand them.  But you're, it's just a really cool math trick that makes it mathematically fairly simple, at least in computing terms, to describe an orientation as four terms instead of nine.  And as part of that process, it makes it very easy to be able to blend between them. And that blending is what we did earlier with matrices called slurp.  But that's who do you kind of making our lives a little too easy, you cannot actually spherically linearly interpolate matrices, who do is converting them under the hood for you and doing all of the Caternion math and then converting them back. So, um, the reason why it's important to know sort of at least what quaternions can do.  Is because they are everywhere. Any game engine that you've ever used when you're like, you know, making if you've ever tried like programming some simple stuff in unity or whatever, where you're like rotating your little guy around on the screen to like move towards a direction or something.  It's all quaternions under the hood. And the reason for that is because quaternions blend really easily without flipping.  You know, if you've, if you've ever any of you who've been character animators who have set keyframes where you're trying to set oiler rotations and blend between them, you're in the graph editor or whatever trying to adjust like your rotate X 90 and rotate why this and rotate this.  And all of a sudden, like stuff gets locked up and starts jittering and flipping and all of that that's called gimbal lock and it's an intrinsic problem with any animation in 3D because most animators.  Any animator can't really think in quaternions they're thinking and oiler angles because just conceptually quaternions are insane.  But under the hood, all of these engines when you're telling these things like hey slowly aim this thing and this other thing because I want my tank to be shooting in this direction instead of this one, it's going to be quaternions under the hood.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_043.jpg

### VEX [125:43]
**Transcript:** That's what it's working with every time pretty much without fail.  What I'm going to do really quickly is just I'm going to take this peg head and first I'm going to clear all the scarbage off my screen.  And what I'm going to do is just build a little thing that will rotate between two different orientations and blend between them.  So first I'm going to create my initial rotation a so my default orientation. I'm going to call this ROT1 and this I'm going to make this identity, but the identity function returns a matrix.  The identity matrix like we were doing before, but what you can do with a matrix is you can wrap it up as a quaternion using literally the Caternion function.  So in Vex, you can say take this 3 by 3 matrix and there's other overloads to this function, you can define a quaternion out of an angle and an axis as well.  But in this case, we're just I'm taking this matrix and saying, okay, you are now a Caternion.  But it means the same thing a quaternion defines an orientation similarly to what a matrix a 3 by 3 matrix does. So it this is a ROT1 is a no no up quaternion. It means nothing.  The second one ROT2, what I'm going to do is basically, I'm going to run the random functions. I'm just going to say rant and we'll just say seed. I haven't defined seed yet, but we'll get there. And I'm going to, didn't mean to put that first.  And I'm going to normalize this because I want my my quaternion to be a unit quaternion.  The the random function in addition to recruiting random floats, it can also create random quaternions. So this is just going to create a random orientation for us. And then for seed, I'm just going to appear to say float seed equals, we'll just bind that to a channel.  And now our output. So again, I'm going to create another vector for I'll call this out.  And what I'm going to do is use again slurp, which is short for spherical linear interpolation. And it's basically slurping to quaternions creates the it it finds the shortest path between them and blends between them. And we'll do that basically every time that's part of the magic is that it always finds the shortest path between two points on a sphere or, or the way to do it.  So I'm going to slurp from the identity rotation to the random rotation. You want to do so based on a bias channel, which is just going to be a zero to one channel. So let me just hit the button here. And now I've got a random seed which currently does nothing in a bias which currently does nothing.  So it's actually rather than multiply P by a matrix. If you want this quaternion to affect the orientation of some of a vector and P is a vector.  What I can do is say P equals q rotate, q rotate rotates a vector by a quaternion and P is a vector. So it's this is functionally similar to multiplying by a matrix.  I'm going to say rotate out or rotate using out. That's our that's our quaternion and apply this to P.  And if I put the display flag on here, you can see that nothing happens yet. But if I increase the bias, it's going to slowly twist towards whatever this random orientation is that I have.  And if I random randomize the seed here, you can see it's just generating all of these different random orientations.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_044.jpg

### Blending [129:21]
**Transcript:** But when I drag the bias down, it's always finding the shortest path between that default orientation and this new one, no matter what. You know, I can leave it down here and  You can see it just kind of jitters a little bit. But it's always finding the quickest path, the quickest or path between orientations and blending between them.  Let's go to the next example. So one of the other cool things that you can do with  Quaternions is that they can be easily initialized as an axis and an angle.  So let's  Let's just code this again from scratch and we'll take one. So again, I'm going to find an angle.  We did this a lot with our matrices, but this is another nice way to think about Quaternions.  Here's my angle and then I'm going to find an axis. This is going to be  I'm going to give it a normalized axis again anytime you're dealing with rotations or orientations.  You want your stuff to be normalized.  So now I've got an angle and an axis parameter and let's set these to  like a 180 rotation around Z.  And now I'm going to define my initial rotation, which again, let's just say that it's no rotation at all. So  Embrygian identity matrix into a terrain. And then let's say for rotate to I'm going to do this  The same Quaternion function, but you can see that there's a lot of overlords, right?  One of them is giving it a matrix three, which is what we're doing before, but we can also provide it an angle and an axis.  So what I'm going to do is say, let's do the radians. Let's convert our 180 degree angle to radians. That's our angle. And then the axis. We already defined.  And then again, I'm going to say, oh, I'm sorry. You know what? These are not these are vector force. I've got to be careful about that.  And then the out vector for the out Quaternion is again, I'm going to slurp between these two. So one, two and then the bias channel.  And then as I did before, I'm going to curate P by my out Quaternion.  And then let's create that bias channel. And now if I do this, I'm doing a full rotation of 180 degrees around Z and I can cleanly  blend between this. And if I, you know, if I change the axis or whatever, this thing, again, it doesn't care. It will always find the smoothest path between any two orientations that I define. And that's the magic of Quaternions. And in Houdini, you know, because they give you the ability to do this with matrices, you kind of, you might lose some sense of how awesome this is.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_045.jpg

### qConvert [132:00]
**Transcript:** Like this is really one of those powerful things that you like Quaternions are a necessity for doing this kind of work.  Houdini, thankfully, abstracts and mobs also abstracts some of this away from you. So you don't have to think about it as much, but this ability to smoothly blend between interpolations is huge.  So another thing to point out and we kind of alluded this before is that Quaternions and three by three matrices, largely mean very similar things. They both can describe orientations.  So really quickly, let's again, let's, you know, let's just copy this effects that we did here, all this stuff that we got the beginning here, except that we did at the end, and it's in a copy these first five lines. And then let's paste them here.  And so, in this example here, you can see that I've packed this pickhead. So I have a single packed primitive with a packed transform.  And I want to take this Quaternion orientation that I did and apply this to the transform matrix.  So the way that I can do this is by taking this Quaternion and converting it back into a transform matrix. And that's done using a function called q convert. So I'm going to say matrix three because I'm setting the three by three matrix on this on this packed primitive now, M is equal to q convert.  And then the output here. So this is taking our blended interpolation, let's turn this on.  And let's say, you know, we can do again, like a one of the grutation around around Z.  So now I have this new orientation defined. And so what I can do is say set prim and transit the same way I did before to update the packed primitive intrinsic because this pick is packed.  I am setting the transform intrinsic for the current point and I'm setting it to this new matrix three M that I've defined.  And now when I scrub the bias, you can see that it's updating the transform intrinsic of this one packed primitive instead of just multiplying or curotating P.  Let's go over here.  And let's literally just copy, we can just copy this node directly exactly paste it here.  And then we can just delete that stand in and plug it here.  So we're going to do exactly the same operation, right. But in this case, check this out. I'm converting this peghead and I'm using mobs transform to scale it up by double.  If I take this quaternion and I do all this great slurping and everything and then try to apply it back as a matrix.  And then we can just play flag on here. The orientation is right, but the scale is gone.  And the reason for that. And this is why quaternions are used to blend between things, but when you're actually storing data and computing like the final transformers of things, you're still always using matrices.  And that's because quaternions can only describe orientations in any useful way. They can't encode scale the way that a matrix can.  Like we can do all of this great blending, you know, without getting matrices involved because quaternions are so cool, but they can't encode scale.  So what we can do instead, if we, you know, again, in later versions of who do we can just slurp and not think about it, but when you're thinking about this in terms of quaternions.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_046.jpg

### Final Convert [135:19]
**Transcript:** What you can do instead and just we're going all the way to the last example here, we've got our pickhead here, he scaled up and we've got a 30 degree rotation on him.  And what we want to do for this last example is apply like an additional rotation on top of that. So what we're going to do at the beginning here is roughly the same as what we have been doing, right. So I'm going to define an angle, so float angle.  Angle, I'm going to define an axis, normalize CHV axis.  I'm going to describe my two quaternions. So the first rotation, sort of my default rotation is going to be quaternion ident, same as I'm always doing here.  And the second one is going to be  quaternion radians angle and axis.  So I'm just I'm creating an angle axis for quaternion out of these parameters that I've set here. So let's just set the angle again to 180 and let's make this a rotation around Z.  And now I'm going to create my output quaternion, exactly the same as what I was doing before and that output quaternion is going to be slurping between our identity quaternion and then the angle axis quaternion that we're defining and blended by a bias channel, she'll set here, set that to one.  And now what I want to do is I'm going to convert this quaternion into an angle axis vector.  So another way that you can describe a rotation in Houdini that if you've ever looked at the w attribute in a particle simulation or target w.  This is a common place where you'll see things described in angle axis vector notation, rather than describe it as a quaternion, which is really only describing like an orientation, something like w, which in particle simulations and Houdini is the spin.  So it's not just describing orientation is describing angular velocity.  The advantage of using, you know, there's a lot of advantages to using that, but one of them is that you can describe an amount of magnitude as well as just an orientation and you can describe it in just three terms instead of having to use four or nine or whatever.  The way it works is an angle axis vector as opposed to an angle axis quaternion.  The direction of spin or the orientation is defined by the normalized vector.  The amount of spin, basically the amount of force is the magnitude of that vector.  So that's how it's being encoded is the length of the vector describes the amount of rotation or the amount of spin that's happening and the direction of the vector describes your axis that's your axis for rotation.  The way that you can take an orientation like a quaternion and convert it is just by again using q convert, but you give vex the hint that you want to convert this into a vector and rather than a matrix or something else.  So if I say vector will call this ROT is equal to q convert out.  So this is taking our paternion and turning it into an axis angle representation.  Next, I can basically I can extract the angle and the axis from this.  Let's say our axis is equal to normalize ROT.  So this is taking the that angle angle axis vector and stripping out the magnitude part of it.  So this is just getting us the axis of rotation.  And now to get the amount of rotation. So this is the angle part.  I can say float our angle equals the length of that quaternion of that vector that I've converted.  So that's giving me the the angle. So now that I have an axis is an angle, what I can do is take this quaternion that I've blended and apply this to the matrix as a rotation using angle axis notation like we were before.  So again, I have to fetch the original matrix here. So matrix three m equals same pre-mentraise function. We've always used.  I'm going to grab the transform intrinsic from the current point number.  And then I'm going to use rotate to rotate that matrix by our angle and our axis. So these these values that we pulled out of that quaternion using q convert.  And then just use set pre-mentraise to set these values right back. So I'm going to set the transform intrinsic with the current point number.  I'm going to set it to this new matrix m that I've just output.  So the whole is that I get this, you know, again, just to drive the some I get this scaled pig head rotated and he maintains a scale.  And this is not something that you'd be able to do if you were just directly converting the quaternion to a matrix, so you would lose that ability.  So this is another way of looking at this of taking that information that you got from blending these quaternions together.  And encoding them in a way in which you can use them in practice. Now that we have the ability to directly slurp matrices and who denies is less necessary to do.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_047.jpg

### Data Grouping [140:41]
**Transcript:** But I still think it's an important thing to have in your back pocket and also just help differentiate between what can quaternions are good at versus what matrices are good at.  Does anyone have any questions about this before we wrap this up is there's one more quick section I want to go into.  Okay, I know quaternions are really confusing. I recommend not trying that hard to understand them.  Because they're unless you're unless you're really good with imaginary numbers, they're just really difficult to wrap your head around.  But worthwhile, I included a link in the in the comments and the stickers for the quaternion section to a three blue one brown video that also has a link to an interactive app that tries its best to explain to you what the stuff actually means.  It's still hard, but from a practical standpoint, as long as you know how to use these effects functions and you know the difference between quaternions and matrices, you can still use them just fine and not think too hard about it.  So the last section that I want to go into is called extraction and this is just a quick thing to sort of explain.  You know, once you've got all these numbers encoded in a matrix, how do you get something intuitive back out of them again because sometimes you don't want to matrix sometimes you want vectors or you want to be able to see this a different way.  So in this first example, I've got the usual pickheads copy to points, whatever.  Let's say that from these I want to get an up vectors right like I want to know what n is or what up is for each of these points and I don't have that now with mobs you just use mobs extract attributes that's what it's for, but if you want to do this manually for some reason.  This is how it would work so in Vex i'm first going to grab the matrix like I have been so i'm just going to run prim and transit to get that three by three transform matrix.  And in order to basically extract and are up from this is a couple of different ways that you can do it, but the way that I like to think about it is basically applying the transformation of our matrix to world n or world up.  And the way that you do that is through multiplying like with anything else, so I can just say v at n so i'm going to bind this to the n attribute is equal to the matrix times world n so world n you normally think of as our forward direction is positive z.  And then up is just equal the same matrix times zero one zero world up if I do that and then look at the spreadsheet you can see that n is equal to positive y which is correct all these pigs are facing positive y and the top of the pigs head is pointing towards negative z so it's extracting the right values and we can use this end and up to do you know velocity.  Set ups for a sim or if we just need to like orient other things to match this like now we have that data ready to go.  Just to go to the next concept here again i've got this these pig heads copied the points and i'm running them through a mobs randomized to just scramble the orientation of these points.  You can use this basically the same exact technique with quaternions if you have an orient attribute and you do extract vectors from it.  So in this case i'm going to say v at n is equal to.  You rotate again because we're going to rotate a vector by this orientation quaternion.  E dot at orient is the orient attribute that mobs randomized generates when it randomizes the orientation of these things and then i'm going to rotate again world z by the same vector and i'm going to do the same thing for ups of puritate orient by world y.  And if I do this you can see that i'm getting n and up vectors that are all sort of you know various random scrambled.  Vector is here but if I turn on normal visualization i might have to delete the points here to be able to see them.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_048.jpg

### Data Pulling [144:42]
**Transcript:** Yeah there we go and i'll template this but you can see that for each of these pig heads that normal vector is facing right down the nose.  So this is how you can kind of pull different kinds of data from matrices or quaternions there's one last function that I want to go into that's a little bit more complicated.  But there's a way to get even more data out of a matrix, especially something like scale or if you carry about the translate section of a matrix or if you care about.  For example the.  The oil or angles that you would rotate something to using a transform software mobs transform that would get you to a given orientation you can extract that i can't really explain you.  To you how to do that from a math standpoint but who didn't gives us a very great convenience function to handle that called crack transform it's a little unwieldy to use but i'll try to explain it here I recommend looking at the docs for it because it's a bit weird.  So again I am copying stuff to points here so i've got this usual grid of pickheads and i'm randomizing position orientation and uniform scale for all these pickheads and what I want to do is take this information and extract it and right now i'm kind of cheating mobs is doing this for you it's like generating all these attributes i'm just going to just to clean this up i'm going to attribute delete and just remove orient.  So i'm going to pull these out of ourselves so in this wrangle what i want to do first is because i'm extracting position as well just for kicks i want to get the full matrix so i'm going to say matrix m is equal to get packed transform which is our shortcut for just getting the full transform and transic from whatever this this operator is so.  Now crack transform is one of those magical vex functions that if you look at the docs for it, it has a lot of overloads that have that ampersand for parameters right so it will write to multiple arguments that you pass into it so.  What I need to do is i'm going to be getting my translate rotate and scale back as vectors and so i'm going to create temporary variables to how is each of these things i'm going to say vector translate.  dr rotate.  dr scale so these are just three temporary holders for where these values are going to go when I run crack transform the other thing that I want to do is to find a pivot for each of these but the pivot for for each one of these things the way that we have it set up right now is effectively the center point.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_049.jpg

### Crack Transform [147:27]
**Transcript:** For each of these packed primitives so for the purposes of cracks transform I can just say that the pivot is zero because it wants to pivot in the local space of each of these objects and the pivot is at the centroid if you looked at the pivot intrinsic for each of these it would be zero so we don't need to change this at all.  And now the last thing is to actually run this gigantic function so crack transform.  You need to read the docs to understand how this function works because it's very strange but essentially what it's looking for is.  It wants to know the first argument is what order you want to handle translate rotate and scale it zero is sort of the default what you what you would expect and there's other ones that you can look at but you you you you have to look at the docs to know what these numbers correlate to so like.  One I think it's like rotate translate scale or something it's it's all very strange but you you have to get this from the docs I'm going to stick with zero for this that's like the default expected the second argument is the the.  The.  The rotate order and again it's something that you have to look at the docs to get but what it's going to the rotate order is essential for extracting rotations because if you want to get the.  The rotation written out as oilers so as angles like along this much along x this much along the y this much along z the order matters because as all of you know if you've ever rigged anything like if you have a wrist that is.  That where the rotate order is like x y z and like you move the wrist 90 degrees and like try to rotate down and all the sudden gimbal locks starts flipping like that order matters and has to be like very specific for any given joint or rotation zero is the default x y z that most transforms start with but default but there are reasons why you might want a different rotate order so keep that in mind when you run this.  Then it wants to pivot which is zero but we're just going to put it in here to make it more readable then we pass it the matrix and then the last three terms are essentially the outputs so we're going to write the translates to translate the rotates to rotate and the scales to scale.  So now what I'm going to do is take these translate rotate scale values that I generated and just bind them to variables so that we can see them in the spreadsheet so I'm going to say at.  I like to when I'm making temporary variables that are just for debugging ideas a lot with hda's especially I prefix them with two underscores to imply that it's temporary so I'm going to say at underscore underscore p is equal to translate and the reason I do it this way is because if I'm wrapping up an hda or some other tool.  And I'm making a lot of like debugging variables so that can kind of see what's happening in the spreadsheet as data flows through it.  Later on what I can do is drop down an attribute delete and just say underscore underscore star and that means it just automatically wipes out all temporary variables and I don't have to worry about individually identifying them it's just a handy trick when you're building.  When you're building tools in general.  And then r is equal to rotate and as is equal to scale and when I run this you can see that the under the temporary p value here matches my actual p exactly the same number.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_050.jpg

### Thoughts [150:47]
**Transcript:** And then what you're getting here for are you're getting values between negative 360 and 360 because these are oil or rotations in XYZ so you could take these values and apply these the same with a transform stop the same way if you had your pivots configured correctly and it would match.  And then my scale vectors are here as the as the last one.  So that's everything.  That is that is as far as I was planning on going with this math lesson just and we still have a little bit of time left so if anyone has any extra questions or they want clarification on any of this again mobs is doing a lot of this like these same concepts mobs is doing them under the hood for a lot of different operations.  And then we can add some insight into how some of this looks practically mobs is open source so you can crack open you know mobs transform mobs pivot mobs apply attributes is where is really complicated because it's trying to handle a lot of different cases but it's a good way of kind of seeing like how some of these things conceptually work under the hood.  And then we could take what we had built earlier when we were doing like building our own aim constraint and wrap that up as a as an hta and if you had it reading mobs fall off as a bias it would be roughly the aim modifier that you know you can.  So knowing what how these concepts work under the hood you if you wanted to and I encourage that you do you could build on top of mobs maybe use apply attributes to sort of abstract away some of the worst of this that you know that your fall offs are going to work as they as expected and all that stuff is kind of built in for you.  But these same concepts because so much of mobs is about translate rotate scale and applying those concepts in different ways.  I highly recommend that you crack open the notes just to like learn better how they work and sort of what they're doing and how it relates to all these concepts i'm going over and also to potentially use them to build your own tools.  If you come up with something really cool and you want to push it to the repository I accept pull requests you're you're more than welcome to contribute.  It's it's it's it's an open project i'm hoping that with more people really learning how it works under the hood that we can sort of expand it into a broader tools that are at time.  We're doing a link into the chat there for everyone to.  That file.  Math.H actually has all of those.  The TRS and the xyz information so the zero one two three like the explanation of all of that yeah and the documentation for.  The crack transfer function will link you to that same file path yeah but it's it's it's not something that you're ever going to get without unfortunately without cracking open that file but thankfully most of the time when using crack transform it's zero is all the way down and you just have to remember.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_051.jpg

### Q&A: Make Your Own Packages [153:40]
**Transcript:** What the term the sea term for like what thing you want to extract 90% of the time that's all you need.  Um.  That's a that's a big topic.  So there's a lot of stuff in the Huda doc documentation about building hda's and things like how to.  How to name space things that sort of thing which is extremely important that's the first thing that I would do if you're wrapping up in hda is like name space everything so that you don't get name clashes with other people's operators.  And that's you know when if you were for example if you were to look at my I'm not just sharing my screen let me open this up real quick.  For example, mobs randomize here.  You should all know about in general and who Dini I leave this open all the time is if you go to your assets tab and go to the asset manager.  If you go over here to the configuration tab there's something here called the asset definition tool bar and by default on most to Dini installs it's it's turned off.  What you want to do is set this to show always and what that gets you is this tab up here that gives you the asset name and path so let's you see what O TL is this like what am I what file on disk am I loading this from or is it embedded.  And it also this drop down let's you select versions so if you have multiple versions of an hda that you're developing over time and you want to kind of be able to like step through them.  I do this you know you can see that there's a randomized one I went around a one one when you're developing a toolkit you plan on people on sustaining it people are going to be using it for the long term anytime you make a change to something that significantly could change the parameter layout or what those parameters do.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_052.jpg

### Q&A: Asset Creation [155:39]
**Transcript:** What you do is you bump up that version and labs tools actually have some some stuff to handle asset management for you that I recommend looking into side effects labs.  But if you if you right click on something and go to it in the asset manager and go to the operator list you can see that the Mops randomized HDA has these two definitions built in and I can right click this and duplicate it.  And then give it a new name and so I could just give it and this is where namespace and comes in the operator name I could change this to one to.  But the way that you separate this is with two coldens and so what you would want is sort of your facility name or your you know your name or whatever you're going to develop under the name of the toolkit for me it's Mops.  And then the name of the descriptive name of the operator and then after that a major and minor version number and what this allows you to do is make sure that anytime you a the namespace means that your tools won't clash with other people's which is really important.  But it also means that the major major minor version thing means that anytime you change something where if it's not a bug fix if it's something that's significantly going to impact people's files those definitions remain separate so if somebody has an old HDA that's using version one even if they upgrade the latest version of your tool set.  And those definitions stay where they were until the user explicitly goes back and changes it or drops a new node.  So that's that's number one like definitely pay attention to that when you're building these things out.  As far as like configuring packages and stuff like that really.  And then you can see that the way that the directories are laid out where.  And like you can feel free to use Mops as an example there's a lot of free toolkits out there and some of them are using more old school methods of package management and some of them are more new school.  I try to keep Mops pretty new school because I think packages are sort of the way forward for how this works. But if I go to my projects tab and look inside Mops here.  You can see that there's like there's an icons tab there's an otls tab scripts toolbar all these are standard so when you're building out a package if you want the easiest possible way to get it loaded into into a facility.  You need to put everything in a single directory and then have it broken out by otls all your HDAs go here toolbar your shelves go here.  And then you can either do I think there's something else for like command line scripts like H script and then for Python.  So script slash Python all of your Python libraries going here.  And if you build it this way with this exact directory structure like named 100% the same way when you build it.  All you need to do to get a user to have this in is to make them as to have them add this root path to who do you path.  And then you can do that either through the end or they can do it through a package file.  But that means that it's a it's a one key instead of having to set like who do the otls scan path and the dd toolbar path and all these other like individualized environment variables that are a nightmare to keep together in your head.  If you set up your directory exactly this way everything just works with a single key.  So that's another thing that I would recommend if you're if you're going to build something out like this otherwise it's just.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_053.jpg

### Q&A: HDAs in Unreal [158:56]
**Transcript:** It's hard for me to sort of describe all of the things that I've screwed up over time like.  You know make backups use Git for source control.  Which you know I can't I can't go into that here it's it's kind of annoying to work with but you have to learn it if you're going to develop software.  Make lots of backups.  Another question in the chat here about would an hda that uses mobs nodes work in unreal.  That's a good question and it should assuming that the unreal environment is configured so that it can see those hda so if the environment is the same.  The mobs is all is all vex into the hood so it should work it but the only thing that would make it weird is if your environment weren't configured to work directly like if it was having trouble seeing those hda's for some reason I don't have a ton of experience with Unreal engine.  I've used some mobs nodes in hda wrappers and unity and it seems to work but somebody who's more unreal focused would probably have better answers on that than me.  And Vic is asking about a mobs plus course I am working on new tutorials.  I got kind of thrown for a loop because there's just been a lot of other development in mobs and mobs plus and I got a day job and and everything else.  So video tutorial video production slowed down a little bit but I do have more in the works I need to do there are already a lot if you go to the mobs YouTube page there are a number of tutorials about individual mobs plus nodes that I recommend watching.  I just I haven't completed that series yet there's a lot more ground to cover especially when it comes to mobs stops like the whole physics system that you can use.  The next tutorial I do is going to be mobs stops for sure because that's like a big missing piece but i'm not going to do those here because I don't want to charge people for it that's double dipping you know i'm not going to charge people for for software I'm already charging money for for tutorials so when they come out they'll be on the mobs YouTube channel so I recommend subscribing if you if you want to know when this are coming out right away and I post all this stuff to Twitter all the time so you'll you'll be seeing more that too.  Cool and the other questions.  You're getting lots of love in the chat.  I appreciate it you guys are welcome to speak up if you want to you don't have to use the chat i'm done talking.  You can you can unmute yourself brilliant class.  Thank you appreciate that.  Yeah you're you're take on major season quattriniens was actually very interesting the matrix especially.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_054.jpg

### Closing Statements [161:51]
**Transcript:** Was a nice different perspective that kind of opened up a few more doors in my brain and i'm it my perspective is is pretty uneducated like in terms of formal math training I had to teach all my stuff to myself basically when I started developing mobs i was like oh this is going to be easy you know me and more it's just like skipping on a playground or whatever and ended up being really hard.  That's great to hear yeah I mean and again if you if you run into things that you're used to that are easier elsewhere or there's like specific problems that you're trying to solve that you think would be better generalized as like a broader tool.  Come find me you know you can talk to me on discord you can talk to me on Twitter like i'm constantly you know new tools when I develop them tend to go into mobs plus.  But things that already are tightly integrated with the base system I am still developing it constantly i'm doing bug fixes.  I'll occasionally add new stuff to it where it's stuff that's just like.  Like really obvious extensions of existing tools that need to be there so.  Suggestions are totally welcome.  If you have them and you're not a developer yourself you're not going to about to do pull requests like just reach out and talk to me and that includes both mobs and mobs plus like if you feel like something is missing and either part just come talk to me.  And I mean it feel free to reach out to me if you have questions or any or suggestions or anything like i'm happy to help people with this stuff.  Definitely post to the discord and stuff to if there's questions or even artwork to anything that you build that loves to see everybody's artwork will share it across all social media.  Alright well that's it take it easy everybody.

**Frame:** tutorials\frames\mops-motion-operators-for-houdini-part-3\frame_055.jpg


---

## Structured Notes

### Core Technique
CGI math foundations underlying MOPs: vectors (normalize, dot/cross product, double-cross), trigonometry (sine/cosine/unit-circle/arc-cosine), 3×3 and 4×4 transform matrices (rotation via sin/cos, scale, aim constraint, local vs world-space multiplication order, inversion, pivot math, forward kinematics), and quaternions (slurp blending, qrotate, qconvert, angle-axis, crack-transform extraction). Everything is built from scratch in VEX visualizer files distributed with the course.

### Summary
163m38s masterclass on the linear algebra and trigonometry that powers MOPs (and all of CG). Three example files — `01_vectors`, `02_trig`, `03_transforms` — walk from first principles to practical VEX. Topics: vectors (normalize, dot product, projection, Lambert/Fresnel shading, cross product, right-hand rule, double-cross for geo-normal forces), trig (sine/cosine as triangle ratios and unit-circle coordinates, arc-cosine to measure angle between vectors), 3×3 rotation matrices (sin/cos under the hood, scale via vector length, aim constraint built manually and via `make_transform`/`lookat`, matrix blending via `slurp`, orientation pitfalls with only N and no up, `dihedral` shortcut), local-vs-world matrix multiplication order (`A*B` vs `B*A`), 4×4 matrices (adding translation, row-centric storage), matrix inversion (undo, space-switching), pivot offset math, forward kinematics in a detail-wrangle for-loop, quaternions (`quaternion()`, `slurp`, `qrotate`, `qconvert`, angle-axis/w attribute, gimbal lock), and transform extraction (`crack_transform`, extracting N/up from packed-transform matrix or orient quaternion). Closes with Q&A on HDA packaging, namespace/versioning, MOPs in Unreal.

### Key Steps

**1. Vectors**
- Normalize any vector used for orientation (divide by its own length → unit length)
- `dot(a, b)`: ratio −1 to 1; parallel = 1, perpendicular = 0, opposite = −1; used in Lambert shading (`dot(N, lightDir)`) and Fresnel/facing-ratio (`dot(N, camDir)`)
- Projection: `C = dot(a,b) * b` — projects vector A onto B
- `cross(a, b)`: finds orthogonal vector; normalize inputs; not commutative; right-hand rule; becomes unstable when a ≈ b (aim-constraint up-vector selection matters)
- **Double cross**: `cross(N, up)` → swirl force; `cross(cross(N,up), N)` → surface-following downward force

**2. Trigonometry**
- `sin(theta)` = opposite/hypotenuse; `cos(theta)` = adjacent/hypotenuse
- Unit circle: coordinates of a point on a unit circle are `(cos θ, sin θ)` — full circle = 2π radians
- `acos(dot(normalize(a), normalize(b)))` → angle in radians between two vectors
- `radians()` / `degrees()` functions for conversion (VEX math wants radians)

**3. 3×3 Transform Matrices**
- Identity: X=(1,0,0), Y=(0,1,0), Z=(0,0,1) — "no operation"
- Rotation around X: Y = (0, cos θ, sin θ); Z = (0, −sin θ, cos θ); X unchanged → `matrix3 M = set(X, Y, Z); P *= M;`
- Scale = scale the row-vectors; skew = make them non-orthogonal
- **Aim constraint (manual)**:
  1. `vector aim = normalize(target - P)` — subtraction gives direction
  2. `vector side = normalize(cross(up, aim))` — orthogonal to both
  3. `vector up2 = cross(aim, side)` — re-orthogonalize up
  4. `matrix3 M = set(side, up2, aim)` → bind to `3@transform`
- **Aim shortcuts**: `make_transform(aim, up)` or `lookat(target, P)` — both handle cross products internally
- **Matrix blending**: `slurp(identM, aimM, bias)` — Houdini converts to quaternion internally

**4. Orientation Pitfalls**
- Giving `copy_to_points` only an `N` attribute with no `up` → Houdini guesses up (rotates identity matrix to align Z→N) → unexpected pig-on-back orientation
- `dihedral(oldN, newN)` → matrix that rotates oldN onto newN (wraps cross-product + arc-cosine in one call)
- Copy-to-points reads `3@transform` (3×3 matrix), `4@transform` (4×4), or `v@N` + `v@up` — be explicit

**5. Local vs World Space (Matrix Multiplication Order)**
- `out = A * B` → apply A's local rotation first, then B → rotation happens in A's local space
- `out = B * A` → apply B first, then A → translation/rotation in world space
- This is literally the only difference between "local" and "world" space transforms

**6. 4×4 Matrices**
- Adds translation (4th row): rows are X, Y, Z, translate; extra column is zeros + 1 (math shortcut)
- Row-centric in Houdini; column-centric in math textbooks/Wikipedia — transpose mentally when reading docs
- `4@xform` binds a full matrix in VEX; `CH4("matrix")` reads a matrix parameter
- Invert: `P *= invert(storedMatrix)` — undoes a previous transform; useful for space-switching

**7. Pivot Math**
- Rotation always happens around origin of current space
- Manual pivot: `P -= pivot; P *= M; P += pivot;` — where pivot = `getbbox_center(0)` or known centroid
- `pre_translate(M, -pivot)` — applies translation before other matrix transforms (avoids local-space weirdness)

**8. Forward Kinematics (Detail Wrangle + For Loop)**
- Point wrangles run in parallel — can't chain parent→child; use detail wrangle + `for(int i=0; i<npoints(0); i++)`
- Per iteration: fetch `matrix A = getpackedtransform(0, i)`, build rotation `B = identity → rotate(B, angle, axis)`, compute `out = parentXform * A * B`, then `setpackedtransform(0, i, out)`, update `parentXform = out` and `pivot = point(0,"P",i)`
- `pre_translate(A, -pivot)` before multiply to handle parent-relative pivot

**9. Quaternions**
- `quaternion(matrix3 M)` — convert 3×3 matrix to quaternion
- `quaternion(radians(angle), axis)` — angle-axis constructor
- `slurp(q1, q2, bias)` — spherical linear interpolation (shortest path, no gimbal lock)
- `qrotate(q, P)` — rotate vector P by quaternion (like `P *= M` for matrices)
- `qconvert(q)` → matrix3 (loses scale!) or vector (angle-axis notation)
- **Angle-axis vector** (w attribute): direction = axis of spin, magnitude = spin amount; `vector rot = qconvert(q)` → `vector axis = normalize(rot); float angle = length(rot);` → `rotate(M, angle, axis)` to apply while preserving scale
- Quaternions can't encode scale; use for blending then convert back via angle-axis → rotate the original matrix
- Gimbal lock = Euler rotation order problem; quaternions avoid it internally

**10. Transform Extraction**
- Extract N/up from packed-transform: `v@N = M * {0,0,1}; v@up = M * {0,1,0};` (matrix times world Z/Y)
- From orient quaternion: `v@N = qrotate(v@orient, {0,0,1}); v@up = qrotate(v@orient, {0,1,0});`
- `crack_transform(xformorder, rotorder, pivot, M, translate, rotate, scale)` — writes T/R/S to output args (note ampersand args); `xformorder=0` (TRS), `rotorder=0` (XYZ) for defaults; see VEX docs for other constants in `math.h`

**11. HDA Package Best Practices (Q&A)**
- Namespace: `facility::toolname::major.minor` — prevents clashes, preserves old versions in user files
- Directory structure: single root folder → `otls/`, `toolbar/`, `scripts/python/`, `icons/` → one path in `HOUDINI_PATH` loads everything
- Asset manager: enable "Asset Definition Toolbar" under Configuration → see source OTL path + version dropdown
- MOPs is open-source; pull requests welcome; MOPs+ tutorials on MOPs YouTube channel

### Houdini Nodes / VEX / Settings
- `normalize(v)` — unit vector from any vector
- `dot(a, b)` — scalar similarity ratio
- `cross(a, b)` — orthogonal vector (normalize inputs for orientation use)
- `acos(dot(a,b))` — angle in radians between normalized vectors
- `matrix3 M = set(X,Y,Z)` — build 3×3 from row vectors
- `rotate(M, angle, axis)` — rotate matrix in-place (ampersand arg); `pre_rotate`, `pre_translate`, `pre_scale` for local-space ops
- `make_transform(aim, up)` — build orthogonal 3×3 aim matrix
- `lookat(target, from)` — one-liner aim matrix
- `ident()` — identity matrix (matrix or matrix3 depending on context)
- `slurp(M1, M2, bias)` — blend between two matrices/quaternions
- `dihedral(oldN, newN)` — rotation matrix that aligns one vector to another
- `primintrinsic(0, "transform", @ptnum)` — fetch 3×3 packed transform
- `getpackedtransform(0, @ptnum)` / `setpackedtransform(0, @ptnum, M)` — 4×4 packed transform shortcuts (H18+)
- `quaternion(M3)` / `quaternion(radians(angle), axis)` — create quaternion
- `qrotate(q, v)` — rotate vector by quaternion
- `qconvert(q)` → matrix3 or vector (angle-axis)
- `crack_transform(xformorder, rotorder, pivot, M, &t, &r, &s)` — extract T/R/S from 4×4 matrix
- `getbbox_center(0)` — centroid of input geometry (useful for pivot)
- `4@xform` / `3@transform` — full 4×4 or 3×3 matrix attribute prefixes in VEX

### Difficulty
Intermediate–Advanced

### Houdini Version
H18.5+

### Tags
[mops, math, vectors, matrices, quaternions, vex, linear-algebra, trigonometry, aim-constraint, forward-kinematics, transform, dot-product, cross-product, slurp, quaternion, intermediate-advanced]

---

## Related Tutorials
- mops-motion-operators-for-houdini-part-1.md (MOPs generators, modifiers, falloffs)
- mops-motion-operators-for-houdini-part-2.md (Move Along Spline/Mesh, Delay, Spring, RBD/Vellum integration)
- vops-02---random-noise---houdini-beginner-tutorial.md / vops-03---vector-operations---houdini-beginner-tutorial.md / vops-04---geometry-interactions---houdini-beginner-tutorial.md (VEX/VOPs deep dives)
- noise.md (noise math in Houdini)
