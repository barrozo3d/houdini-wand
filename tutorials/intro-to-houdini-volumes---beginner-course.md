---
title: Intro To Houdini Volumes - Beginner Course
source: YouTube
url: https://www.youtube.com/watch?v=wR0SDptfygg
author: Voxyde VFX
ingested: 2026-06-23
houdini_version: "any modern (H18+)"
tags: [volumes, vdb, sdf, velocity-field, vop, point-cloud, volume-rasterize, beginner-intermediate]
extraction_status: complete
frames_dir: tutorials/frames/intro-to-houdini-volumes---beginner-course/
frame_count: 15
---

# Intro To Houdini Volumes - Beginner Course

**Source:** [YouTube](https://www.youtube.com/watch?v=wR0SDptfygg)
**Author:** Voxyde VFX
**Duration:** 137m16s | 15 section(s)

---

## Raw Data (for Claude Code extraction)


### Volumes & Voxels [0:00]
**Transcript:** Volumes are a gigantic part of Houdini and VFX in general.  I would go as far as to say that around 90% of VFX shots require some use of volumes.  And you might think that we as VFX artists only use volumes when creating explosions or smoke type of effects,  but in reality volumes are used all of the time in many types of effects.  This is why it's crucial to have a good understanding on what volumes are and how to use them.  In Houdini there are many ways to create and manipulate volumes and we'll cover them more  as we progress, but let's start with a simple volume. So I'll drop down a geometry container,  let's step inside and I will drop down a volume node. Now we have a bunch of settings here and  I'll go over this in a second, but in order for us to see the volume we need to give it some  initial value. So in our first component here we can set this value here to 1 and now Houdini will  know how to drop this volume in the viewport. And this is the default behavior as I increase this  value here, we can take an up this volume and as I decrease this we can fade it out. So if this  reaches a value of 0 now we won't be able to see this. This doesn't mean that volume doesn't exist  just that it has a value of 0 so it's invisible in the viewport. Let's set this back to a value of  1 and we can also give this any name we want over here. Typically with smoke type of effects you  will often see the name density like so. So this is what we'll use here again you can give this  whatever name you want. So whenever we are dealing with volumes you have to think in terms of a  3D grid. What happens here is that we have this box and if I go to my wireframe display and press  w we can see that this is the bounding box of our volume and we can imagine cutting up this box  on all of the sides. So let's press w again and I'll just step in Photoshop for a second, I'll make  a print screen of this. So let's say that this is our box and I'll just create a simple outline of  this. So if we slice up this box on all axis like so and we divide this up we are gonna be left with  a bunch of smaller cubes like this and this small cube here is called a voxel. So each voxel is going  to be this small box that contains information and they're all gonna be neatly aligned in this grid  type structure and how many voxels we have in total and how big this entire volume is can be  determined by the settings over here so we can increase the size overall and if we increase the  sampling divisions here we can determine how many divisions we have for each side so if I increase  this we increase the total number of voxels and I'll set this for now this uniform sampling method  here. I'll set this to non-square just so we can better visualize this. Now we have a 10 by 10  grid which gives us a total of 1000 voxels so I can go to the information here and we can see  we have this 10 10 and 10 here in between brackets which specifies the size of our grid and we have  the total number of voxels here to 1000. In fact if I'll just press D on my keyboard and I'll go to  the background settings here and I'll make this background dark so we can visualize this better  and also in the guide step over here I will turn on draw time and also set the geometry  formation to always on which will give us the total number of voxels in the corner of our screen  over here so we can see that we are working with 1000 voxels now and if I increase the divisions  here let's try 100 by 100 and 100 now we have 1 million voxels and we can see how much sharper  the volume got now we have this very crisp outline of our box so there's a direct relationship between  the total voxel count and the quality of the effects simply put the more voxels you have the  better looking your effect is going to be but this comes with an obvious drawback which is that  the more voxels you have the heavier your scene gets and this is another reason why Houdini is  super powerful because it can draw millions of voxels in the viewport very fast and efficiently  a lot faster than any other software at least I can only assume that in blender if you want to use  more than 5 voxels it will crash instantly so we can increase the voxel count here and we can also  have non-uniform voxels as well so for example if I grab this middle volume so the count on the Y  axis if I set this to a value of 10 we will only have 10 voxels on this Y direction which means that  voxels will be stretched so if I go back over here in Photoshop real quick what happens is let me  let's maybe just disable this a voxel will end up looking something like this so it's going to be  stretched out like so so it's not going to be completely square it's gonna end up looking like  this and voxels below it will look something like this now with the settings that we use this  is going to be 10 times as stretched but this is just really straight that we can have non-uniform  voxels this is not really super useful to have non-uniform voxels but for some reason it's here  and we don't really have to worry about this 99% of the time you will work with square voxels  so I will set this back to 100 let's change this sampling mode here let's set this to  by size which is the version that you will most likely use 99% of the time this 0.1 value is the  result of dividing all the sides so if I check the information here let's also just set the size  back here to a value of 1 so I will hold down control and middle mouse click where it says size  so it's 1 by 1 by 1 just so we have nice and even divisions so if I check the information here  we can see that we have this 10 by 10 by 10 grid and if we divide all of these values we end up  with this size over here 0.1 which is this size that we set directly inside the perimeter and now if  we set this to a value of 0.01 we'll have a grid that's 10 times bigger on each side and this  will mean a total number of 1 million voxels so we very quickly went from 1000 to 1 million and  this will mean that we have a grid that's 100 on each side so again if we divide all of these  values up we end up with a 0.01 value now there's a floating error here we don't have to worry about  we can safely say that it has this size of 0.01 so in essence the lower value that you use  over here the more voxels we will get and the better quality you will have on the effects you will  spend a lot of time adjusting these values so you might as well get comfortable with using these  low values to better explain what a voxel is I recreated a 3d voxel grid with actual geometry  just so we can better visualize this grid and now we can also do some operations this way so  here I have a clip node that slices the geometry in half and we can imagine now that this is a  cross section of our volume so these are the insides of all of our voxels so each of these  small cubes represents a voxel and the information is stored right at the center of each voxel so if a  voxel has a density of 1 it's going to be 1 right in the center of the voxel and if the voxel next to  it is going to have a density of 0 everything in between is going to be smoothly interpolated so a  value that's halfway in between these two voxels will have a density of 0.5 and somewhere around  maybe over here we will have a value of 0.75 so everything in between will be smoothly interpolated  and this is something we can have with any type of geometry or points or anything else so this is  why volumes are super powerful and I created a simple visualizer here I have my original  volume that I created and I just added some simple noise to it and I have a sphere that I can grab  now and if I move this sphere around I also have a text here that will read in the density of our  volume depending on where our sphere is so if I move this sphere around we can see how our values  change and we can also see that there is a gradual and smooth transition between these values  now so far we have only been storing this density value which is a float value but we can also  store vector values as well which can tell smoke simulations or particle simulations in which  way to go and we'll get into vectors a little bit later but for now I just wanted to mention this  we can have many types of data stored in voxels we can also have integer values and matrices  but 99% of the time we are dealing with just the two types float and vector values

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_000.jpg

### VDB vs Standard Volume [8:52]
**Transcript:** in Houdini there are two types of volumes standard volumes and VDBs and it's been decided a long time  ago now that VDBs are the superior version and I'll quickly explain why if we have a geometry that  we want to turn into a volume to later use in a smoke simulation we can do this with an ISO offset  node so here it is and I set the method here by size and set the division size of 0.025  now if I look at the information here we are already working with roughly 500 000 voxels  and even though we see the density only where the geometry is we in fact have voxels inside this  entire bounding box of the geometry so if I do a bound from here this entire box is filled with  voxels that store information it's just that the voxels inside the geometry are one and all of the  voxels outside are zero and this is how Houdini knows how to draw this in the viewport so again I  have a geometric representation of this 3d grid of voxels and I will slice this in half to do a  cross section and we can also preview our geometry as well so this is basically the cross section of  the geometry too so everything inside this bounding box is filled and this is why we have this  large number of voxels so this is not ideal so this is how the standard volumes work and here we  have the VDB version so this is VDB from polygons and if we compare these two results we see we have  mostly the same result with some minor differences but the voxels size here is the same so we are using  the same size for our grid the 0.025 value so here we have the same value and if I look at the  information for our VDB versions we see that we only have 57,000 voxels so this is almost 10 times  less voxels than our standard volume version what happens if we do a cross section of this and let's  also preview the geometry we can see that we no longer fill up this entire bounding box with voxels  but only create them where we need them and this is why VDBs are superior because we can have  what's called a sparse grid so any voxel that would essentially have a value of 0 is removed  from the grid entirely this obviously says up a lot of computation time and memories so VDBs  are a lot more efficient and the cool part about VDB is that they also come up with a lot of notes  so if I press tab and type VDB we can see that we can do really a lot of operations here but what  school is that we can also use all of the regular volume operations as well so if I type volumes  we can use all of these notes as well so if VDB is still a volume at its core so all of these  other notes will work as well whereas with a standard volume we cannot use any of these VDB nodes  so if I were to go to my ISO offset version I can't really append any VDB operations here because  these won't work so if you ever find yourself working on a simulation and you append a VDB node  and it doesn't work double check that you're working with a VDB volume and not a standard one  if I check the information for this ISO offset we can see that our name here is with a teal  color so maybe I can also just give this a name real quick here okay so we have density and this is  with this light blue or teal color and if we go to the VDB version if I check the information here  this will have a darker blue color so this is just something to be mindful of now to sum up VDBs  are very powerful and efficient and moving forward we are going to be using VDBs

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_001.jpg

### SDF Distance Volume [12:31]
**Transcript:** with VDB comes another type of volume that's super important which is called the sign distance field  sdf for short now when I first started using kudini and heard this name sign the distance field it's  sounded super sci-fi like something out of blade runner but in reality it's pretty simple to create  an sdf we can use the same VDB from polygons and here we can say that instead of this fog VDB  we can turn this off and we can turn on the distance VDB instead so distance VDB will give us this sdf  volume which looks something like this in the viewport and the way the sdf works is we have the same  voxels that we had earlier with our density so we are using the same size here but the difference is  that in each voxel now we are simply storing the distance from the surface of the geometry so if I go  back to the cross section geometry representation here and let's also grab our toy as well and let's  let's maybe just do a slice of this so we don't have this entire half of the geometry on the side  and we're left with a thin slice so I'll just do another clip here and reverse this and just  increase the offset like so and I'll repeat this for the geometry as well you don't have to worry  about all of these operations again this would be just a geometry representation so this is kind of  what's happening if we were to grab a slice of our VDB so if I zoom in here I'll maybe do a  print screen and let's step over in Photoshop so here we have the surface of the geometry and let's  say maybe that this voxel here has a distance of let's maybe say for example 0.1 then this voxel  next to it will have a distance of 0.2 and this one will have a distance of 0.3 and so forth the  further away we get the higher our distance values will be now again all of these values are  interpolated so we'll have information within this entire space over here we also have information  on the other side so over here this voxel will have a distance value maybe of negative 0.2 and  negative 0.3 and this is why it's called a signed distance field because we have positive values  on the outside and negative values on the inside and in the viewport Houdini will simply draw  some geometry for each voxel where the distance value is 0 so this is why we have this kind of weird  representation of our toy geometry so this is what the SDF is and we also have a couple of options  here for exterior and interior band voxels so by increasing these values we determine how further  out from the geometry we should have voxels and this is better seen maybe in the geometry  representation here let's maybe go to our clip and I have set this up so that when I increase this  peak node we increase the narrow band so if I increase this value here we can kind of see what  happens when I increase the exterior or interior band voxels this is kind of what happens we just  add more rows of voxels on top of what we have so if I go back to this we can control each side  individually so how much how many voxels we have on the outside and how many we have on the inside  and using these values as they are here is directly related to the voxel size here so the lower we  set the size and the more voxels we have the more we would have to increase our exterior band voxels  we can also use world space units instead so when I turn this on our narrow band will adjust  accordingly to match the values that we set here so now even if we change the voxels size  for the entire volume our exterior band size will be the same so our expansion will be the same  even as I increase this we can see that the bounding box stays relatively the same and this is  useful a lot of the times because we can make tests on lower quality volumes for faster iterations  and then when we set the volumes back to a higher quality we won't have to adjust these values  as well so this is all the sdf is it's simply a volume that records the distance from the surface  of a geometry and this value can either be positive if it's on the outside and negative if it's on  the inside so we established that vdb's are preferred over the standard volumes and a vdb can be a

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_002.jpg

### Volume VOP [16:54]
**Transcript:** fog vdb or a distance vdb and now we can start having some fun and introduce the volume VOP  so let's go ahead and add this I'll type vv and I will go down here to this volume VOP let's  drop this down and we will connect this with our vdb from polygons and this volume VOP works on both  fog vdb and distance vdb but for now let's focus on the fog vdb so over here I'll turn off the  distance vdb and turn on the fog vdb so we will work on a fog volume first now for this next part  it's important that you have previously watched the intro to VOP series or that you know your way  around VOPs in the context of regular geometry a lot of things we do with VOPs or VEX for regular  geometry can sort of be applied to volumes as well and this is another big reason why Houdini is  super powerful with volumes and I just want to add the regular attribute VOP for now to  compare the two right away we can see a slight difference when we select either of these the  attribute VOP has the option to run over primitive points vertices so all of the different components  of a geometry whereas the volume VOP doesn't have this option at all so it's more unique than a  regular attribute VOP and if we step inside we kind of have the same structure for the volume  VOP as the attribute VOP and in fact I'll just do a screen grab of this so we can compare them  side by side so on the left over here we have the volume VOP and on the right we have the  attribute VOP and the first difference that we can see here is that the volume VOP only has the  one output which is the density and the attribute VOP has all of these different outputs because  usually a geometry will contain multiple attributes so we can export back to our position velocity  force and in the volume VOP by default we only have this density now we can export to multiple fields  as well and we'll get to this in a second but for now we can just focus on this density for the  inputs we have some similarities as well we have the position on both versions another than that  the only other things similar here really is going to be the time, time, time, ink and frame  so we have this as well on the attribute VOP and we have this four orange inputs over here  openput1,2 and 3 and 4 which will return the geometry that we grab from outside the volume VOP so  it's going to be corresponding with these four dots so again this goes back to everything we know  from the intro to VOP series now for the volume VOP we also have these inputs here ix,  iy, rex, center, orig and to be honest here most of these inputs so everything that's between density  and time are not super helpful in most cases and a lot of information that these inputs can provide  we can generate it ourselves from the position so this p over here for example this bb stands for  bounding box and we can get the same thing if we were to append a relative to bounding box from  our position here and this node we have in the regular attribute VOP as well so most operations  inside the volume VOP I prefer to do them using the position value because this will be more  consistent in the way that i do it with an attribute VOP when working with geometry now it's  important to keep in mind that this position over here refers to the position of the voxel itself  so it's not tied to whatever information the voxel is holding whether it's a density value or a  velocity value the position will give us where the center of each voxel is in our scene so if i go up  and if i go back to the cross section over here i'll maybe zoom in here and we can do a screen  grab of this when we are inside a volume VOP and we are grabbing the position we are really grabbing  the center of each voxel so we can imagine this point in the center of each of our voxels and this  will be what the position is referring to so back in hudini inside the volume VOP it's important to  keep in mind when we use this position that this is operating over the 3d grid voxels itself  not really on the density values that are shown in the viewport so let's maybe just set the  display flag back to the volume VOP and let's start modifying this volume so let's stay back inside  one of the most common things you will do inside a volume VOP is use noise to break up the smoke  so from the position here we can generate a noise pattern let's use a turbulent noise in this case  and if we plug the result of this noise inside the density we can see our noise pattern over here  and now we can increase the amplitude and we can play around with the frequency and we can kind of  see the results that we get so again this is just like how it would work on actual geometry in fact  what I can do I'll go up and from my rubber toy geometry over here I'll just scatter a bunch of  points so let's drop down this scatter I'll turn off some of these settings here and I'll just  create a bunch of points here let's do one million and if I append an attribute VOP so the regular  version I can jump back inside the volume VOP with these settings that we set up over here so I'll  just grab this control C and I can paste this inside the attribute VOP let's link this back to our  position and if I output this to my color so inside the city we can see that we have the same  pattern so if I preview this and preview my volume we have the same noise running over both of these  objects so this is pretty cool in my opinion that you can just grab some of these nodes from the  volume VOP and paste them directly inside the attribute VOP so we can imagine that if we have a  big network of nodes you can grab all of them and you can just plug them inside the attribute VOP  and with some slight modifications you can do the same thing in both versions now if we go back to  our volume and check our toy we can kind of see that the toy has some sort of jagged border around  it now whereas before when we just had our density so if I plug the density back inside our density  we can kind of see we have a way nicer and smoother border if we compare these two so if I plug  back the result of the noise we have this ugly result around the border and to get the correct  result of this noise we have to multiply it with our original density so I'll plug the density  back here and I'll drop down a multiply and I want to multiply the existing density with our noise  value so if I apply this now we have the smooth border that we had earlier and we also have the  noise pattern on top so if I disable this we can see the result and we can also compare it  with how it looks like if we just output the noise values like so so we can clearly see the difference  now and it's important to note that this noise and a lot of the operations that we do inside of VOPs  operate on all of the voxels and if I were to go up and I'll get rid of the scatter I have the  ISO offset node here so again this will mean that the entire bounding box is filled with voxels  if I were to duplicate my volume VOP node with the noise applied with let's plug this here and  preview the result if I step inside and if I output the noise values directly inside the density  we can see now that the entire grid is filled with this noise pattern so our noise is overriding  the values of all of the voxels and this also goes back to what I said earlier about the  difference between regular volumes and VDBs over here to the VDB if we apply the noise directly so  if I apply this here we don't fill up an entire box of voxels because the voxels only exist  right outside the surface of the geometry so all of this is to say that usually the operations we  do in VOPs we want to apply it to our existing density so most of the things we do will either  multiply add or subtract from the original density and apply it back to the density or whatever  other field we might have like velocity temperature and so on let's set up a more practical example

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_003.jpg

### Fog Volume VOP examples [25:31]
**Transcript:** of VOPs and this technique is something that I did in a lot of projects over the years let's just  start with a fresh volume VOP so I'll delete this one and I'll drop a new one so volume VOP and  let's dive inside let's say that I want to fade the density along the height of our object so I  want more density at the base and no density right at the top we'll drop down a relative to  bounding box node and we'll connect the position to this node and this node is covered in the  intro to VOP series but just as a refresher this node will give me a 0 to 1 value along each of  the x, y and z dimensions of our bounding box and they're all stored as a vector inside this node  so if I want to grab the 0 to 1 value of our height so the y axis I can do a vector to float from  here and this will split up the x, y and z component so if I want to grab the y component this will  be the middle value over here so if I want I can plug this directly inside the density so let's  plug this over here and we can already see the sort of effect that we want we have a gradient only  that this is starting with high density at the top and no density at the bottom but we can kind of  see this gradient here let's also remember that most operations like I mentioned earlier will  have to do in relation to our existing density so let's multiply this by our existing density so  I'll just plug our density back I'll do a multiply here and let's grab again the second value here  and this will be the new result so when I plug this directly inside the density we can see the  difference so we want to use the existing density here as well and from here we can further add  some more control so I'll make some room and this 0 to 1 value of our y component let's filter  this through a ramp so I'll grab the wire I'll type ramp and choose this ramp parameter over here  I'll set the ramp type to a spline ramp and now if I go up we have our ramp controls here at the top  and I'll grab the endpoint here and just move this up and now we have what we had previously but I  can reverse this ramp if I click on this icon over here we can do a complement ramp so if I  click this now we have the reverse effect so this is what we were aiming for and now I can also  control these handles to squeeze in the transition where this happens so as I bring these handles  closer together we tighten up the region where the fading occurs we can also offset this entire  fading if I were to add or subtract to our position so if I go back inside over here before we  introduce our ramp so after this vector to float I will do an add and I will promote the second  input to the top level so middle mouse click promote parameter and if I click this we can give  this a name in the label region over here so I'll name this offset and now if I go up we have this  offset parameter here and as I increase or decrease this we can see that we are moving where this  entire transition happens so if I were to squeeze this even more together so it's nice and tight  and if I move this around we can essentially offset this transition and hopefully we can see that  already we are getting some cool results so for example we can animate this offset value to reveal  our object like so so really we just a couple of nodes we're setting up some cool effects and if I  want to take this one step further we can introduce some noise as well so let's step back inside  and I want to displace the position from where we grab our bounding box values so this speed that  we are plugging over here let's go ahead and let's drop down an anti alias flow noise and I'll  drop this here let's generate this from our position and this I want to be a 3D noise and I want  to add this to our position over here again from where we generate the bounding box value so let's  drop down and add and we will add our noise to this position and we can already see this change in  the viewport so if I were to right click the noise and I go to the Vex Vops options here let's  create input parameters so this will expose all of the parameters that the noise has let's go up  and now here we can control the frequency of the noise and we can control the amplitude  and I can go back to my ramp and I can fade this out less or more and we can also control the  offset of this and one really cool thing that we can do here if I animate any of the offset values  we can even introduce a animation in our noise so for example I can grab the middle value over here  which will be the offset on the y direction for the noise so we can see if I just scroll the values  here what this will do for us so I can just simply set up an expression here that will tie it to our  time in the timeline so if I say dollar sign D this will give us the time and now if I play the  animation we can see in the viewport we have animated smoke and this actually looks kind of like a  simulation but it's not really a simulation we are just offsetting the noise and if I want to make  this slower I can just divide this value here so let's divide this let's make this four times  a slow so I'll divide this with a value of 4 and if I play now we can see we're starting to create  a very simple ground fog and this technique I used in a lot of projects over the years so if for  example I were to replace this geometry toy with a simple box I can drop this here and I can just  plug this directly in our chain and we can see that everything updates so I can press enter  with the box selected I will hold down shift and I can expand the dimensions of the box  like so and maybe I'll turn off the grid and press escape now let's go back over here and to  hide this wireframe we can press shift W and now we can see we created a very simple ground fog  and this will work very well in a lot of cases and you can see how fast and easy this was to set up  and because we are using the bounding box this will work with any geometry and for example let's  just do a peak head as well and I'll plug this here so we can see that no matter what geometry  are using or where it is in our scene this will always work because every object will have a  bounding box that can contain it so that's where the values are being generated that's 0 to 1  gradient values so again just to demonstrate this more if I play around with the offset and I  maybe play around with the ramp as well or I can reverse this we can kind of see the different  results that we get so a lot of cool stuff that we can do I want to show one more practical example  with density so let's say that I want to fade the density from a specific position in our scene  let's maybe just place this valve to the side and this can be let's rename this to gradient fade  and I'll just create a new volume VOP and let's rename this one to position fade and if you want  to grab these examples I provide the project files with the downloads of this course so if you're  watching this on YouTube or some other place I'll have the link to the course where you can  download the hip files so you can inspect the setups now for this position fade we'll want to  reference a position in our scene and for this I'll just use a simple sphere so I will drop down  a sphere and I will set this to a primitive so if I use this primitive option this sphere will  be treated as one single point so I can go to the information tab and see that we only have one  point and this is just so we can easily reference this as point zero let's also append a transform  to this sphere so we can move it easily in our viewport and maybe I'll move it to the side  let's template our big head geometry to see where it kind of is all right so I think something  like this will be fine I might want to make this sphere smaller just for preview purposes it  doesn't really matter what scale this is but I'll just make this smaller so after this transform  let's plug this in our second input of our volume VOP so now if we step inside here we can do an  import point attribute and we will use this to grab the position of our sphere so the attribute  will be P and the signature will be vector tree so this is fine the input we want to point two  hour second input over here so let's dive back so we will point this input two hour second input  and we also have to specify the point number and again since we are using just one point the point  number will be zero so I can middle mouse click this pt num value here and promote this to a  constant and I will leave this with the default value of zero so now with our sphere position we  can generate a distance from this sphere to the position of each voxel so we will grab the voxel  position from over here we'll do a distance and we'll plug the sphere position as our second input  and if I plug this directly inside the density we see something sort of happening and by default  the distance values as they are will not be super helpful so we have to remap these values we can  use a feet range after our distance and with the source min and max I can control the spreading  of our fading and we can kind of see something happening here around the year as I move this  source max value further out and I increase this we sort of start to see this gradient effect  and I can reverse the destination min and max to reverse this gradient so if I set this here to one  and our max value to zero now we can see this gradient a lot better of course we will want to  multiply this with our original density so let's plug the density back we'll do a multiply and  let's plug the feet result in our second input so let's maybe just promote the values of the feet  so I'll go to VaxVops options and let's create input parameters let's go up and now if I grab the  transform of our sphere and press enter I can move this around and I can determine where I want  this gradient to originate from so this is very helpful a lot of the times let's maybe just  reverse the values here back to where it was originally so let's say that in our 3D space  this position where our sphere is let's say that this is our camera and I want to fade the density  that's closest to our camera because a lot of the times what happens is when you have a volume  simulation that comes towards the camera so you can imagine something like a dust shock wave  you might not want the density to obscure part that's right in front of the camera too much  so you can grab the camera position and you can fade the density that's closest to the camera  like so so if I maybe if I just place this over next to the ear we can see the effect that we are  having so this is super useful a lot of the times and I use this in a lot of other projects as well  so this is just another practical example of what you can do with a volume  valve now like I said a lot of the operations that you can do with a regular attribute  valve you can replicate in a volume valve and really here we're just scratching the surface of  what's possible and we can really push this a lot so in my magical skyping course we use volume  valves to modify the cloud scape and the animation of the clouds spreading apart and twisting is  also done with volume valves so we can use valves to fake simulations create interesting  volumes and we can also layer them on top of real simulations to get that extra level of control  and if you want to check out this course this will be available on my website Voxide.com so feel  free to check this out now so far we've only been doing operations over one volume which was our

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_004.jpg

### Multiple Volumes [37:39]
**Transcript:** density but we can actually store multiple values in the same object so in fire simulations you will  usually need a volume that drives the flame and a separate volume that drives the smoke that  the flame creates so having multiple volumes is important for the simulation process but also  for rendering we usually need different settings to control the flame and the smoke separately we'll  start fresh with a rubber toy and we'll do a VDB from polygons and let's turn this into a fog VDB  and I'll decrease the resolution here or rather increase the resolution by reducing the scale  so I'll use 0.01 now if I do a volume valve from here so far we've been affecting our density  so in between here if I do for example a multiply constant I can increase or decrease the amount of  density we have and everything works now this output here is set by default to export to a volume  that's called density so in our VDB from polygons if I go up and I go back here if I change the name  here to let's say temperature now we can see that this volume valve no longer works so if I go  back inside and modify our values here nothing changes in order to affect this volume that's now  called temperature we have to do a bind export and we have to specify the name ourselves so in the name  parameter here let's type temperature and I will also need to bring in the temperature volume  so I will do a bind so regular bind not a bind export let's do a bind let's also name this to  temperature I'll plug this input temperature to our output temperature and if I just duplicate this  multiply constant node here and plug this in between now we can see that this works it's still  represented as a fog in our volume but we can see that now we can affect the temperature volume so  if I go up we have to be mindful of the name of the volume that we are operating on so this bind  export kind of works in the same way it does in an attribute valve where we can bring in an export  attributes on geometry but the difference for volumes is that we can't use this bind export to  create new volumes the volume has to already exist in order to do operations with it so for  example if I go up and if I were to be working on a standard geometry so not a volume if I do  a attribute valve from here I can grab any of our input attributes that we have and we can do a  bind export so let's grab our position for example I'll do a bind export and I'll name this to  pause now if I go in the geometry spreadsheet let's hide all attributes and let's only show our  pause we have this pause attribute and if for example I grab the same position and let's just  modify this for example sake I'll do a net constant here and just add a value on all axis I can do  another bind export like so and let's just name this new pause for example and we can see we have  created a new attribute with both the bind export so now we can see the new pause attribute here as  well now in a volume VOP let's also go back to the scene view we can't really do the same thing we  can't do a bind export and create new volumes of for example if I want to create a new volume based  on temperature let's do a bind export and see what happens so I'll do this bind export and let's  name this to heat nothing changes and if I also go up and I look at the information we still only  have the temperature field created so our heat field that we want to do operations on has to  already exist and for example I can duplicate our VDB from polygons node if I hold down out I'll just  duplicate and this new volume let's name this one heat and now I can merge these two together so  I'll drop down a merge let's pluck our second VDB from polygon here and now we can kind of see  something happening in our viewport if I look at the merge information here we have both fields  that we need now temperature and heat and now in our volume VOP we can affect this heat field so if I  do a multiply constant here and if I play around with the settings we can see changes in our viewport  so we are indeed affecting our heat field it's just that both the temperature field and the  heat field are displayed as fog in our viewport so we don't really have a clear visual indication how  this works and we'll talk more about this in a second but for now I just wanted to express that  unlike the bind export in an attribute VOP in a volume VOP the field needs to be there already  in order to do operations on it so let's go up and I'll get rid of this volume VOP and also the  attribute VOP and like I said earlier in a typical fire simulation we will have a field for smoke  and a separate field for fire so I'll name one of these fields let's name this density and for  the second VDB from polygons let's name this other field temperature and you can use whatever name  you want here it's just that a lot of the nodes and presets that Houdini comes with will look for  these names by default so temperature and density and this is why I'm giving them these names and now  when we merge the two volumes we have both fields here and we can change the resolution for each field  individually but sometimes you will want to create a volume based of an existing volume in order to  have the same resolution so let's let's go ahead and get rid of the march and the second VDB  from polygons and let's say that I want to base the temperature off of this density field  I'll set the voxel size here back to 0.01 and to create our temperature field I can simply change  the name of this volume so I can append a name sub here and I can set the name here to temperature  and now we can see that this name changed here from density and I can simply merge this  back together with our density so we have the same result now temperature and density  but now with this VDB from polygons I will control the resolution for both fields at the same time

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_005.jpg

### Volume Visualization [43:56]
**Transcript:** now we are drawing both fields as smoke in our viewport but let's say I want to use the  temperature to control the emission of the volume to do this we can use a volume visualization node  so let's append this here in this node we will have two tabs one for smoke and one for emission  and we can specify which fields we want to drive each of these so for smoke we are looking for our  density field by default here so I can use this ramp here to remap our values and we can kind of  see something happening in our viewport let's maybe break up the smoke so we introduce some noise  in order for this to be more obvious so after the VDB from polygons I want to do a volume  noise fog from here and this node is really just sort of like a preset of what we build earlier  ourselves with VOPs so over here I'll set the operation to multiply let's do zero centered  range values and I'll also enable ramp remap ramp here and I'll just bring this point up slightly  so we can see we have the same result that we had earlier so again all of this we can do our  selves in a VOP node and if I press tab and type here volume we can see we have a lot of operations  that do different things and a lot of these are based on volume VOP so in fact if I were to go  inside this node so right click and allow editing of contents and step inside we can see that it's  really just a volume VOP edit core and this is why I'm stressing over this volume VOP so much if we  understand how the VOP node works we can really do anything we want and we're not limited to this  preset options that we have here so going back to our volume visualization node if I adjust this  ramp now we can see how we can affect the density in our viewport we can also multiply the density  scale overall to take an up our smoke but it's important to know that these changes are only  affecting our viewport display we are not really modifying the actual density values stored inside  the voxels we are just artificially changing the values as they are displayed in the viewport  this is important because when we render we'll have to do the same thing the density values we use  here we can also use later in our shader when we do rendering and with this volume visualization  node we can get an idea ahead of time of how it's going to look we can also give our smoke a  different color so we can map this diffuse field to a different volume but a lot of the times we'll  use the same density field so let's just point this to the density and now if I change this ramp  here we can see how this affects our smoke so I can give this a blue color I can play around with  remap values here increase the density so this will be our smoke and in the emission tab we can  point the emission field here to our temperature and now if I increase the emission scale we start to  see something happening usually with the emission we will have to use values above one so I'll just  use 10 in this case we can also remap the ranges like we did for our density with this ramp and  we also have a color field which we can use a different field or we can point this to the same  temperature field so if I set this to temperature we can kind of start to have this fiery look and  this is because by default Houdini gives us this orangey ramp that's giving us a fire look but of  course we can change this if we want so I can use a different color here and when you are setting  the colors for the emission it's important that you introduce a bit of white to the colors so it's  not completely saturated so I'll just reduce this here to saturation maybe of 0.9 and if I go back  to the emission scale here I can increase this further to make this really bright play around with  the emission ramp and we can maybe also introduce more color information here so I might want to drop  this down slightly to have more of a gradient and if I increase the whiteness point here I can also  remap the minimum and maximum ranges we are sourcing so if I turn this on and I reduce the maximum  range we can see we are starting to introduce more of our end color here so if this were more white  we have this very vivid gradient now so we really have a lot of visualization options here  now currently our temperature field is the exact same as our density field so these two are  overlaid exactly on top of each other and we can't really tell apart the density from the smoke  so we can add a volume VOP before our volume visualization so let's drop a volume VOP down  and let's step inside and let's do some changes only on our temperature field so I will do a bind  and let's bring in our temperature and we will do the same thing as we did earlier with our  gradient fade along the bounding box so from the position here let's do a relative to bounding  box let's split this vector up so let's do a vector to float and let's go ahead let's grab our  temperature I will do a bind export back to our temperature so let's type the name here I'll  plug these together and let's also so from our second input which will be our height let's also  use a ramp so we can further control this and I'll set this to a spline ramp and now we want to  multiply our existing temperature by this ramp value so let's plug this over here and if I go up  I can reset the ramp so I can hold on control and middle mouse click the title here this will  give us the default ramp and now if I go back to the volume visualization node let's maybe increase  the emission scale we can kind of see something happening and let's maybe just set this back our  emission ramp to the default values I can go back in this volume VOP and if I reverse this ramp now  by clicking this button here we can see what's happening a little bit better and I can squeeze this ramp  now to determine where we want our emission field so we can see only the base of our rubber toy  has this emission and if I go back here let's maybe just go to the smoke or let's maybe just  rather increase the density so I can push this out further to increase the density and let's maybe  just use the default colors for our ramp for the density so I'll reset this diffuse ramp just so  we have color only on the emission and this is now the result so if I go back to the emission  let's maybe turn on the emission color ramp back and I can increase the emission scale even  further and we can see how the emission is only here at the bottom maybe I can go back to the  volume noise fog and change these settings a bit and hopefully you can see now where I'm going  with this I can go back to the volume VOP here and control where our emission starts to fade and we  can clearly see a distinction now between the density field and the temperature field an even  cooler thing we can do here instead of this volume noise fog we can drop down a cloud noise  and let's replace the volume noise fog so I'll get rid of this and I'll plug the VDB from  polygons into this cloud noise and let's plug these inside our cloud noise like so so we can see  what is happens and if I just look at this cloud noise this will simply this place our volume  in sort of like this cloud shape and we can adjust the settings here if we want we can make this  more obvious and increase the element size and so on so all of these settings are pretty self  explanatory and if I step inside this cloud noise so I choose allow editing of contents and go  inside you can probably already guess that this is also based on a volume node and in fact if I  step inside here we can see the entire network that creates this effect now this is really a  huge network and this is because Houdini wanted to give us as many options as possible but really  the logic behind this effect is a lot more simpler than this to recreate this we only really need a  few nodes here so you can actually check out how this cloud noise is built so let's go up and let's  go back and if I look at the volume visualization now we can really start to see some cool effects  let's maybe just set our emission ramp so in the emission tab let's set this back to our fiery look  so now we kind of have this fiery look on our rubber toy and I can go back to our cloud noise and I  can even animate this and we can create some really cool effects we can also use a more advanced  version of this volume visualization node and this was introduced more recently Houdini we can  drop down a pyro bake volume and here we have a few tabs the smoke tab works kind of the same as  as it works in the volume visualization tab so I can increase the density of our smoke like so  and if I go to the fire tab and turn this on we kind of have the same thing as we did with our  emission but we also have the scatter tabs so I'll just turn off the fire for now and turn on  the scatter one so if I turn this on we start to see something happening here and as I increase  the intensity here let's increase this a lot more and maybe we can just reduce the source range  so if I decrease this value here we can start to see our scatter a lot better let's maybe increase  this even more and and the values here are kind of directly related with our smoke so I can decrease  the density scale to make our scatter more obvious let's maybe increase the shadow density as well  and make our smoke darker and let's go back to our scatter tab and we can play around with the  settings until we get something really cool so we can play around with some of the blur values  and the masking as well so this scatter is used all the time in explosion to get this fine detail  that's using the density field as a mask on the temperature field and we can kind of see how we  get this sort of a pyroclastic look and maybe I can go back to our volume knob here and let's make  some changes on this ramp let's maybe use another dot here and I'll make this a B spline so we have  a smoke fading and also I can increase the temperature values directly inside the volume  so before our pyro bake node if I go inside here I can simply grab the third value here of our  multiply and I can promote this to a parameter and if I rename this to let's say global amount if I  go up and I increase the global amount we will multiply everything that's happening after our volume  so if I increase this we can kind of see how everything else increases as well and maybe I can also  in the pyro bake volume maybe I can increase the hardcore a little bit maybe not so much so we  can see that this does take a little bit of fine tuning but we can get some really sick results we  can also add the fire on top of this so if I add the fire back we also have our original emission  on top so maybe I can reduce this so we really have a lot of options here to control this look and  we can see how easy it is to get a pretty cool effect we can also use volume vops on distant VDB

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_006.jpg

### VOPS with SDF [55:18]
**Transcript:** so as the F's let's drop down another VDB from polygons here and we'll use the default distance VDB  here so this will create an SDF that's called surface and let's maybe just decrease the  voxel size here so increase the resolution 0.01 and let's do from here a volume VOP and let's step  inside so again we right away run into the same problem with the naming which is that this volume  VOP is tied to density and we want to bring in our surface field so let's do a bind I will type here  for name surface and if I hold down alt and duplicate this node as it is I can turn on this option  to use this node to set parameter attributes and export here I will type to always and this  essentially now turns it into a bind export node so if I do a bind export we can see that this is  the same node as a bind with just different settings to it so this is how we can recreate the  bind export node so now if I plug the surface input into our output surface in between here we can  modify our volume and if we do a multiply constant and I increase and decrease this value we can see  that nothing happens whereas on a fog volume this will fade out our density or thicken the density  but nothing happens for our sdf and this is usually because with sdf we want to add or subtract  values rather than multiply or divide so if I instead delete this and add a add constant node now  if I set this value to 0 here and I slightly increase the value we can see we push the sdf in  and if I decrease this value push the sdf out and this is already different than if we were to add  or subtract values from a fog bdb so let's go back to our cross section and see exactly what happens  let's go over here and also template the geometry and I'll just step inside Photoshop real quick  so right over here is where the surface of our sdf is drawn meaning that right on the surface  this voxel will have a value of 0 over here and over here we'll have 0.1 and further out we'll  just increase the density so maybe this is 0.2 and here we'll have negative 0.1 and negative 0.2  and so on so when we add or subtract values in a volume VOP this operates over all of the voxels  at the same time and this voxel over here where we have negative 0.1 if we add a positive value so  if we add 0.1 to this this now becomes a value of 0 and so our surface will be drawn over here  and this is why when we add to our sdf we are actually pushing the volume in and similarly when  we subtract a value of 0.1 so if we subtract it from over here now this voxel will have a value of 0  and our surface will be drawn further out over here and this is more or less how we can think about  sdf's as operations that are pushing and pulling along the normals of the volume like so  so it's kind of like pushing the normals of the geometry so if I grab the rubber toy and I add a  peak node to this this will push the points along the normal so if I so if I increase or decrease this  we kind of have the same thing happening as we tower volume VOP on on the sdf so if I go inside here  let's maybe just delete this and do another add node and I'll just promote this to the top  and I'll rename this label here to add so if I increase this we push our volume in and as I  decrease this we push the volume out only the problem when we do it on the geometry we can see  that the geometry starts to intersect with itself and we get this very weird and nasty result  whereas with our sdf's we don't have this problem because everything will be smoothly interpolated  since that's the nature of volumes now if I push the values slightly more we can see that the  sdf suddenly starts to disappear now going back to our cross section and I zoom out here a little  bit even though we are offsetting the values and we are pushing the surface out like so we are  still operating within the same voxels with which we start so when the surface that we push out  reaches outside of the volumes that we have so for example over here we can no longer draw our  surface in the viewport because those voxels here don't exist and this is where on our vdb from  polygons our exterior band and interior band voxels come into play as I increase these values here  we start to introduce more voxels around our mesh and now we are able to push our volumes more  we can still see that we reach the limits of our voxels and for example we have this very  handy option here that says fill interior so now everything inside our mesh will be filled with  voxels so now essentially I can push this in as far as I want because we have voxels everywhere  inside our mesh and we can see how easily if I were to animate this value we can create a  revealing geometry animation like so and to turn this sdf into regular geometry we can do a  convert to vdb from here let's append this to our volume VOP and let's convert to polygons  and now if I press shift w we have actual geometry and we can play around with the additivity size  over here so if I increase this this sort of decimates the geometry and tries to retain the shape  and if this is a value of zero it's going to use the maximum subdivisions that it can  and the resolution of this geometry is tied to our size of our vdb so if I decrease this voxels  size here we are also going to decrease the resolution of the geometry so these are tightly linked  together some of the other advantages of sdf is that we have a lot of algorithms pre-built to help us  change the shape so for example after the volume VOP we can do a vdb smooth sdf and we can kind of  blur our result I can increase the settings here and try different operations and see what we get  and the vdb smooth here is used in a lot of different effects and it's also used to help us  with meshing fluid particle simulations also a very common one is going to be vdb reshape sdf  and if the operation here is dilate and I push this out we kind of have the same result  as we do when we simply add to our sdf so as we did earlier with our volume over here only with  the vdb reshape sdf we have some extra options here so we can control this a little bit better  and if I type sdf here we can see that we have a few more operations that we can do so these are  all pretty helpful but the vdb smooth and the vdb reshape sdf are two of the most commonly used nodes

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_007.jpg

### Working with SDF [62:25]
**Transcript:** so I'll get rid of these three nodes and let's go back to our volume VOP and I'll reduce this  add node in fact let's go inside and let's remove this promoted parameter over here and instead of  adding a constant value to the sdf we can generate noise values and add those instead so from our  position we actually don't need this volume VOP node we can get rid of this I'll get rid of this  global output here and let's just grab our position so the p and we'll do a turbulent noise  and we can plug the values of this noise directly inside our add and we can kind of see something  happening now to further control this we can do a feed range after our turbulent noise to further  control our values and I can decrease the destination max now and we can see that as I increase this  we start to eat away more of our sdf and we also have this nice noise pattern we can also go  to the turbulent noise and I'll set the type here to simplex and I know that the simplex outputs  values between negative 0.5 and positive 0.5 so in our feed range I will set the source main to  negative 0.5 and our source max let's do 0.5 and now we can use the destination mean and max values  here to sort of push in our sdf if I increase the destination max and I can also push out some values  if I decrease the destination mean here so I can go in the negative direction and we can see how  some of these values now inflate sort of our rubber toy so we can control these both and we can  get some interesting results and of course we can go to our turbulent noise and increase the  amplitude so this will accentuate the difference between the low and high values and I can also play  around with the frequency and the roughness so we can get a lot of interesting results and we can  turn this into geometry now if we want to and we can really build a lot of organic shapes this way  now it's important to know that even though we are displacing the sdf in 3d the noise values that  we generate here are 1d so this signature here is a 1d noise and this is outputting a float value  and this is something to keep in mind while working with sdf usually you will want to add or subtract  float values whether you generate the values with noise or something else if I change this signature to  a 3d noise we still get something going but this is not super helpful as we are adding a vector  to what's essentially a scalar field and we'll get more into this a bit later but if we go back to  what I said earlier on sdf contains a distance value from the surface and this is a float value so it  doesn't make sense to add a vector to it and for example if I go up and if I were to do a noise  displacement on the regular geometry so from here I'll drop down an attribute verb let's go  inside and I'll plug our p into the output p and I can generate a turbulent noise here if I  set this to a 3d noise and add this to our position so let's go ahead and add the noise here  and let's also do a simplex so we have both directions so I'll set this to simplex noise and  maybe increase the frequency and we also need to update the normal so I'll go up and I'll just  depend a normal sop as well just so we have the correct shading let's go back inside the attribute  verb wait this amplitude we can control the displacement and we can see that this works fine  when we are using a 3d noise to displace the geometry because we can move the points around in  3d space but again with volumes we can't really move the voxels we can only change the values that  the voxels contain and if I wanted to kind of replicate on the geometry what happens in an sdf  we can use a displace a long normal node so I'll drop this here let's get rid of this connection  and I'll go back to the turbulent noise and set this to a 1d noise and plug this in the amount  input over here and now I can plug the displace p in our output position so now if I go to this  displacement I can increase the scale and we can kind of see we are pushing the geometry along  their normals so this is more on the lines of what's happening with an sdf as well but we can see  the limitations when we are working with regular geometry we have all of this really nice intersection  and we are really depending on the geometry resolution whereas with a volume of sop we no longer  have these problems and we get a smooth interpolation between the voxels so we get an overall much  smoother result and we don't have any intersecting problems so in our volume pop I'll go back here  and let's set this signature to a 1d noise so it's important with sdf to always keep in mind that  even though we are displacing this volume in 3d we are in fact working with float values

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_008.jpg

### SDF VOP Examples [67:21]
**Transcript:** so there's really a lot of applications with sdf and they're commonly used to create all types of  shapes and geometry and for example I'll start with a simple sphere so let's drop this down  I'll set this to polygon and increase the frequency and also I might increase the uniform radius  or rather uniform scale here to a value of 2 and let's turn this into an sdf so we'll do VDB  from polygons we'll keep this as surface I might want to increase the resolution so let's do 0.05  and let's append a volume pop here let's step inside and we'll do a bind on our surface so again  the same process that we did earlier I'll set the name here to surface I'll duplicate this node  by holding alt and on this node I'll turn on this use this node to set parameter and export to  always and plug this here so earlier we used a noise pattern to displace this sdf but who did it  really comes with a lot of different patterns that we can use so let's get rid of this density here  and from my position let's do a cellular noise so this comes with all kinds of different parameters  and even outputs let's just see if we add one of these outputs to our surface and if I grab the  border here and add this we can kind of see an interesting shape in our viewport usually with  any kind of pattern for sdf you will want to pair it with a feet range node so let's drop this  here and I'll just decrease this destination max for now and let's see what happens we are lacking  some voxel information when we go inside our volume here so let's go up and let's go to our VDB  from polygons and turn on our fill interior option so we don't get this volume disappearing when  we push our volume too much inside and also I might decrease this voxel size let's use 0.02  so we get a higher resolution let's go back inside and now if I increase the destination max we  can push this further in and if I were to go up I can convert this let's do a convert VDB and  let's plug this here and I can turn this into an actual geometry so now we also have polygons  and like I said we can use all kinds of patterns that Houdini provides to create a lot of interesting  shapes so this is already looking like a sci-fi spaceship or something let's go back inside here and  we can also play around with a source mean and source max and see what different results we get  so maybe I can reduce the source max or I can push the destination mean out and we can see that  we also need some more voxels on the exterior of our surface so I can go up go back to this VDB  and I can increase the exterior band voxels so when I increase this we can add more until we  no longer have these holes in our VDB so we can see with a high enough value this fixes it  and this is also why I like to use the world space units instead so if I turn this on we can  set this to a value directly here so we can see that if I set this exterior band to a value of 1  if we look from negative 3 to negative 4 over here this is the entire one unit length  that the SDF or other our voxels will cover so we really have a lot more room now and I think the  world space units is a little bit more intuitive so let's go back inside and let's maybe choose one  of these other outputs for the cellular noise let's try distance one and we can see what kind of  weird results we are starting to get over here maybe I can play around with the values and get  something kind of interesting going so feel free to play around with some of these patterns there's  also a pattern for hexagon so if I do a tiled hexagons pattern from here we have to pair up our  ID with a random node so this will give us a random value per each hexagon and we'll see this in  a second if I plug the result of this random inside the feedrange we can kind of see what we get  over here and again in our feedrange node I can play around with the settings here so we can  also use this to create some interesting patterns so this looks pretty interesting as well  with a lot of these patterns let's get rid of our cell noise with a lot of these pre-built patterns  we can displace them if we displace the position from where we generate the pattern so if we do a  turbulent noise from here I want to set the signature here to a 3d noise and let's use  simplex and we will add this to our position over here so let's do an add and let's plug our  noise here and now this will combine our hexagon pattern with this noise so if I drop down the  amplitude here we can control how much noise we introduce so now we can really start having some  fun with this sdf and we can see right away that we can kind of use this to create some really  interesting cracks on a ground surface we also have a Voronoi noise so if I drop down the Voronoi  noise it kind of works in the same way we can plug our position in the position here and I'll  grab this seed and plug this inside the random and now we have a Voronoi pattern so if I increase  the frequency here maybe we can see this better and of course we can go back to our feet range  and let's try some of these values out maybe I can push in our source mean or I can push this out  or inwards and this will take a while to get used to we can also layer these patterns as well  so for example I can duplicate these nodes and let's plug this ID from our hex style back inside  the random and I can add our hexagon pattern on top of what we have also so when we do this  we really start to get a lot of interesting and organic shapes and we can see how easily this  to layer stuff and get super complex geometry and one classic example of an sdf is to create some  simple rocks so I'll get rid of all of these nodes let's go back here and I want to generate a  whirling noise for this so let's drop down a whirling noise we'll grab our distance one output  let's do a feet range let's plug this in our add and for the whirling noise I will decrease the  frequency here and if I go to the feet range I can reduce the source max and the destination max  and actually maybe I want to increase the source mean I'll set the source max to zero and increase  the source mean and we can see we get this fast dead result that kind of looks like a rock so now  I can increase the destination max and we end up with a really cool looking rock or at least a base  for a rock and of course we can still displace our whirling noise pattern with again if I use a  turbulent noise set this to 3D noise and let's maybe just use something else we can also use  sparse evolution noise here if I do an add and this plays this position before we input it in our  whirling noise so let's plug the noise to our add we can see now that this is actually really  starting to look like a rock so maybe I can decrease the amplitude here and here we have it so now  if I go back to the whirling noise I can play around with the offset here and if I change the offset  we see how fast we can generate an infinite amount of rocks this way so really powerful and  useful stuff like I said as the Fs are used to create all sorts of organic shapes and even some  hard surface shapes as well now let's do one more practical example and for this I'll go back to  my rubber toy let's just duplicate this over and I'll copy these dots already switch the sphere  to my rubber toy so let's go to our settings here I might reduce the voxel size to increase the  resolution and if we go inside the volume VOP let's go back here and I want to keep the  binding port and the export and I'll delete all of these other nodes in between and let's also  let's just delete that for now and let's do the same trick that we did with our fog which was  the gradient based on a bounding box so let's do from our position we'll do a relative to bounding  let's split this vector up let's do vector to float and let's use the z direction instead which  will be our third value here and if we plug our z value here inside a compare node we can set this  condition here to greater than so now all voxels that will have a bounding box value on the z  direction greater than the amount we set here will retain the current SDF values and if this  condition is not met we are going to set the surface value to zero so if I just simply add this  compare result to our surface if I do an add here and I plug this here now as I increase this value  we can kind of see we have this reveal of our object on our x direction so this is something  pretty cool and here as well if we want to introduce some noise we can introduce it before our  relative to bounding box so we can do a turbulent noise from our position again I'll set this  to a 3d noise and we'll do simplex let's add this here before the relative to bounding box  and now we have some noise as well and if we convert this back to geometry to preview this better  let's do a vdb convert from here and let's set the convert to polygons here we can see the result  and if I go back inside we can control this transition with this compare value here so I can  push this values around to reveal our object now in reality when we set this to a value of zero  here this is where our transition should start but we can see that we have to go quite high here  in this value so maybe at a round point let's see at a round point three we start to reveal our  object and this is because the bounding box is based on the voxels so if we go up to our vdb  from polygons we have a lot of extra voxels around our surface so if I press shift w we can see that  this is actually the bounding box and this is because we set this exterior bend here to a high value  if I reduce this we can start to see that we get rid of some of these voxels so now all of our  operations will be faster and our bounding box also will better match our object so I can  reduce this quite a bit here and now if I go back to the convert result if I step inside the  volume knob now we can see that as I get closer to a value of zero we actually start to reveal our  object we still have a little bit of a gap but it's not as big as before so now if I increase this  from around zero we can go all the way up to a value that's close to one and this way we reveal  the entire object like so so this again can be the grounds for a lot of different effects and in my  laser reveal course we use a similar technique to sort of grow the helmet only in that case we  layer more stuff with our sdf to create a more complex look now so far we've been dealing with

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_009.jpg

### Velocity Field [78:44]
**Transcript:** these two types of volumes distance VDB and fog VDB and these can store float values so here  again this is the cross section of our rubber toy with some voxels running it and this here  this blue line is the surface so with sdf we are storing the distance value to the surface and when  we create smoke or density these will all store simple float values that specify the amount of  density any voxels can have and in both cases the values that we store are float values so these  only have one dimension and the volumes that we create based on float values are called scalar  fields so the density and sdf volumes are both scalar fields now if we want to create some sort of  movement we will have to store a vector value that has both magnitude and direction and now this  new type of volume will become a vector field so in a smoke simulation for example we can move  this smoke around by this vector field so any given voxels will have this vector information  and the direction will specify in which direction our smoke should move and the magnitude will  specify how far along we should push the smoke so this is essentially the strength of a vector  and typically with vector fields we use them to describe a velocity so more commonly you will  find them described as velocity fields so most tutorials and most people will refer to this field  as a velocity field so this is how I'll also refer to them in this lesson now in Houdini volumes  or VDBs will be split into two big categories which is fog volume and sdf volume and this is  because sdf is a special type of volume so we have to specify this from the start if it says  the f or fog so fog volume or sdf volumes are going to be the class of the volume now for fog volume  so far we've only been working with float values so this is how we create density or temperature fields  or any scalar field for that matter the vector field is also a fog volume type so these both are  part of the fog volume class and again these are called types and with vector fields we can go  one level below and we also have vector types so we can also store color information inside a volume  and because the color is made up of three components this is also a vector so R, G and B are the  three components and this is why it's also a vector but this type can't hold direction as well  and this is why we have this separate type for velocity vector which also stores both direction  again and also magnitude and float values can only be float so there's no other types here now don't  worry about all of this too much for now you'll get used to all of this pretty quickly all of this  is to illustrate that if we want to create a velocity vector we need to create a volume we need to  set the class to fog volume then the type of the fog volume will have to be a vector type  and the vector type will be velocity so back in Houdini when we do a VDB from polygons this  distance VDB option will automatically set our volume to an SDF class which again has no other  types and when we turn our volume into a fog VDB this will set our class to a fog volume  but it will be automatically set to the float type so this is what happens when we turn on the fog  volume option in the VDB from polygons and we see that here we don't have a third option to  create our velocity field so usually with velocity fields we have to create these from scratch so to  create this volume we can drop down a simple VDB node and here we can specify what type of volume  we want to create so we can give this a name and typically for velocity fields you'll often find  the name being used as well like this and now we can specify the class and we can see that the  default here is going to be this fog volume and if we do a drop down here we don't really see our  SDF class and this is because the official name for the SDF volume is level set so for all intents  and purposes this level set is the SDF volume now there's also other and staggered vector field  and really I've no idea what these are 99% of cases it's either going to be a level set so an SDF  or a fog volume staggered vector field sounds like something we need because it has this vector field  in the name and I'm sure it's useful in some cases but I've honestly no idea what it does  so just ignore this for now so back to our velocity field we'll need a fog volume and the type  here will set to vector float and we can see that in between float and vector float we also have  bull integer vector integer but again in 99% of cases it's either going to be float or vector float  and now we need to specify the vector type so if we go back to our drawing here this can either  be color or velocity so if we go back by default we'll have this stopper color UVW option here  so this is going to be mostly used for color and this is again a vector that doesn't have a  direction it only has magnitude so here we will have to set this to displacement velocity acceleration  and this is the vector type that we need that also has direction again we have some more vector  types here but mostly it's either going to be this velocity or the color option so let's set this  back to displacement velocity acceleration we can also specify here an initial direction and let's  set the display to our VDB so if I set the background value let's set this first value to one so  this will just be a vector points in our x direction so it's going to be 1 0 and 0 and we can  also specify a voxalessice here so if I want to increase the resolution we can see that nothing  happens in our viewport if we just have the simple VDB node and this is because this VDB node  essentially just creates an empty container that only specifies what kind of volume we want to  to actually create a volume we'll have to pair this up with a VDB activate node and we can see  here we have VDB activate SDF so this will only work on SDF volumes and the regular VDB activate  will work on a fog volume so let's use this version instead and I will plug this here let's set the  display to this and if I increase the size now we can start to see something in our viewport  we can specify the size here and we can also move this around but typically what you will do  is use a geometry to base this volume on so I will just drop down a simple box in our scene and I  will plug this as our second input for the VDB activate and I will set the mode here to reference  so now it's going to grab the box geometry that we created so if I select this and press enter  I can increase the size of our box and our volume will follow this geometry and it's important to  note that this VDB activate is going to use the bounding box of the geometry so if I drop down a  rubber toy and and use this in my VDB to activate it's going to use the bounding box of our geometry  so unlike the VDB from polygons it's not going to follow the surface of the geometry it's going  to use the bounding box and this is actually good for velocity fields because a lot of times  we need this extra information around the geometry so if we can imagine a smoke simulation that  flies around our rubber toy if the smoke gets pushed too much further out from the surface we'll  need the surrounding voxels to actually have information so we can push our smoke back to the  surface so usually with velocity fields you'll need to cover a bigger area now this is tied to  the bounding box of the geometry but if we want to expand this region we can do another VDB  activate a node so let's drop down a second VDB activate and I can set the mode here to expand  and this will increase or decrease our region and it's pretty much the same thing we did with our  VDB from polygons so over here where we have the exterior band and interior band voxels this  simply adds more rows and columns of voxels around our existing voxels so this is the same thing  that happens with our VDB activate when we use this expand option and we can also just use the  expand distance instead so this is my preferred way of using this option because this is tied to  the world units in the same way that it works with the VDB from polygons so let's get rid of the  second VDB activate and I'll switch my input here back to our box and get rid of my rubber toy  now going back to our velocity field we can see right away that even though we created a vector field  we still see this displayed as density in the viewport and this is because they're both of the  same class which is fog volume so if we want to preview our velocity we can scatter some points  inside this volume and I will use the box for our scatter so from the box let's do a points  from volume and this will fill up our entire box with points and we can increase the points  operations here we donate quite as many points and then I'll plug these points inside a volume  trail node and point the second input of this volume trail to our velocity volume so if I plug  this here if I preview the result this will create a trail of our points that go in the direction  of our velocity so we can kind of see this vector points in our x direction which is the value that  we set over here this background value and this is a better way to visualize the velocity field so  we can go back here and I can decrease the amount of points so maybe we don't need as many to  get an idea on how this looks and now we can actually start having some fun and let's see how we can  use volume vops to modify our velocity field I'll just add a null in between our volume and the  volume trail to keep things a little bit more organized so let's maybe drop this down and this  will be my velocity field so in between here we can do a volume vop and append this after the  VDB activate and let's dive inside now same deal here we have this bind to density by default we can

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_010.jpg

### Velocity Field VOP Examples [90:00]
**Transcript:** get rid of this and to change the velocity field we'll have to do a bind export and we'll set this  name here to our field which is VEL we also have to specify that this is a tree float vector type  or otherwise we won't get the correct result so we already have an initial value that points along  our x direction but we can override this so if I just drop down a constant node I can set this  to a tree float also let's plug this here and I can use these values now to control our field  so I can give this some direction in the x dimension and we can play around with these settings and  hopefully we can kind of see the way our trails change it's not super noticeable what we're doing  so far so let's modify this further now this should not be a surprise to you at this point but one  of the main ways we can modify velocity is to add noise so from our position we'll generate a  turbulent noise and the signature here has to be a 3d noise because again we are working with  vector so let's add our noise values to our constant and now we can start to see something a little  bit more interesting if we go to the turbulent noise I can increase the amplitude and we can see  how this changes our trails I can play around with the frequency I can also animate this noise so  sort of the same operations that we usually do with noise let's maybe go up and I'll go to my  points from volume and I might want to use less points and we can also go in the volume trail here  and we can reduce the trail length so maybe this is a little bit more easier to see the velocity field now  this volume trail here doesn't modify the velocity field in any way this is simply for us to  visualize our field now one of the main ways we are using velocity fields is to push a smoke or  a particle simulation and for this example I'll just set up a simple particle simulation I'll do  a very basic setup and won't go too much into deal with it I will probably have an intro to  particle cores dedicated specifically to particle simulations so for now we'll just do something simple  to demonstrate how the velocity field works let's drop down a simple sphere because we will need a  geometry to source our particles so let's take a look at our sphere and I will also template the  volume trail so with this no selected I'll press E and now in my sphere I can turn down the uniform  scale maybe a little bit and I will just position this slightly to the side and maybe something like  this now it's important that the particles that you want to affect are inside the velocity field  as far as the position is concerned so from our sphere we can do a pop network let's plug this here  and let's step inside so if I press play we can see that now we are spawning more particles  on each frame and after our pop source and again let's not worry about what all of these nodes do  this will be a separate course dedicated for particles after our pop source let's do a pop  a Vect by volume so let's drop this over here and we have to specify a velocity field in the  sub-f so I can go up and I can grab this velocity field so this null over here and I'll press  Ctrl C let's go back inside and let's paste this in our sub-f and now this will grab the path  to this velocity field and if I reset the timeline now and I press play we can see that the particles  are moving in the direction of our velocity field now there are quite a few settings here and again  I will cover all of these settings more in depth in the particle course but for now let's just  set the advection type here to update velocity since we will get a more accurate movement in relation  to this velocity field so if I reset the simulation and press play now our particles are moving a  little bit faster and we can see this weird thing happening here which is that when our particles  are escaping the velocity field so if I go up we can also preview our trails and this is the  entire bounding box of our velocity field so when they are pushed upwards and they escape the  velocity field they no longer get this vector information that we have over here and they're  just sliding along the surface so this is obviously not we want and we can go back to our box  over here that determines the size of our volume and if I press enter I can increase the size here  and increase our field so now if I go back to the particles and reset the simulation if I press play  now we have a little bit more space to add vector our particles but obviously here we can still see  that they reach a certain point when they no longer find any voxel information okay so we do  this sliding so this is why I said earlier that with velocity fields is usually better to have  more voxels surrounding the regions that we want to simulate now to fix this we can just change  this velocity field so if we go inside the volume vault let's just change the direction and let's  just make this vector our initial direction point only in the x direction so I'll set the value  here to 1 0 and 0 and this is our new field now and also for our noise we should have said is from  the beginning let's change the noise type to simplex noise so now the pattern the noise values  will be applied in all directions so if I increase the amplitude now we can kind of see the result  we get so if I go up and I reset the simulation let's take a look okay so we can kind of see  these particles as they move along our trails and we can probably no longer need to visualize this  volume trail so I can press e on this to get rid of the template flag display and let's just focus  on our pop net simulation so I can press play and they will travel along our velocity field and  over here at some point they're going to reach the bounds as we can see and basically they keep  moving forward because they will maintain the last velocity that they registered while they were  inside of the velocity field so this is why they keep moving we don't really have to worry about  this typically when you will create a velocity field you will more than likely take into account  the path that you want the particles to take and adjust the velocity field accordingly so if I go  back to my volume VOP and I animate this noise let's maybe just right click and choose to promote  input parameters I'll go up and let's just preview the volume trails I can animate this noise by  animating the offset value here so if I animate the first value let's set this to a dollar sign T  expression and if I press play we can see that the noise kind of has this swimming pattern to it  and this will also translate to our particle simulation so if I go to the pop net now and press play  now we will have a much more organic result and a particle or rather the simulation feels more alive  so if I change the offset value here let's go ahead and multiply this to make this faster so let's  multiply this by 3 maybe let's reset and now we can see that they're even faster so now it's  actually starting to look like a fluid simulation so hopefully now you can kind of get an idea on how  these velocity fields work now obviously this velocity field that we set up here is just a very  basic vector volume so we can get really a lot more advanced with this and inside the volume VOP we  have access to all of our VOP nodes and we can use geometry for the outside to direct how we want  the velocity to be and we can combine it with other volumes as well and we can also use SDS  so there's really a lot of stuff that we can do let's do a more practical example and try to

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_011.jpg

### Custom Velocity Field [98:21]
**Transcript:** create a velocity field based on a curve so I'll drop down a curve sub from here I'll set the  primitive type to nerves curve let's go to our curve and I'll press I'll hold down spacebar  and press tool to go in the top view and let's just draw this curve maybe across a span of four  units so from negative two to a value of two I'll press enter in the viewport and let's just draw a  simple curve as to maybe something like this I'll press spacebar one to go in the perspective I'll  press F to go into editing and I might just want to also make this more of a 3d curve so just try to  have a more interesting path here this doesn't really matter this much for this example so let's  just do something like this I think this will be fine now because this is a nerves curve it doesn't  have any usable geometry so we usually pair this with a resemble node so let's drop down a resemble  and now if I turn on the point display we have regular geometry that we can use and we can store  attributes now on these points now earlier I said that usually to create a velocity volume we start  with a simple vdb node and create an empty volume first and then use vdb activate to set the size  and all of that stuff but we can also just base this velocity volume on an existing fog volume  so I want to give this curve some thickness and we can use a sweep node for this so let's drop  this here I'll set the surface shape here to let's do around the tube and now we can increase the  radius to take on this geometry and we can also increase the resolution so give this more columns  and something like this will be fine and now with this geometry we can use our good old vdb  from polygons so let's drop down a vdb from polygons from here and if I increase the voxel size we  can see something weird happening in our viewport and this is one point that I forgot to touch on  earlier in order to turn geometry to volumes we need to have closed geometry so over here we can see  that on both ends we have these holes in the geometry and to fix this with the sweep node we can  scroll down here and let's go to end caps and we'll set the end cap type let's do a single polygon  so now we have closed off this geometry and if we go back to the vdb from polygons we can see we get  expected result so I can increase the resolution here and everything works as expected so keep in mind  if you are using this vdb from polygon and you don't have the expected results this usually means  that the geometry is not closed off so from here we need a fog vdb so if we go back to our graph  the velocity field is a fog volume class so let's go back here and I'll just turn on fog vdb  and turn off the distance vdb now if I look at the information here this by default will give us the  regular float type of fog volume so again if we go back here this is the default that our vdb  from polygons will do and we need a way to turn it into this vector type so from here we can do a  convert vdb and we will convert to by default this is set to volume but let's say convert to vdb  which it already is but now we have access to our class and the type as well so for class we already  have what we need which is a fog volume we can see that we can actually convert fog to sdf and  we can also do the other way around so if we were working with a sdf here so if I turn this to  surface just for demonstration if I go back here we can do convert sdf to fog so this is also  very useful as well let's just do our regular fog vdb and here we will say no change and now for our  vdb type we actually want a vector float now we don't have a visual representation in our viewport  that this changes but if I go to the information here we can see now that a voxel store a vector  information so previously over here where it says vector s this was float so now our density vdb  is actually a vector volume and on this note we can change this density name we can go to our vdb  from polygons and let's just name this to vel again you can use whatever name you want here but  usually hudini will look by default for this vel name when we are doing a pyrosimulation or  other operations down the line you might also find velocity volumes named as v so sometimes it's  just a simple v and this is also how some of the hudini nodes are configured to look for a volume  that's named v but I'll just keep this at vel and we can move forward from here so what I want to do  is give this velocity field a direction that goes along the curve and what we can do is create  this direction as a attribute on our curve geometry and then use a volume v on our velocity field  to sort of borrow this attribute from the geometry so from our resample over here I'll just drag  all of this to the side let's go to our resample where we have the points on the geometry we can do a  polyframe from here so let's drop down a polyframe and here we have this tangent option which will  give us a vector that points from one point to the next point and this will create this direction  that we want and I'll turn off this normal name and instead of the tangent name tangent U I'll  just name this there for direction and if we go to the information tab and let's create a  visualizer for this here we can preview the direction of our vector so this is what we want let's  turn this off and from our velocity field let's do a volume VOP let's connect this here to our field  and we want to reference our curve geometry so from this polyframe let's plug the polyframe node  as the second input in this volume VOP and let's step inside so in order to grab this  there attribute from the geometry we can open a point cloud on our curve geometry so from  open put tool which again references this curve let's do a pc open so point cloud open so this  pc open we talked about in the intro to VOP series but I'll try to explain this again in relation  to volume so here I have a cross section of our volume and let's imagine that this grid  represent our voxels and here we have let's say that this green line represents our geometry  and because we created this volume with a VDB from polygons we in reality we sort of have a  border that looks something like this so this red outline so it's not a box container as it was in  our first example when we created a velocity field using the simple VDB node now again this will  depend on the effects you're trying to create a lot of times you might want the full box container  and sometimes you can get away with less voxels such as this case so the pc open will open a point  cloud from the position of each voxel and again this will be a point that lives right in the center  of each voxel and for example for this point it's going to open a search radius and this search  radius needs to be big enough to contain the points of our geometry so I have to set the  search radius to a high-nav value until it reaches the actual geometry so something like this  and this will be in world units and we'll see this in action in a second and now that the  search radius can contain the points of our geometry of our curve let's say that it found maybe  these four points on our geometry we can also specify the total number of points that we want  to consider so for example even if the search radius is big enough we can limit this to only  select two of the points that it finds first so usually the less points we search for the faster  disoperation will be so that's also something to keep in mind but let's go back to our examples  so let's say that we found four points now we can grab attributes from these and if we want to  grab the position attribute we will return the average of the position of all of these points  so in this case we might return a position value that's maybe somewhere around here so an  average between all of these positions and then we will do this for all of the voxels at the same  time so we open up a search radius so for this voxel for example we find all of these points over  here and the average of these might be a point that somewhere around here and for this point  with the average of the points might be here and for this one we might return this now we can  grab any attribute doesn't necessarily have to be positioned so if we had a float value on our  curve geometry that we want to average and let's say this float value might be a value of two for  this one 1.5 for this point and one over here the average of these points will return us a value  of 1.5 so we can also grab any attribute that we want and in our case we will return that  the attribute so the vector that points along the curve and this averaging here of the values  usually will provide us with very nice and smooth results and this is constantly used in simulations  so if we go back in Houdini the file on this PC open will simply specify which geometry we want to  consider so again this will be our curve geometry we want to open the point cloud from the position  of our voxel so we will plug the P inside the P here and then here we have the search radius  and the number of points so again the search radius we will have to increase far enough for the  voxels to reach our curve geometry so I'll just set this to an initial value we'll have to preview the  velocity field in a second to actually see this in action and again the number of points specify  how many we want to consider so we actually don't really need to average out 10 points in this case  after maybe averaging five points we can get a pretty accurate estimation so considering more  points in our averaging operation doesn't really give us a better result maybe I'll just set this  to a number of points five and we can always just increase this if we find that we're not getting  the most accurate results again the search radius and the number of points kind of go hand in hand  and for both values we want to use the absolute minimum necessary to get our results the more we  increase the search radius and the number of points the slower the operation will be and now to get  the attribute that we want from our handle we will do a PC filter and this is what will average out  attribute that we need so in this case it's set to the position by default and we said that we  want to grab the dirt attribute and the signature here will be a vector channel so we'll leave this  as it is let's do now a bind export to our volume and let's export this back to our  well field and let's set the type here to three floats and here we don't need the density so again  we're previewing this as a fog volume and this is not useful let's go up let's do a null from here  and this will be our well and again we will use a volume trail so from our sweep over here let's  scatter points inside this geometry we will do a points from volume let's maybe give this a  points separation of zero point two and we can plug this in a volume trail node as the first input  and we will plug the velocity volume as our second input so now let's see what we get  and we kind of see now this vector in action and now I can go back to our pc open values over here  and we can see for example if I were to decrease the search radius we no longer get the information  that we need on the voxels that are further outside so if we go back to our graph these voxels that  are further away from the geometry don't have a search radius that's big enough to actually find  the points on our geometry in order to retrieve the attribute that we want so this is why the  search radius needs to be big enough so I'll just increase this until each voxel can find points  on the curve geometry so maybe somewhere at around point five will be okay in this case and if I  increase the number of points here we can see that we get the same result when I increase this but  if we have two few points we might not get the correct result we can see that in this case even  with one point we get a pretty good result let's just set this to a value of five and keep this  or maybe let's just use four so we can now see this in action so if I go up we can go back to our  setup with the particles so let's grab our sphere let's maybe just template the volume trail with  E I'll go to my sphere and let's press enter and I just want to move this to the end of our sphere  so let's just move this up and we have to make sure again that this is actually fully inside our  velocity field so let's do something like this and I might want to reduce the uniform scale a  little bit so now if I grab this velocity so let's grab this null node press Ctrl C and we'll  replace the top f inside the pop network over here for the pop-up vector by volumes we will replace  the top f with our new velocity so let's press enter and now if I simulate we can see that the  points are going in the opposite direction so to reverse the direction of our vector we can go  back inside our volume vop let's maybe just preview the volume trail so if we go inside the volume  vop to reverse this direction we can do a negate here which just multiplies the vector with a value  of negative one so now if I go up our points should be going in the correct direction so let's  press play and now we can see that we are following this velocity volume and we can also preview our  trails and this is the result now naturally when they escape our field they stop moving and  this is usually fixed if we make our particles die off when they reach this point or we can create  lots of different logic that simply removes the particles when they reach a certain point or we  might have a camera in our scene that doesn't even capture this part so this is just standard  now obviously to make this more interesting we can introduce some noise so back in our volume  vop let's generate a let's do a curl noise of this time so we will do a vector to vector 4 and the  curl noise can use four values and we'll use the fourth dimension here we'll plug this into our  time so we can animate this noise and from here we'll do a curl noise let's set that type to  simplex and we will add this to our velocity so after the negate here we'll drop down an add  and let's plug this over here and we can already see this change in our viewport so let's right  click and choose to promote input parameters so now we can go up let's go let's just preview our  volume trails and by default if I play this will also have animation and this fourth dimension in  the frequency is what's going to determine the speed of this so if I grab this fourth value here  and I decrease the amount we can see our noise animating slower so maybe I'll use a value of  a point one in this case we can increase the amplitude and we can play around with the offset as well  so let's see now what result we'll get in our simulation and let's press play all right now the  amplitude might be a little bit too strong and we can see again when they escape here the velocity  field on the borders they stop moving entirely but they kind of maintain this direction I might want  to decrease the amplitude here let's use points for maybe all right so this is more like it and we  can see we have this nice fluid kind of animation really here sky is the limit as far as modifying  this volume is concerned so let's say for example we can see that as the particles progress  further to this through this velocity field they start to get a little bit more chaotic and let's  say that we also want to push the particles towards our curve as they progress so back to our volume  trails we can grab the position of our curve and generate a vector from the position of each  voxel to this position on the curve so back in our volume valve again from our pc open we can do  another pc filter so I'll hold out to duplicate this and we will use the position as our channel  so on the points that we find over here we will average their position and when we grab the  position of each point so something like this if we subtract the position of the voxel with  this position that we find we create a vector that points towards the curve so for this voxel we  subtract the position with what position it finds with the pc open and we have this one and then in  this case we'll have this vector so this is what's going to help us push the particles back towards  the curve so let's go back here and with this pc filter we grab the position and we will subtract  the position of the voxel so let's do a subtract from here let's plug the pc filter as the second  input and we will just simply add this result to our velocity and we can kind of see this happening  here we can also scale this vector if we do a multiply after the subtract and I want to promote  the second input here let's maybe rename this to push back so it's clear what this does and if I  go up as I increase this value now we can see how our vector field changes and I think this has to go  in the opposite direction right now as it is I think it's actually pushing it further away from  the geometry okay so this is what it does so let's reverse this direction of this vector we can  step back inside and we can simply reverse the order of subtraction here so I'll grab this  node press shifter and now we have the actual vector that we need so if I go up we can see now how  this vector field points towards the curve so let's just give this something like 0.5 and let's see  the result now so let's press play and now over time as our simulation progresses we can see that it  travels further and further closer to the geometry so now I can go back here and I can increase the  amplitude of our noise and maybe I can decrease this pushing back vector and let's press play and we  can start having a lot of fun with this now so now we also don't run into that problem of the  particles escaping the velocity field and as I play around with some of the settings we can  get some really cool results we can also animate this noise faster so I can increase the fourth value  here and this is the result that we get so we can see it's a nice and smooth animation and it  follows our curve pretty closely so we can really do a lot of this and here I'm just barely  scratching the surface now so far we've been using mostly geometry that has polygons to create

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_012.jpg

### SDF From Particles [118:28]
**Transcript:** volumes based on the geometry but we can also turn geometry that only has points into volumes so if  I create a simple point here I'll drop down an add click this plus a button here and this will  create a point in the center if we want to turn this into a volume we can use a VDB from particles  so let's drop this here and append this and this will create a sphere volume based on our points  and here we have a few settings so first of all we have the usual voxel size that determines the  amount of voxels that we have then we can either create an SDF or distance VDB and we can also  just create a fog VDB like so there's also this mask VDB option but this honestly doesn't come  up that often don't worry about this for now we have our two main classes so the fog VDB and SDF  or distance VDB let's just keep this as an SDF for now and check some of the other settings point  radius scale determines how big will draw our volume in our scene so we can control the size of  this volume now if we have more points this will be applied to all of the points and minimum radius  in voxels determines how many voxels a point needs to cover in order for it to be drawn so if we go  back to our cross section here if we look at this voxel here let's say that there's a point the  blue dot let's say that we have this point that's right in the center of the voxel now it doesn't  necessarily have to be in the center of the voxel let's just say that this is in the center just  for the sake of demonstration if the radius that we set for our points doesn't cover this let's  say 1.5 span of voxels then it won't get drawn so we can see here that it covers this whole voxel  and maybe it covers at least half of the voxel next to it so if this radius wasn't big enough so if  this was smaller to maybe 1.2 it will no longer get drawn so this needs to be at least covering one  and a half voxels around the point so back in Houdini if we have a voxel size of 0.1 if we set this  point radius scale here to 0.1 as well it will perfectly fit only one voxel so it's it will look  something like this so now this minimum radius will ignore this point but when the scale goes above  1.5 so if I increase this when we go above 0.15 so to 0.16 now this condition is met and we can draw  our sdf in the viewport now this value of 1.5 is what Houdini recommends that a point should at  least cover 1.5 voxels to not get artifacts but a lot of times I reduce this minimum radius scale  to match the size that I'm using so if I were in fact using a point radius scale of 0.5 I can  just reduce this until we get our volumes working so I wouldn't worry too much about this size over  here to be honest if your volume gets some really nasty artifacts this setting here is something  that you should look into but generally I have said this to a lower size and I got the results that  I wanted let's reset these values for now now the point radius scale here is really just a  multiplier of a p-scale attribute that exists on the points now if our point in this case doesn't  have a p-scale attribute Houdini will assume a p-scale value of 1 if this doesn't exist so if I  set this to back to a value of 1 for the point radius scale if on my point I add a p-scale  attribute so let's drop down an attribute create I can create a p-scale attribute so I'll  set the name here to p-scale and I'll just set the value here let's maybe leave the value at 0  and if I go to the vdb from particles now if I increase the p-scale value so if I scroll this up  we can see that this changes the size of our volume so I can increase this and decrease this and  now if I go back to the vdb from particles this radius scale like I said will act as a multiplier  so I can still control the size using this option instead so whether you want to adjust the size  directly on the points with a p-scale or you want to control the size here directly this will be  up to you they both will do the same thing and usually we want to turn points into sdf volumes when  we are measuring liquid simulation so let's see how this works on our previous example here I will  set the p-scale attribute in this attribute create let's set this value to 0.1 and I will connect  this to our popnet so I'll drag this to the side let's look at our result in the vdb from  particle so we can see nothing happened and this is because our point radius scale is too small so  as I increase this we should start to match the conditions that we need so we probably need to go  above 1.5 because we have this p-scale value here of 0.1 so when I go above 1.5 we can start to see  this now so if we had the same value that we had before so if I drop this point radius scale to 0.25  I can also increase the amount of voxels we have until we find the right value to create our volume so  if I reduce the voxels size here I will hold down shift and use my scroll wheel to go down we can  reach a value we can see 0.01 in this case where we have enough voxels so that this point radius scale  will match this condition so if we go back to our cross section if we look at this voxels here our  point scale this radius here this will stay the same but when we introduce more voxels we are  actually dividing up the grid like this so let's create maybe one more division now we can see  that the radius actually covers maybe two and a half voxels since we divide this so this condition  here minimum radius will be matched even though the point radius scale will stay the same so all of  these values voxels size point radius scale and minimum radius are all very tightly linked with  each other and all of these usually require something as soon as to get the correct result now we have  a pretty okay resolution for our volumes and with more voxels we can get away with a lower radius  scale so I might want to reduce this and we can see we reach this 0.5 the 0.15 value here where we  call our points again I can set this to a lower value and just reduce the minimum radius but let's  keep this at 1.5 and just set the radius scale to the lowest value that we can possibly use and now  to turn this sdf into actual geometry we can do a convert vdb and we'll set this to polygons  all right so here we have it and the power of the sdf is that we can use all sorts of sdf  operations so after we turn our points into sdf we can use a vdb smooth sdf and I will decrease  the iterations here and we can use different algorithms to see what kind of results that we get  and usually with fluid I think this mean curvature flow is what's generally used so we can go back  to our geometry result and after the vdb smooth I can also do a vdb reshape sdf and we can push  this volume out or we can erode it so we can eat away parts of our volume we can squeeze our mesh  I can place this vdb reshape before our smooth for example and I can dilate this so I can push this  out like this then I can smooth this out so I can increase the iteration and I can combine this  with another vdb reshape sdf set this to a row all right so now we can see how easily it was to smooth  out this result so if I disable all of these nodes we can see that this was the initial result where  we just turned the points into volumes and when we do all of these operations now we get this nice  liquid kind of mesh so hopefully this gives you an idea on how to mesh particles and there's  really a lot of things to consider when you want to mesh fluid and there's a whole art to it really  fluid meshing probably has its own fanbase but at its core it's really just turning the points into  an sdf volume than doing multiple operations on volumes and also optimizing the points themselves  before you turn them into volumes now like I mentioned before we can either turn the points

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_013.jpg

### Volume Rasterize Attributes [127:23]
**Transcript:** into an sdf or we can turn them into a fog volume like so but there is a better way to use the  fog volume version and this is by using a volume rasterize attribute node so let's drop this down  over here and we kind of have some of the settings that we are used to we have the Vox  size and the particle scale so this particle scale here will also act as a multiplier kind of like  it does over here in the VDB from particle nodes let's go ahead and just duplicate the attribute  create node which has our p-scale value and the way the volume rasterize works is that it's going  to look for an attribute and base the density or whatever kind of volume we want to create on that  attribute so this is why we don't have anything currently showing because we have to specify which  attribute we want to rasterize so over here we have all of these attributes that we get from our  popnet simulation but let's go ahead and just create a simple density attribute that has a value  of 1 over all of the points so in our attribute create i will hit this plus to add another attribute  i will set the name here to density and i will just give this a value of 1 and now in our volume  rasterize attributes we can specify the attribute here let's type density and now we can see our fog  volume and we can increase the Vox size here so we can reduce this and in the case for the volume  rasterize attributes we don't have to worry about meeting this minimum radius in voxel threshold  this option here is more when we are dealing with sdfs to be honest so in our rasterize attributes  we don't have to worry about this and i can set this to let's set this to a lower size so 0.01 to have  a lot of voxel information and now again we have a p-scale attribute on our points and this  particle scale here will act as a multiplier this minimum filter size here is honestly something  that you should just leave as it is we can see that as i change this nothing really happens  there are a few cases where you might want to change this this filter size and this again  is a way for Houdini to kind of fix artifacts between voxel interpolation but you really shouldn't  worry about this this option for now so let's leave the particle scale at 0.5 for now and we can  see that we essentially now have a small simulation so we can render this now as a volume instead of  particles and this is how a lot of magical effects are created we create a particle simulation and  then we render those as a volume and the cool thing here is that we have this velocity blur  option so if we go to our points we have velocity on all of our points that we got from our  particle simulation and this volume-restarize velocity blur option will smear the density along  these velocities from our points so when i turn on velocity blur we can kind of see something  happening and if i go to the settings here i can increase the shutter so this will smear the  density even more and we can also give this more samples which will increase the quality  and the shutter offset specifies in which direction we should smear and generally we should set this  to a negative one value so we can smear the voxels backwards because it makes more sense in a simulation  we want to trail backwards from our position so we can see as i change this shutter offset from one  to negative one our voxels are kind of going backwards a little bit now this isn't really the  highest quality result and even if we decrease the voxelsize here so we use a grid that's twice  as big we still didn't improve this quality and if i decrease the voxelsize we can probably  set the particle scale to a lower value as well and if i set this to a lower value here we can see  now better this velocity blur in action so when i turn this on and off we can see how all of the  voxels are now smeared in this velocity direction from the points so this is super helpful especially  when rendering and usually when we want to turn the particles into smoke we need to be working with  a lot more points so in the particle simulation i'll just go here for my pop source and in the  birth tab i'll increase the constant birth rate let's add 10 times more particles so i'll add  another zero here and let's go to the simulation and re-simulate so now essentially the more points  we have that can be contained within a voxel the better and smoother result we will have so in our  volume-restrival attributes we see now a much smoother result and with more points we can lower  the voxelsize so i can go even lower now to get a better result and i can decrease the particle  even more so if i set this to 0.1 now we can see we get very nice smooth results and this velocity  blur we can really see now in action so this is already looking pretty cool so we are  restaurizing this density attribute from our points and currently our density has a value of 1  across all of our points but we can use different values per each point so if i use an attribute  noise float let's set this over here and let's change our density i'll set the operation for the  noise let's multiply this ranges 0 centred and let's also preview this if i click on this icon over  here here we can see the values of our noise and maybe we can enable ramp so we can further  control this and i can increase the amplitude so now the red values will have the full density and  violet areas will have no density so when we go to the volume-restrival attributes here we can see  these gaps in our density we still have voxels here they just have a density of 0 so it kind of  looks like the volume is empty on these sides and it has a full density value over here again if we  go to our points we can see the pattern that our noise creates so previously when we didn't have  noise on our density we had this result and with noise now we have this so this is also one of the  strengths of the volume-restrival attributes because the attributes will usually have different  values per point this volume will use all of that information and we don't necessarily need to  restrivalize only the density we can add as many attributes as we want here so because our  particles have points we can also restrivalize the velocity attributes so if I add here v to this list  now we also have a velocity field so if I go to the information here we have the density which is  this flowed value and we have our v which is a vector three types so we can very simply create a  velocity field with this volume-restrivalize as well and this comes up in a lot of cases so if I  want to preview this velocity field let's go to our points and I don't really want to trail  200,000 points so what we can do here is simply merge the points that are close to each other  let's drop down from our popnet we'll do a fuse and we will just increase this snap distance  and we can see that as I increase this it will simply average all of the points and merge the  points that are close to each other so now we have only 3,000 points and we can do a volume trail  from here let's plug the volume-restrivalize as the second input and let's look at our trails  to preview this velocity field here we don't have the correct result it's going to grab the density  volume to trail the points which doesn't make any sense so when we have multiple volumes on any  given object here for the volume trail we actually have to specify the velocity volume  ourselves so let's point this to the V volume and now we can see our velocity field so we are  turning the points into volumes and then we are storing the information from the points inside  those voxels and in this case we are storing the density and this can be used for shading and  we are also storing the velocity if we further want to use this as a velocity field later so  essentially with this velocity field I can use this to push out smoke or another particle simulation  and over here where we created our initial volume velocity using the regular VDB node we could  use the box container to scatter points and then we could have created a velocity attribute on all  of these points and then use the volume-restrivalize attribute to turn this into the velocity field so to  have this result so whether you want to use geometry or points to create volumes will be up to you and  it will depend on a case by case basis and you might find a lot of tutorials that use the volume-restrivalize method  instead so some people prefer to just work on points to get the directions that they need so the  velocity attribute and essentially in this volume-vob here where we decided how our velocity field should work  all of these operations if we just simply add all of these two our points inside the  netribute vob we can then resturize the points and we get sort of the same result usually for  velocity field like I said I like to create the volume from scratch but there are cases where you  might want to use points instead and resturize those to create the volumes

**Frame:** tutorials\frames\intro-to-houdini-volumes---beginner-course\frame_014.jpg


---

## Structured Notes

### Core Technique
Houdini volumes = 3D grids of voxels storing float (scalar) or vector values. VDBs are sparse grids (only non-zero voxels stored) — always prefer VDB over standard volumes. Two VDB classes: fog (float=density/temp, or vector=velocity) and level-set/SDF (signed distance). Volume VOP operates per-voxel using voxel center position; always multiply noise by existing density to preserve borders. For SDF: add/subtract float values to push surface in/out; fill interior critical for operations inside mesh. Velocity fields: VDB node (fog, vector float, velocity type) + VDB Activate for container; animate with curl noise 4D; use PC Open + PC Filter to borrow direction from curve geometry. Volume Rasterize Attributes = preferred way to turn particle attributes (density, v, CD) into volumes.

### Summary
Voxyde VFX comprehensive volumes beginner course (137m16s, 15 sections). Covers voxel math and resolution control (by-size mode, 0.1 = 10 voxels per unit), VDB sparse grids vs standard, SDF (signed distance field — positive outside, negative inside), Volume VOP internals (position = voxel center; bind/bind export; density vs field name), practical fog VOP examples (gradient fade via bounding box ramp, position fade via sphere + distance, animated noise with $D expression), working with multiple named fields (bind name=temperature/velocity; fields must pre-exist for bind export), Volume Visualization + Pyro Bake Volume for viewport display, SDF VOPs (add/subtract, fill interior, Convert VDB, VDB Smooth SDF, VDB Reshape SDF), SDF from noise patterns (cellular, hexagon, Voronoi, Worley = rock), velocity field types and construction (VDB + VDB Activate, Volume Trail visualization, POP Advect by Volume), curve-based velocity field with PC Open, SDF from particles (VDB From Particles → VDB Smooth → mesh), and Volume Rasterize Attributes for particle-to-volume conversion with velocity blur.

### Key Steps
**VDB Fundamentals:**
- Always use VDB from Polygons (not ISO Offset) for geometry → volume conversion
- By size mode: voxel size = 1 unit ÷ N voxels; smaller value = more voxels = better quality
- Exterior/interior band voxels: use world space units; fill interior = ON for inward operations
- Standard volume = full bounding box filled; VDB = sparse (only active voxels, 10× fewer)

**Volume VOP Essentials:**
- Position (P) = center of each voxel — same coordinate system as scene
- Default exports to "density" — change with bind export + explicit name
- Bind export CANNOT create new volumes; field must pre-exist (merge two VDB from Polygons first)
- Always multiply noise result by existing density: `density × noise` — otherwise fills entire bounding box
- Copy nodes freely between Volume VOP and Attribute VOP (same VOP architecture)

**SDF Operations:**
- `add constant`: positive → push surface IN; negative → push surface OUT (opposite intuition)
- `multiply constant`: no visible effect on SDF (values change but surface position unchanged)
- Noise displacement: use 1D (float) not 3D (vector) — SDF is a scalar field
- Fill interior: required when pushing surface in more than exterior band allows
- Convert VDB → polygons: adaptivity controls mesh density; tied to voxel size resolution
- VDB Smooth SDF: blur/smooth surface; operations: mean curvature flow best for fluid meshing
- VDB Reshape SDF: dilate (expand) or erode; same as add/subtract but more control

**Velocity Field Construction:**
- VDB node: name=vel, class=fog volume, type=vector float, vector type=displacement/velocity/acceleration
- VDB Activate: reference mode uses bounding box of reference geo (not surface); expand mode adds padding
- Volume VOP inside: bind export vel (3 float); turbulent noise must be 3D signature for vector
- Volume Trail + Points From Volume = visualization; POP Advect by Volume = actual simulation use
- Advection type: "update velocity" more accurate than default; particles maintain last registered velocity after escaping field

**Curve-Based Velocity Field:**
- Sweep + end caps → VDB from Polygons (fog) → Convert VDB (vector float, velocity type)
- Polyframe SOP on curve: tangent attribute named "dir" (direction along curve)
- PC Open: position=voxel P, search_radius must reach geometry, num_points=4-5
- PC Filter: channel="dir" → gives averaged curve direction per voxel
- Negate vector to correct direction; subtract position for push-back-to-curve vector
- Curl noise 4D (pos → float4 + time) → animated organic direction

**SDF from Particles / Fluid Meshing:**
- VDB from Particles: point_radius_scale × pscale = effective radius; minimum_radius_in_voxels = 1.5 (minimum to avoid artifacts)
- Lower minimum_radius_in_voxels to match your usage if needed
- Typical meshing chain: VDB from Particles → VDB Reshape SDF (dilate) → VDB Smooth SDF → Convert VDB (polygons)
- Mean curvature flow smoothing preferred for fluid look

**Volume Rasterize Attributes:**
- Set density attribute on points first (value=1 or noisy); specify attribute name in node
- Particle scale = pscale multiplier; no minimum radius threshold concern (unlike VDB from Particles)
- Velocity blur: shutter controls smear length; shutter offset = -1 (trail backward); samples for quality
- Multiple attributes: density + v (velocity field) listed together; use Volume Trail on fused points to preview velocity
- More points = smoother volume; more points allow lower voxel size

**Practical VOP Patterns:**
- Gradient fade by height: Relative to Bounding Box → Vector to Float (Y) → Ramp → multiply density
- Animated noise: turbulent noise with $D expression on offset (div by speed factor)
- Ground fog: bounding box + ramp + noise + box geometry
- Position fade from object: Import Point Attribute (sphere P) → Distance → Fit Range → multiply density
- SDF reveal animation: Relative to Bounding Box Z → Compare (> value) → add to surface; noise displaces bounding box lookup

### Houdini Nodes / VEX / Settings
- **VDB from Polygons**: fog VDB or distance VDB (SDF); exterior/interior band voxels; fill interior; world space units
- **Volume VOP**: per-voxel operations; position = voxel center; bind (input) + bind export (output); density default output
- **Volume Visualization**: smoke tab (density ramp, density scale, diffuse color) + emission tab (emission field, emission scale, emission color ramp); viewport-only, no actual data change
- **Pyro Bake Volume**: smoke + fire + scatter tabs; render-preview-grade quality in viewport
- **VDB Activate**: reference mode (bounding box of geo) + expand mode (add rows of voxels); expand distance = world units
- **VDB node**: create empty VDB container; class=fog volume; type=float/vector float; vector type=color or velocity
- **Volume Trail**: visualize velocity field; 2nd input = velocity volume; V Volume = specify by name when multiple volumes
- **POP Advect by Volume**: 2nd input or sub-path to velocity volume; advection type = update velocity
- **VDB Smooth SDF**: smooth SDF surface; mean curvature flow for fluid; iterations controls smoothness
- **VDB Reshape SDF**: dilate (expand), erode (shrink), smooth; expand before smooth in meshing workflow
- **VDB from Particles**: pscale × point_radius_scale = effective sphere size; minimum_radius_in_voxels threshold
- **Volume Rasterize Attributes**: converts point attributes to volumes; velocity blur (shutter, shutter offset, samples)
- **PC Open / PC Filter**: sample curve/geometry attributes at each voxel position; search_radius must contain nearest point
- **Convert VDB**: polygons or VDB; class/type conversion; fog→SDF and SDF→fog possible; adaptivity for polygon density
- **Polyframe SOP**: tangent attribute for curve direction; name it "dir" for velocity field workflow
- Relative to Bounding Box: gives 0–1 range per axis; use with ramp for gradient fades
- Name SOP: renames VDB field (string attribute "name")
- **VDB Activate** expand mode vs VDB Reshape SDF: functionally similar; reshape has more options

### Difficulty
Beginner-to-intermediate — conceptually approachable but many subtle behaviors require hands-on experimentation

### Houdini Version
Houdini (any modern, H18+); VDB operations and Volume VOP stable across versions

### Tags
#volumes #vdb #sdf #velocity-field #vop #point-cloud #volume-rasterize #beginner-intermediate

---

## Related Tutorials
- `intro-to-vops---houdini-beginner-tutorial.md` — prerequisite: VOP fundamentals
- `intro-to-houdini-pyro---full-beginner-course.md` — builds directly on volumes foundation; pyro target fields and micro-solvers
- `intro-to-houdini-particles---full-beginner-course.md` — particles + velocity field + POP Advect by Volume
- `improve-solaris-performance---houdini-tutorial.md` — proxy VDB workflows for Solaris pipeline
- `houdini-tutorial---wispy-smoke.md` — practical velocity field application for smoke FX
